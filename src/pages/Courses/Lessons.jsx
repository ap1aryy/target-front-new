  import './Chapters.css';
import {
  Title, List, Button, Text
} from '@telegram-apps/telegram-ui';


export const Lesson1_1 = () => {
  return (<List>
    <Title weight="7" style={{ marginTop: 16 }}>1.1 Обзор экосистемы TON</Title>

    <Title style={{ fontSize: 16 }}>Дорогие участники!</Title>

    <Title style={{ fontSize: 16 }}>
      Добро пожаловать на наш курс по разработке на блокчейне TON — в захватывающее путешествие в мир передовых технологий и децентрализованных приложений.
    </Title>

    <Title style={{ fontSize: 16 }}>
      Мы уверены, что успешное прохождение этого курса откроет перед вами не только новые горизонты, но и двери к возможностям, которые предоставляет экосистема TON. На пути к успеху вас ждут увлекательные задачи, вызовы и практические упражнения, которые помогут вам глубже понять и овладеть навыками разработки.
    </Title>

    <Title style={{ fontSize: 16 }}>Не забывайте, что каждое решенное задание приближает вас к заветному SBT-сертификату.</Title>

    <Title weight="7" style={{ marginTop: 16 }}>Условия для его получения:</Title>
    <ul>
      <li>Честно пройти все уроки курса, решить предложенные задачи, тесты.</li>
      <li>Подписаться на наш официальный Telegram канал: <a href="https://t.me/ton_insights_ru">TON Insights</a></li>
    </ul>

    <Title style={{ fontSize: 16 }}>Желаем вам вдохновляющего обучения и множества проектов! Поехали!</Title>

    <Title weight="4">💎 Добро пожаловать в первую часть!</Title>

    <Title style={{ fontSize: 16 }}>В этой части вы рассмотрите весь блокчейн TON целиком, его архитектуру, принципы построения и важные компоненты.</Title>
    <Title style={{ fontSize: 16 }}>Один из уроков будет посвящен структуре блокчейна и подробному описанию его внутренних процессов. В другом уроке мы остановимся на принципах работы блокчейн-сетей в целом и их эволюции на протяжении трех поколений. Наконец, мы углубимся в контракты и токены сети TON.</Title>
    <Title style={{ fontSize: 16 }}>К концу части у вас будет всестороннее понимание экосистемы TON и ее компонентов.</Title>

    <Title weight="4" style={{ marginTop: 16 }}>📝 Вы узнаете:</Title >
    <ul>
      <li>о кастодиальных и некастодиальных сервисах, а также разнице между ними;</li>
      <li>о различиях между централизованными и децентрализованными сервисами;</li>
      <li>что можно создать на блокчейне с помощью смарт-контрактов.</li>
    </ul>

    <div className="video-container">

      {/* Вставьте видео по желанию */}
    </div>

    <Title weight="4" style={{ marginTop: 16 }}>📝 Что вы теперь знаете</Title >
    <ul>
      <li>Чем кастодиальные кошельки отличаются от некастодиальных.</li>
      <li>Блокчейн сам по себе является бэкендом для децентрализованных приложений.</li>
      <li>Всё в блокчейне, по сути, представляет собой смарт-контракты (децентрализованные обменники, токены, доменные имена).</li>
    </ul>

    <Title weight="4" style={{ marginTop: 16 }}>📚 Конспект урока</Title >
    <Title style={{ fontSize: 16 }}>В этой части мы обсудим общую архитектуру, принципы построения и другие важные компоненты платформы TON.</Title>

    <Title style={{ marginTop: 12 }}>Что такое TON?</Title>
    <Title style={{ fontSize: 16 }}>
      TON — это платформа для децентрализованных приложений, объединяющая пользователей, валидаторов и разработчиков самих приложений. Это больше, чем просто блокчейн.
      Блокчейн TON объединяет три стороны:
    </Title>
    <ul>
      <li>пользователи, владеющие ассетами в TON;</li>
      <li>валидаторы, управляющие консенсусом сети;</li>
      <li>разработчики приложений, предоставляющие сервисы на основе сети.</li>
    </ul>

    <Title style={{ marginTop: 12 }}>Кастодиальные и некастодиальные сервисы</Title>
    <Title style={{ fontSize: 16 }}>Приложения и сервисы делятся на две группы:</Title>
    <ul>
      <li>кастодиальные,</li>
      <li>некастодиальные.</li>
    </ul>

    <Title style={{ fontSize: 16 }}><strong>Кастодиальные сервисы</strong> — это сервисы, управляемые той же стороной, что несет ответственность за средства, размещенные на этих сервисах.</Title>
    <Title style={{ fontSize: 16 }}>Онлайн-биржи обычно являются кастодиальными, поскольку им нужно использовать средства с кошельков для того, чтобы гарантировать заявленный обмен.</Title>
    <Title style={{ fontSize: 16 }}><strong>По этой причине онлайн-биржи здесь представляют риск, поскольку злоумышленники могут взломать их и украсть средства без возможности восстановления.</strong></Title>

    <Title style={{ fontSize: 16 }}><strong>Некастодиальные сервисы</strong> – это сервисы, которые не хранят средства пользователей и оставляют их полностью под контролем владельцев.</Title>
    <Title style={{ fontSize: 16 }}><strong>Поскольку все средства остаются в распоряжении пользователей, взламывать системы становится сложнее.</strong></Title>

    <Title style={{ marginTop: 12 }}>Централизованные и децентрализованные приложения</Title>
    <Title style={{ fontSize: 16 }}>Приложения могут быть:</Title>
    <ul>
      <li>централизованными,</li>
      <li>децентрализованными.</li>
    </ul>

    <Title style={{ fontSize: 16 }}><strong>✨ Централизованные приложения</strong> — это приложения, работающие на сервере. <strong>Децентрализованные приложения</strong> не требуют наличия сервера, а запускаются напрямую на смарт-контракте блокчейна.</Title>

    <Title style={{ marginTop: 12 }}>Что создают на основе блокчейна</Title>
    <Title style={{ fontSize: 16 }}>С помощью блокчейна можно создать разнообразные инструменты и сервисы.</Title>
    <Title style={{ fontSize: 16 }}>Смарт-контракты лежат в основе:</Title>
    <ul>
      <li>децентрализованных бирж;</li>
      <li>токенов:</li>
      <ul>
        <li>валют,</li>
        <li>коллекционных токенов,</li>
        <li>служебных токенов;</li>
      </ul>
      <li>аукционов,</li>
      <li>доменных имен.</li>
    </ul>

    <Title style={{ marginTop: 12 }}>Заключение</Title>
    <Title style={{ fontSize: 16 }}>Экосистема TON объединяет кошельки, обменники, трейдеров и пользователей, взаимодействующих через смарт-контракты. Этот курс посвящен авторизованным смарт-контрактам, их развертыванию в TON, разработке дополнительных функций и взаимодействию между централизованными и децентрализованными приложениями.</Title>

  </List>)
}

export const Lesson1_2 = () => {
  return (
    <div>
      <Title weight="7" style={{ marginTop: 16 }}>1.1 Обзор экосистемы TON</Title>

      <Title weight="4">Что вы теперь знаете:</Title>
      <ul>
        <li><strong>TON</strong> — это реестр изменений стейтов, что является более широкой концепцией по сравнению с реестром переводов.</li>
        <li>В блокчейне TON нельзя мгновенно получить стейт другого контракта. Это возможно только через сообщения, поскольку TON является асинхронной системой.</li>
        <li>Сеть TON гарантирует, что каждое сообщение будет доставлено, но не гарантирует время доставки.</li>
        <li>Каждый контракт имеет собственный микроблокчейн.</li>
        <li>Обеспечиваемый валидаторами консенсус гарантирует, что сообщения рано или поздно будут доставлены, что они не повторятся и двойная трата средств будет исключена.</li>
      </ul>
      <link></link>
      <Title weight="4">📚 Конспект урока</Title>
      <Title weight="4" style={{ marginTop: 16 }}>Блокчейн и изменения стейтов</Title >
      <Title style={{ fontSize: 16, marginTop: 4 }}>Блокчейн TON работает как реестр изменения стейтов аккаунтов сети.</Title >
      <ul>
        <li>Смарт-контракты TON тоже можно считать аккаунтами.</li>
      </ul>

      <Title style={{ marginTop: 12 }}>Что нужно знать об аккаунтах:</Title>
      <ul>
        <li>У аккаунтов есть свои хранилища;</li>
        <li>У аккаунтов есть свои уникальные адреса;</li>
        <li>Каждый аккаунт имеет свой баланс монет TON;</li>
        <li>Аккаунты хранят программный код.</li>
      </ul>

      <Title style={{ fontSize: 16, marginTop: 4 }}>На протяжении жизненного цикла смарт-контракты предоставляют почти неограниченную функциональность. Они могут:</Title >
      <ul>
        <li>Изменять данные, в том числе исходный код контрактов;</li>
        <li>Осуществлять перевод токенов;</li>
        <li>Отправлять сообщения другим контрактам.</li>
      </ul>

      <Title weight="4" style={{ marginTop: 16 }}>Однако TON предполагает и некоторые ограничения, определяемые архитектурой сети:</Title >
      <ul>
        <li>👀 Контракты не могут видеть никакие другие объекты в сети, кроме себя самих.</li>
        <li>🌐 Смарт-контракты в TON подобны компьютерам в сети Интернет.</li>
      </ul>

      <Title weight="4">Гарантии со стороны блокчейна</Title>
      <ul>
        <li>✅ Вы сможете определить адрес любого входящего сообщения и быть уверены, что за этим адресом скрывается определенный код, которому вы можете доверять.</li>
        <li>✅ Блокчейн-сеть гарантирует доставку сообщений, но не гарантирует время, которое займет эта доставка.</li>
        <li>❌ Цепочка сообщений может охватить множество шардов и потребовать много блоков для финализации, поэтому не стоит надеяться на конкретное время доставки сообщения.</li>
      </ul>

      <Title weight="4" style={{ marginTop: 16 }}>Что такое сообщения и что такое транзакции?</Title >
      <ul>
        <li>✉️ Сообщение — события, происходящие между контрактами.</li>
        <li>💎 Транзакция — активность в рамках самого контракта.</li>
      </ul>

      <Title weight="4" style={{ marginTop: 16 }}>Каждый контракт имеет собственный микроблокчейн</Title >
      <ul>
        <li>❗ Каждый контракт имеет собственные транзакции, а значит, у каждого контракта есть и свой собственный микроблокчейн.</li>
      </ul>

      <Title weight="4" style={{ marginTop: 16 }}>Двойные траты и консенсус</Title >
      <ul>
        <li>💵 Двойная трата — расход одной и той же цифровой валюты два раза и более в пользу различных сервисов.</li>
        <li>Консенсус предотвращает двойные траты со стороны валидаторов.</li>
      </ul>
      <video controls>
        <source src="/videos/1.2.mp4" type="video/mp4" />

      </video>
      <Title weight="4">Двухуровневый блокчейн в TON</Title>
      <ul>
        <li>Мастер-сеть и рабочая сеть.</li>
        <li>Рабочая сеть виртуальна — так она обеспечивает возможности шардинга всех включенных аккаунтов.</li>
      </ul>

      <Title weight="4" style={{ marginTop: 16 }}>Запомните важные свойства мастер-сети:</Title >
      <ul>
        <li>Ее нельзя шардировать;</li>
        <li>Она имеет высокие комиссии;</li>
        <li>Она содержит лишь конфигурацию сети и снимки всех подсетей от разных групп валидаторов.</li>
      </ul>

      <Title weight="4">Задержки в обработке транзакций</Title>
      <Title style={{ fontSize: 16, marginTop: 4 }}>Проведение транзакций занимает около 10–12 секунд, по 5–6 секунд в рабочей и мастер-сети.</Title >

      <Title weight="4">Заключение</Title>
      <Title style={{ fontSize: 16, marginTop: 4 }}>TON использует блокчейн-архитектуру, где каждый смарт-контракт представляет собой изолированный аккаунт с данными и программным кодом. Механизмы консенсуса предотвращают проблемы, например, двойные траты. Несмотря на некоторые задержки, транзакции подверждаются сравнительно быстро.</Title >
    </div>
  );
};

export const Lesson1_3 = () => {
  return (
    <div>
      <Title weight="7" style={{ marginTop: 16 }}>1.3 Принцип работы блокчейна</Title>

      <Title weight="7" style={{ marginTop: 16 }}>📝 Вы узнаете:</Title>
      <ul>
        <li>как спроектированы Bitcoin и Ethereum, в чем их принципиальные отличия от TON;</li>
        <li>какую проблему решают Bitcoin и Ethereum;</li>
        <li>какие недостатки есть у Ethereum и Bitcoin;</li>
        <li>в чем заключается главный принцип блокчейна TON, который отличает его от Bitcoin и Ethereum.</li>
      </ul>



      <Title weight="7" style={{ marginTop: 16 }}>📝 Что вы теперь знаете</Title>
      <ul>
        <li>Весь стейт Bitcoin — это по сути список аккаунтов, которые еще не истратили свой баланс.</li>
        <li>Ethereum старается развивать концепцию Bitcoin и сделать ее более доступной для разработчиков.</li>
        <li>Ethereum начинался с идеи, что аккаунт может включать не только логические значения, но произвольно заполняемое внутреннее хранилище.</li>
        <li>Аккаунты Ethereum могут взаимодействовать друг с другом и отправлять сообщения в синхронном режиме, подобно тому, как в приложении вызываются функции.</li>
        <li>Модель Ethereum очень гибка и удобна для разработчиков, но совершенно немасштабируема, поскольку и разработчик, и все его контракты сохраняются в общем хранилище, в общем стейте сети.</li>
        <li>Контракты в TON не могут видеть общий стейт сети, а только свой собственный, и единственный способ для них взаимодействовать с другими контрактами — это асинхронные сообщения.</li>
        <li>Контракты в TON могут быть шардированы, тогда как в Bitcoin все стейты контрактов сохраняются в едином хранилище еще не истраченных балансов.</li>
        <li>TON обеспечивает неограниченную гибкость и масштабируемость, но делает это ценой удобства разработчиков.</li>
      </ul>

      <Title weight="7" style={{ marginTop: 16 }}>📚Конспект урока</Title>
      <Title style={{ fontSize: 16, marginTop: 4 }}>В этом уроке рассматривается эволюция блокчейн-сетей в ракурсе трех поколений — Bitcoin, Ethereum и TON.</Title >

      <Title weight="4">Bitcoin</Title>
      <Title style={{ fontSize: 16, marginTop: 4 }}>Bitcoin — это в принципе первый известный блокчейн. Он был создан для адресных переводов цифровой валюты, защищенных от регуляторов. Bitcoin использует реестр аккаунтов, идентифицируемых по уникальным публичным ключам, с помощью которых и осуществляется перевод монет. Транзакции авторизуются с помощью криптографических подписей. Концепция Bitcoin включает транзакции со множеством отправителей и получателей, а также консенсус по схеме Proof-of-Work. Имеется поддержка сценарного языка, обеспечивающего сложные схемы управления транзакциями, которые, однако, ограничены в масштабировании. Для защиты конфиденциальности в Bitcoin предусмотрено создание новых аккаунтов для перевода средств.</Title >

      <Title weight="4">Ethereum</Title>
      <Title style={{ fontSize: 16, marginTop: 4 }}>Ethereum — это второе поколение блокчейна, в котором появились компонуемые контракты и гибкая структура аккаунтов. На этом этапе пользователи смогли совершать в блокчейне произвольные операции, оплачивая их выполнение. Так появились децентрализованные финансы. Но особенности архитектуры, обеспечивающие богатую функциональность Ethereum, вместе с тем ограничивают возможности масштабирования сети.</Title >
      <ul>
        <li>Ethereum пытается развивать концепцию Bitcoin и делать ее более гибкой для разработчиков.</li>
        <li>Любой account в Ethereum может включать не только логические значения, но произвольно заполняемое внутреннее хранилище;</li>
        <li>Изменения в сети — это уже не просто перевод средств с одного аккаунта на другой.</li>
        <li>Аккаунты могут взаимодействовать друг с другом, отправлять друг другу сообщения в синхронном режиме, подобно тому, как в приложении вызываются функции. Принципы Ethereum совместимы со многими известными инструментами и концепциями программирования.</li>
      </ul>
      <Title style={{ fontSize: 16, marginTop: 4 }}>❗ Архитектура Ethereum предоставляет огромные возможности для разработчиков, но она совершенно немасштабируема, поскольку и разработчик, и все его контракты сохраняются в общем хранилище, в общем стейте сети.</Title >

      <Title weight="4">TON</Title>
      <Title style={{ fontSize: 16, marginTop: 4 }}>TON — это третье поколение блокчейна, в котором были введены ограничения, чтобы обеспечить масштабируемость. Контракты в TON имеют ограниченное поле зрения и взаимодействуют с помощью сообщений, что и создает возможности для масштабирования. Консенсус Proof-of-Stake приходит на смену Proof-of-Work, что позволяет выделять отдельные группы валидаторов и эффективно рассылать сообщения. В TON реализован точный контроль стоимости операций, предусмотрена плата за исполнение, хранение данных и маршрутизацию сообщений — это также обеспечивает масштабируемость и снимает риски отказа в обслуживании.</Title >

      <ul>
        <li>Основная идея TON — обеспечить разработчикам неограниченную гибкость и масштабируемость.</li>
        <li>Контракты TON не имеют доступа в глобальный стейт, они могут видеть только свой собственный стейт.</li>
        <li>Единственный способ контрактов взаимодействовать друг с другом — отправлять сообщения в асинхронном режиме.</li>
        <li>Каждый раз, когда вы исполняете транзакцию в рамках контракта, она является полностью независимой от любой транзакции в другом контракте, и соответственно, они могут быть выполнены в любом порядке или независимо друг от друга.</li>
        <li>В TON все контракты могут быть шардированы, они могут взаимодействовать друг с другом через сообщения, за маршрутизацию которых отвечает сеть.</li>
      </ul>

      <Title weight="7" style={{ marginTop: 16 }}>Заключение</Title>
      <Title style={{ fontSize: 16, marginTop: 4 }}>Блокчейн прошел путь от простой, но важной модели Bitcoin до функциональной модели Ethereum, которая столкнулась с проблемами масштабирования. Третье поколение блокчейн-сетей, TON, решает эти проблемы с помощью некоторых новых ограничений, новых возможностей масштабирования и точной оценки затрат в сети. Такой подход обеспечивает неограниченные возможности масштабирования, сохраняя безопасность и эффективность операций в сети.</Title >
    </div>
  );
};


export const Lesson1_4 = () => {
  return (
    <div>
      <Title weight="7" style={{ marginTop: 16 }}>Урок 1.4: Концепции аккаунта и контракта</Title>

      <Title style={{ fontSize: 16, marginTop: 4 }}><strong>Что вы узнаете:</strong></Title >
      <ul>
        <li>О важных особенностях контрактов в TON;</li>
        <li>Как в TON обеспечивается неограниченная масштабируемость;</li>
        <li>Что можно сделать с помощью контрактов;</li>
        <li>Как TON защищен от отказов в обслуживании;</li>
        <li>Что важно учитывать при создании смарт-контрактов.</li>
      </ul>

      <Title style={{ fontSize: 16, marginTop: 4 }}><strong>Что вы теперь знаете:</strong></Title >
      <ul>
        <li>Адрес контракта представляет собой криптографический хеш от исходных данных и кода контракта.</li>
        <li>Данные контракта инкапсулированы, и контракт видит только собственное хранилище и баланс.</li>
        <li>Токены в TON — это отдельные контракты с собственным состоянием.</li>
        <li>TON взимает плату за данные контракта и его аренду.</li>
      </ul>

      <Title style={{ fontSize: 16, marginTop: 4 }}><strong>Что важно учитывать при разработке смарт-контрактов:</strong></Title >
      <ul>
        <li>Стоимость газа для исполнения контракта.</li>
        <li>Аренда, выплачиваемая на протяжении жизненного цикла контракта.</li>
      </ul>

      <Title weight="7" style={{ marginTop: 16 }}>Что можно создавать с контрактами:</Title>
      <ul>
        <li>Контракты аккаунтов пользователей.</li>
        <li>Мультиподписные контракты.</li>
        <li>Токены и их параметры (владелец и другие). </li>
      </ul>

      <Title weight="7" style={{ marginTop: 16 }}>Как защитить TON:</Title>
      <ul>
        <li>Комиссии за выполнение и аренду данных предотвращают атаки типа «отказ в обслуживании».</li>
        <li>Плата за газ зависит от времени выполнения инструкций.</li>
      </ul>

      <Title weight="7" style={{ marginTop: 16 }}>Замороженные контракты:</Title>
      <Title style={{ fontSize: 16, marginTop: 4 }}>Если контракт не может оплатить аренду, он замораживается, и его данные заменяются на хеш последнего состояния.</Title >
    </div>
  );
}


export const Lesson1_5 = () => {
  return (
    <div>
      <Title weight="7" style={{ marginTop: 16 }}>Урок 1.5: Токены, валюты, DNS и т. д.</Title>

      <Title style={{ fontSize: 16, marginTop: 4 }}><strong>Что вы узнаете:</strong></Title >
      <ul>
        <li>Как создавать собственные ассеты на платформе TON;</li>
        <li>Виды токенов в TON: взаимозаменяемые и невзаимозаменяемые;</li>
        <li>Как работает токенизация;</li>
        <li>Как масштабируются токены в TON;</li>
        <li>Как важные токены выполняют функции в сети TON, такие как записи в DNS и имена пользователей.</li>
      </ul>

      <Title weight="7" style={{ marginTop: 16 }}>Что такое ассеты:</Title>
      <Title style={{ fontSize: 16, marginTop: 4 }}>Ассеты в TON — это любые ценности, которые вы хотите добавить в ваше приложение, например:</Title >
      <ul>
        <li>Валюты</li>
        <li>Стейблкойны</li>
        <li>Чеки</li>
        <li>Тикеты и разрешения</li>
        <li>Финансовые контракты</li>
      </ul>

      <Title weight="7" style={{ marginTop: 16 }}>Токенизация</Title>
      <Title style={{ fontSize: 16, marginTop: 4 }}>Токенизация — это процесс распределения общей ценности системы в виде набора передаваемых токенов.</Title >

      <Title weight="7" style={{ marginTop: 16 }}>Виды токенов в TON</Title>
      <Title style={{ fontSize: 16, marginTop: 4 }}>Токены могут быть:</Title >
      <ul>
        <li><strong>Взаимозаменяемыми</strong>: валюты, криптовалюты, доли владения, права голоса.</li>
        <li><strong>Невзаимозаменяемыми (NFT)</strong>: уникальные, не могут быть заменены или разделены, например, записи в DNS и имена пользователей в Telegram.</li>
      </ul>

      <Title weight="7" style={{ marginTop: 16 }}>Масштабируемость токенов</Title>
      <Title style={{ fontSize: 16, marginTop: 4 }}>Масштабируемость токенов означает, что операции перевода между пользователями не влияют на переводы между другими пользователями в сети. Это делает возможным создание неограниченно масштабируемых токенов без ограничений.</Title >

      <video controls>
        <source src="./videos/1.5.mp4" type="video/mp4" />

      </video>

      <Title weight="7" style={{ marginTop: 16 }}>Заключение</Title>
      <Title style={{ fontSize: 16, marginTop: 4 }}>Таким образом, взаимозаменяемые и невзаимозаменяемые токены позволяют вам реализовывать любые виды ассетов, которые могут быть отправлены пользователям напрямую. Это важный строительный материал для разработчиков приложений в сети TON, обеспечивающий масштабируемость и работоспособность системы.</Title >
    </div>
  );
}

import React from 'react';

export const Lesson2_1 = () => {
  return (
    <div>
      <Title weight="7" style={{ marginTop: 16 }}>2.1 TVM и работа памяти</Title>

      <Title style={{ fontSize: 16, marginTop: 4 }}><strong>💎 Добро пожаловать во вторую часть!</strong></Title >

      <Title style={{ fontSize: 16, marginTop: 4 }}>
        В этой части мы затронем несколько тем. Мы кратко рассмотрим схему работы памяти и реализацию виртуальной машины TON (TVM).
        Углубимся в обработку сообщений и изучим, как на самом деле работают транзакции. Также мы обсудим важность аутентификации сообщений в рамках смарт-контрактов.
        Мы оценим актуальные комиссии в TON и посмотрим, как принципы дизайна смарт-контрактов могут быть использованы в разных приложениях. Наконец, мы обсудим, как платформа TON обеспечивает безопасность и масштабируемость создаваемых на ней приложений.
      </Title >

      <Title style={{ fontSize: 16, marginTop: 4 }}><strong>📝 Вы узнаете:</strong></Title >
      <ul>
        <li>что такое ячейка в TON;</li>
        <li>зачем вообще для TON выбрана подобная архитектура;</li>
        <li>как сохранять списки, словари и наборы данных в системе.</li>
      </ul>



      <Title style={{ fontSize: 16, marginTop: 4 }}><strong>📝 Что вы теперь знаете:</strong></Title >

      <Title style={{ fontSize: 16, marginTop: 4 }}>
        Интересная особенность TON заключается в том, что все структуры данных во всей сети, ваших смарт-контрактах и стандартных структурах данных построены на базе ячеек.
      </Title >

      <Title style={{ fontSize: 16, marginTop: 4 }}>
        Ячейка — это строительный блок для всех структур данных в блокчейне TON. Каждая ячейка содержит до 1023 бит данных и до 4 ссылок на другие ячейки. Это позволяет использовать ячейки для создания сложных и многоуровневых структур данных.
      </Title >

      <Title style={{ fontSize: 16, marginTop: 4 }}>
        Когда контракт получает сообщение, валидирующая нода создает экземпляр TVM — специальной стековой виртуальной машины, созданной для выполнения байт-кода TON. В виртуальную машину загружается текущий стейт и код контракта, они сохраняются в ячейках. Затем она по порядку исполняет код, проверяет выполнение всех правил сети (включая вычет стоимости газа), а затем либо заменяет стейт контракта на новый, либо возвращает ошибку.
      </Title >

      <Title style={{ fontSize: 16, marginTop: 4 }}>
        Секрет масштабируемости TON — в ограничении объема работы, который выполняется на любом отдельном участке сети. TON масштабируется в ракурсе всех контрактов, но каждый отдельный контракт не может масштабироваться за пределы своего контейнера.
      </Title >

      <Title weight="7" style={{ marginTop: 16 }}>📚 Конспект урока</Title>

      <Title style={{ fontSize: 16, marginTop: 4 }}>
        На этом уроке мы изучим, как работает память и виртуальная машина TON.
      </Title >

      <Title weight="4">🏁 Начнем с уникальной сущности TON — ячейки.</Title>
      <Title weight="4" style={{ marginTop: 16 }}>Ячейки</Title >
      <Title style={{ fontSize: 16, marginTop: 4 }}><strong>Что такое ячейки?</strong></Title >

      <Title style={{ fontSize: 16, marginTop: 4 }}>
        🔶 Ячейки — маленькие строительные блоки, из которых строятся все структуры данных в блокчейне TON. Каждая ячейка включает до 1023 бит данных 🔸 и до 4 ссылок 🔗 на другие ячейки. Это позволяет использовать ячейки, чтобы создавать сложные и многоуровневые структуры данных.
      </Title >

      <Title style={{ fontSize: 16, marginTop: 4 }}>
        ❗ Таким образом, в TON невозможно выделить массив памяти произвольного размера, как в Ethereum. В TON вам приходится работать с деревом ячеек 🌳
      </Title >

      <Title weight="4" style={{ marginTop: 16 }}>Модель данных в TON</Title >
      <Title style={{ fontSize: 16, marginTop: 4 }}>
        Модель данных в TON, конечно, создает определенные сложности для разработчиков, поскольку здесь нужно дробить данные на фрагменты в 1023 бита, что немногим меньше 128 байт.
        ❓ Приходится планировать, насколько плоской или глубокой должна быть ваша структура данных.
      </Title >

      <Title weight="4" style={{ marginTop: 16 }}>Почему именно ячейки?</Title >
      <Title style={{ fontSize: 16, marginTop: 4 }}>
        ❗ Преимущество такой структуры в том, что весь стейт блокчейна можно объединить в двоичное дерево (дерево Меркла), что позволяет использовать доказательство Меркла — криптографическое доказательство достоверности любой части данных в блокчейне при любом состоянии.
      </Title >

      <Title style={{ fontSize: 16, marginTop: 4 }}>
        Это критически важно, поскольку, когда вы масштабируете большую систему 🌐 с многочисленными шардами и группами валидаторов, вам необходимо подтверждать корректность поведения отдельных групп, доказывать, что они не нарушили регламенты системы. Как раз здесь и нужны компактные и эффективные доказательства Меркла, с помощью которых можно определить недопустимое поведение любого участника системы.
      </Title >

      <Title weight="4" style={{ marginTop: 16 }}>Другие типы данных в TVM</Title >
      <Title style={{ fontSize: 16, marginTop: 4 }}>
        Виртуальная машина TON имеет достаточное количество поддерживаемых типов данных:
      </Title >
      <ul>
        <li>ячейки,</li>
        <li>целые числа,</li>
        <li>257-битные целые числа (позволяют воспроизвести широкий диапазон целых чисел для криптографических и финансовых операций 😎).</li>
      </ul>

      <Title style={{ fontSize: 16, marginTop: 4 }}>
        Все типы данных, с которыми работает TVM, можно прочитать через код контракта из его собственного хранилища, обработать как массив и создать новое защищенное хранилище. Если исполнение было успешным ✔️, экземпляр TVM выгружается из памяти и новый стейт контракта сохраняется на месте старого.
      </Title >

      <Title weight="4" style={{ marginTop: 16 }}>Схема работы памяти</Title >
      <Title style={{ fontSize: 16, marginTop: 4 }}>
        Давайте рассмотрим, как контракт взаимодействует с памятью.
      </Title >

      <Title style={{ fontSize: 16, marginTop: 4 }}>
        Единственный доступный здесь вариант — это дерево ячеек памяти в контракте.
      </Title >

      <Title weight="4" style={{ marginTop: 16 }}>Что важно учитывать?</Title >
      <Title style={{ fontSize: 16, marginTop: 4 }}>
        Создавать многоуровневые ячейки для небольших объемов данных — это нормально. Если вы создаете систему с совсем небольшим числом участников — например, контракт с мультиподписью — вы можете хранить эти данные прямо в хеш-таблице контракта, поскольку размер данных будет довольно мал.
        Однако, если вы создаете систему с миллионами пользователей, вам следует подумать о токенах, подтверждающих право участия пользователей в системе, и о том, как избежать хранение списка всех пользователей в рамках контракта.
      </Title >

      <Title weight="4" style={{ marginTop: 16 }}>Заключение</Title >
      <Title style={{ fontSize: 16, marginTop: 4 }}>
        Вот к чему мы пришли к концу урока.
      </Title >

      <ul>
        <li>Всё построено из 🔸 ячеек.</li>
        <li>Поверх ячеек у нас есть типы, поддерживаемые 💻 виртуальной машиной TON (TVM).</li>
        <li>Новый экземпляр TVM создается для каждого контракта, каждого сообщения, которое обрабатывает 📝 контракт.</li>
        <li>Стоит помнить о 💠 затратах, связанных с большими структурами данных и многоуровневыми цепочками ячеек, которые могут привести к повышенной стоимости исполнения контрактов.</li>
      </ul>
    </div>
  );
};

export const Lesson2_2 = () => {
  return (
    <div>
      <Title weight="7" style={{ marginTop: 16 }}>2.2 Типы сообщений и этапы вычислений</Title>

      <Title weight="7" style={{ marginTop: 16 }}>📝 Вы узнаете:</Title>
      <ul>
        <li>какие типы сообщений может получать контракт;</li>
        <li>что такое транзакция;</li>
        <li>что такое этапы транзакции.</li>
      </ul>



      <Title weight="7" style={{ marginTop: 16 }}>📝 Что вы теперь знаете</Title>
      <Title style={{ fontSize: 16, marginTop: 4 }}>
        Контракт может получать сообщения двух видов — внешние и внутренние.
      </Title >

      <Title weight="4">Внешние сообщения</Title>
      <Title style={{ fontSize: 16, marginTop: 4 }}>
        Внешнее сообщение — это строка данных, которая с точки зрения блокчейна приходит из ниоткуда.
        Данные такого сообщения сами по себе не имеют 🆔 отправителя.
        К ним не привязаны средства в виде 💎 монет TON.
        Эти сообщения могут содержать все, что пожелает автор контракта.
      </Title >

      <Title style={{ fontSize: 16, marginTop: 4 }}>❗ Осмыслить эти данные и применить их к своему коду — задача контракта-получателя.</Title >

      <Title weight="4">Внутренние сообщения</Title>
      <Title style={{ fontSize: 16, marginTop: 4 }}>
        Внутренние сообщения — это сообщения, которые контракты отправляют друг другу 👥
        Эти сообщения имеют более сложную структуру:
      </Title >
      <ul>
        <li>Внутренние сообщения могут содержать балансы средств 💸</li>
        <li>Эти сообщения подтверждены адресом контракта-отправителя. 🔑 Сама архитектура TON гарантирует контракту-получателю, что адрес отправителя верен.</li>
      </ul>

      <Title weight="4">Обработка сообщений и опкоды (коды операций)</Title>
      <Title style={{ fontSize: 16, marginTop: 4 }}>
        Когда контракт получает сообщение, он может применить к нему два отдельных обработчика — для внутренних и внешних сообщений.
        Оба вида сообщений будут содержать 🗻 произвольные данные полезной нагрузки, что определяется контрактом-получателем.
        Для обработки разных типов сообщений контракт использует опкоды.
      </Title >
      <Title style={{ fontSize: 16, marginTop: 4 }}>
        Опкоды (операционные коды) 🔢 — префиксы размером в четыре байта в пользовательских данных сообщения. Они указывают тип операции, которую должен поддерживать контракт.
      </Title >

      <Title weight="4">Транзакции</Title>
      <Title style={{ fontSize: 16, marginTop: 4 }}>❓ Что такое транзакция?</Title >
      <Title style={{ fontSize: 16, marginTop: 4 }}>Транзакция 💸 — набор изменений в стейте контракта ❗ Сообщение — это не транзакция, это просто входящие данные для транзакции!</Title >

      <Title weight="4" style={{ marginTop: 16 }}>Что делают транзакции?</Title >
      <ul>
        <li>▶️ Они изменяют стейт контракта.</li>
        <li>📋 Они создают исходящий список действий.</li>
      </ul>

      <Title weight="4">Этапы транзакции</Title>
      <Title style={{ fontSize: 16, marginTop: 4 }}>Транзакция проходит через 5️⃣ пять этапов.</Title >
      <ul>
        <li>Этап хранения 📒 — Здесь определяется плата за хранение, исходя из числа байтов, сохраненных контрактом с момента последней транзакции. Если средств недостаточно, контракт переходит в замороженное состояние, где его стейт хранится в течение ограниченного времени.</li>
        <li>Этап обеспечения 🏦 — Здесь монеты, прикрепленные к входящему сообщению, зачисляются на контракт.</li>
        <li>Этап вычислений 💻 — Здесь ваша программа оживает. TVM выполняет код, подтверждает каждую операцию и следит за расходом газа.</li>
        <li>Этап действия 🏃 — Самое важное в этой фазе — обновление состояния контракта. В конце исполнения или на любом промежуточном шаге ваш контракт может создать свое новое состояние и хранилище, и они будут записаны после успешного выполнения контракта. В список могут входить другие действия, которые становятся исходящими сообщениями.</li>
        <li>Этап отказа 🎱 — Он происходит, если обновление контракта завершилось неудачей, а входящее сообщение включало флаг, разрешающий отскок сообщения. Если произойдет такой сбой и от входящего сообщения останутся средства, контракт вернет их обратно через исходящее сообщение. Эта функция обеспечивает безопасность пользователей, позволяя им вернуть бо́льшую часть средств в случае ошибки или сбоя внутри контракта.</li>
      </ul>

      <Title weight="4">Заключение</Title>
      <Title style={{ fontSize: 16, marginTop: 4 }}>Подведем итоги.</Title >
      <ul>
        <li>Контракт получает входящие сообщения, которые могут быть внутренними или внешними.</li>
        <li>Внутренние сообщения могут иметь баланс средств и быть аутентифицированы по адресу контракта-отправителя. Внешние сообщения не аутентифицируются сами по себе, и это становится задачей контракта-получателя.</li>
      </ul>

      <Title style={{ fontSize: 16, marginTop: 4 }}>Исполнение транзакций включает пять этапов:</Title >
      <ul>
        <li>Этап хранения, когда взимается арендная плата.</li>
        <li>Этап обеспечения, когда поступившие монеты зачисляются на баланс контракта.</li>
        <li>Этап вычислений, на котором выполняется ваш код.</li>
        <li>Этап действий, когда обновляется хранилище и происходит адресация исходящих сообщений.</li>
        <li>Этап отказа, когда контракт фиксирует сбой и возвращает монеты отправителю.</li>
      </ul>
    </div>
  );
};

export const Lesson2_3 = () => {
  return (
    <div>
      <Title weight="7" style={{ marginTop: 16 }}>2.3 Авторизация сообщений в контрактах</Title>

      <section>
        <Title weight="7" style={{ marginTop: 16 }}>📝 Вы узнаете:</Title>
        <ul>
          <li>Какие типы аутентификации существуют в блокчейне TON?</li>
        </ul>
      </section>

      <section>
        <Title weight="7" style={{ marginTop: 16 }}>📝 Что вы теперь знаете</Title>
        <Title style={{ fontSize: 16, marginTop: 4 }}>
          Первый тип аутентификации — аутентификация на основе подписи. Например, при получении внешнего сообщения кошелек считывает первую часть данных, проверяет корректность подписи, а затем работает с остальной частью сообщения.
        </Title >
        <Title style={{ fontSize: 16, marginTop: 4 }}>
          Второй механизм — аутентификация отправителя сообщения. Все внутренние сообщения в TON идентифицируются отправителем сообщения. Корректность и безопасность этой идентификации гарантируется протоколом TON. Таким образом, каждый раз, когда контракт получает внутреннее сообщение, он точно знает, от какого другого контракта оно было получено.
        </Title >
        <Title style={{ fontSize: 16, marginTop: 4 }}>
          Третий способ — это аутентификация поверх отправителя сообщений. Адреса в экосистеме TON — это не просто уникальные идентификаторы контрактов, но также криптографически защищенные хеши исходного кода и данных контракта. Поэтому адреса не меняются при изменении данных контракта. Поскольку эти адреса являются криптографическими обязательствами по отношению к самому́ коду, с их помощью вы можете проверить код отправителя сообщения.
        </Title >
        <Title style={{ fontSize: 16, marginTop: 4 }}>
          Четвертый тип тоже не стоит забывать — отсутствие аутентификации вообще. Этот важный паттерн становится актуальным, если вы создаете по-настоящему децентрализованные приложения.
        </Title >
      </section>

      <section>
        <Title weight="7" style={{ marginTop: 16 }}>📚 Конспект урока</Title>
        <Title weight="4">Обзор всех типов аутентификации</Title>

        <Title weight="4" style={{ marginTop: 16 }}>Аутентификация на основе подписи</Title >
        <Title style={{ fontSize: 16, marginTop: 4 }}>
          ❗ Любое событие в блокчейне TON должно начинаться с внешнего сообщения. Когда внешнее сообщение приходит в кошелек, он считывает 64 байта данных 6️⃣4️⃣, которые являются подписью для остальной части сообщения. Затем кошелек проверяет подпись своим открытым ключом и воспринимает остальную часть сообщения как инструкции для отправки сообщений другим контрактам в блокчейне.
        </Title >

        <Title weight="4" style={{ marginTop: 16 }}>Аутентификация отправителя сообщения</Title >
        <Title style={{ fontSize: 16, marginTop: 4 }}>
          Все внутренние сообщения в TON идентифицируются отправителем сообщения. Корректность и безопасность 🔓 на этом этапе гарантируется самим протоколом TON. Каждый раз, когда контракт получает внутреннее сообщение, он точно знает, от какого оно контракта. Это намного более дешевая и мощная опция по сравнению с проверкой подписей.
        </Title >

        <Title weight="4" style={{ marginTop: 16 }}>Проверка кода отправителя</Title >
        <Title style={{ fontSize: 16, marginTop: 4 }}>
          Адреса в экосистеме TON — это не просто уникальные идентификаторы контрактов, но также криптографически защищенные хеши исходного кода и данных контракта. Поэтому адреса не меняются при изменении данных контракта. Поскольку эти адреса являются криптографическими обязательствами по отношению к самому́ коду, с их помощью вы можете проверить код отправителя сообщения.
        </Title >

        <Title weight="4" style={{ marginTop: 16 }}>Отсутствие аутентификации для предотвращения ограничений</Title >
        <Title style={{ fontSize: 16, marginTop: 4 }}>
          ❓ Будет ли небезопасно не проверять подлинность сообщений? В некоторых ситуациях безопасность заключается именно в этом — если вы работаете с системой, которая должна быть устойчива к ограничениям, например, с децентрализованным стейкинг-пулом.
        </Title >
      </section>
    </div>
  );
};

export const Lesson2_5 = () => {
  return (
    <div>
      <Title weight="7" style={{ marginTop: 16 }}>2.5 Масштабируемые контракты</Title>
      <Title weight="4">📝 Вы узнаете</Title>
      <ul>
        <li>Что на самом деле значит неограниченная горизонтальная масштабируемость и как вы можете извлечь из нее максимум для ваших приложений?</li>
        <li>Какие принципы помогут вам выжать максимум из масштабируемости блокчейна TON?</li>
      </ul>



      <Title weight="4">📝 Что вы теперь знаете</Title>
      <ul>
        <li>TON гарантирует возможность масштабирования вплоть до уровня отдельных контрактов.</li>
        <li>Монолитные контракты кошельков Ethereum на блокчейне TON должны быть реализованы иначе — ради скорости, масштабируемости и экономии газа.</li>
        <li>Прежде всего избегайте данных переменной длины.</li>
        <li>Если вам нужно добавить список, то хотя бы сделайте его коротким и ограниченным — например, фиксированным по величине.</li>
        <li>Доменное имя TON DNS — это коллекционный невзаимозаменяемый токен, который может включать неограниченное количество дополнительных записей, связанных с ним.</li>
        <li>Масштабируемый способ создания больших списков и словарей данных в TON — это использование самого блокчейна в качестве массива.</li>
        <li>Токены в TON устроены так, что для них существует один-единственный контракт майнера, но он не хранит список всех своих пользователей. Вместо этого он имеет право майнить и создавать токены для других пользователей. При этом балансы отдельных пользователей распространяются в так называемых Jetton-кошельках.</li>
      </ul>

      <Title weight="4">📚 Конспект урока</Title>
      <Title style={{ fontSize: 16, marginTop: 4 }}>Поговорим о масштабируемости.</Title >
      <ul>
        <li>💎 В TON у вас есть четкая гарантия масштабируемости. TON гарантирует, что он может масштабироваться вплоть до отдельных контрактов. Поэтому операции по одному контракту не будут создавать «узкое место» 🍼 для операций по другому контракту. А сеть TON позаботится о маршрутизации сообщений 🚅 между ними.</li>
        <li>❗ Ваша задача — использовать все преимущества масштабируемости блокчейна и не создавать узких мест в своих контрактах!</li>
      </ul>

      <Title weight="4">Проблемы с монолитными контрактами</Title>
      <Title style={{ fontSize: 16, marginTop: 4 }}>Одна из главных проблем для людей, пришедших в TON из традиционных информационных систем, крупных веб-баз данных или из Ethereum, заключается в том, что они разрабатывают свои контракты как монолитные приложения с собственным хранилищем переменной длины.</Title >
      <ul>
        <li>🌐 Например, в Ethereum токен реализован как гроссбух с одним смарт-контрактом, содержащим список счетов. Если у вас миллион пользователей, этот список будет состоять из миллиона строк. Для каждого пользователя будет запись с его адресом и балансом. Разработка таких контрактов проста и понятна: у вас есть атомарные транзакции, вы вычитаете из одного баланса и добавляете к другому. Это очень легко спроектировать, чтобы увидеть, как будет работать система. Однако эта модель не будет масштабироваться в TON. Как только вы поместите этот длинный список в один контракт, то сразу же столкнетесь с проблемами</li>
        <li>1️⃣ Будет постоянно расти стоимость аренды для данных, которые вы храните в контракте 💸</li>
        <li>2️⃣ За каждую операцию с этим контрактом пользователю придется платить все больше и больше, поскольку он будет оперировать все большим объемом данных в контракте 🌄</li>
      </ul>

      <Title weight="4">Правила</Title>
      <Title style={{ fontSize: 16, marginTop: 4 }}>Чтобы правильно проектировать многопользовательские и крупные приложения в TON, придерживайтесь некоторых правил.</Title >
      <ul>
        <li>❗ Избегайте данных переменной длины.</li>
        <li>❗ Если вам нужно иметь список, то, по крайней мере, сделайте его коротким и ограниченным, например, фиксированным по величине.</li>
        <li>❗ Если вам нужно иметь список, который будет динамически увеличиваться, убедитесь, что этот контракт принадлежит одному пользователю, и он имеет исключительное право управлять его хранилищем. Простой пример такого списка — записи TON DNS.</li>
      </ul>

      <Title weight="4">Блокчейн как массив</Title>
      <Title style={{ fontSize: 16, marginTop: 4 }}>Чтобы создавать масштабируемые большие списки или словари данных в TON, стоит использовать сам блокчейн в качестве массива. Вот простой пример:</Title >
      <ul>
        <li>Представьте систему, в которой у вас есть несколько контрактов, связанных 📌 друг с другом, где каждый пользователь заботится о своем индивидуальном контракте. Поэтому вам не нужно хранить весь список внутри своего приложения.</li>
        <li>💎 Самый популярный пример реализации этой идеи — дизайн токенов в экосистеме TON. Балансы отдельных пользователей распределены по так называемым Jetton-кошелькам. 🔥 Это удачное решение, поскольку операции между двумя Jetton-кошельками в одном месте не мешают операциям двух других Jetton-кошельков в другом месте. Они могут быть распределены между отдельными шардчейнами и не создавать узких мест.</li>
      </ul>
    </div>
  );
};


export const Lesson2_6 = () => {
  return (
    <div>
      <Title weight="7" style={{ marginTop: 16 }}>2.6 Обзор стандартных бизнес-проблем</Title>

      <Title weight="4">📝 Вы узнаете</Title>
      <ul>
        <li>В каких сценариях мы можем применять принципы масштабируемого дизайна контрактов?</li>
      </ul>


      <Title weight="4">📝 Что вы теперь знаете</Title>
      <ul>
        <li>В TON токены реализованы в виде отдельных контрактов: по контракту для каждого пользователя плюс отдельный контракт майнера, который предоставляет интерфейс для эмиссии новых токенов.</li>
      </ul>

      <Title weight="4">📚 Конспект урока</Title>
      <Title style={{ fontSize: 16, marginTop: 4 }}>Здесь мы рассмотрим три примера, чтобы увидеть, как принципы разработки контрактов применяются к различным приложениям и как платформа TON помогает создавать масштабируемые и безопасные приложения.</Title >

      <Title weight="4" style={{ marginTop: 16 }}>1. Токены</Title >
      <Title style={{ fontSize: 16, marginTop: 4 }}>
        В Ethereum или даже в системах не на базе блокчейна токены были бы реализованы в виде простого реестра аккаунтов.
        У вас есть список счетов, которым управляет программа. Каждый элемент в списке включает адрес участника и его баланс. Это довольно просто, но, к сожалению, не масштабируется в рамках блокчейна, поскольку ваш смарт-контракт растет с увеличением числа пользователей и становится все более дорогим для каждого пользователя.
      </Title >
      <Title style={{ fontSize: 16, marginTop: 4 }}><strong>Какой подход у TON?</strong></Title >
      <Title style={{ fontSize: 16, marginTop: 4 }}>
        В TON токены реализуются как отдельные контракты: по контракту для каждого пользователя плюс отдельный контракт майнера, который предоставляет интерфейс для эмиссии новых токенов.
      </Title >

      <Title style={{ marginTop: 12 }}>Контракты пользователей называются Jetton-кошельками</Title>
      <Title style={{ fontSize: 16, marginTop: 4 }}>Их задача — хранить баланс токена для своего пользователя. Когда пользователь хочет перевести токены со своего счета на счет получателя, он отправляет внешнее сообщение на свой кошелек. Его кошелек открывает сообщение и уже на Jetton-кошелек того же пользователя посылает внутреннее сообщение с инструкцией отправить деньги на определенный адрес. Этот Jetton-кошелек уменьшает свой баланс на необходимую сумму и отправляет сообщение Jetton-кошельку получателя, который имеет тот же код, но при этом другого владельца. В сообщении содержится инструкция увеличить баланс на ту же сумму, что и делает Jetton-кошелек получателя.</Title >

      <Title style={{ fontSize: 16, marginTop: 4 }}>Здесь используется проверка кода, поскольку код у всех Jetton-кошельков одинаковый.</Title >

      <Title weight="4" style={{ marginTop: 16 }}>2. Контракт с мультиподписью</Title >
      <Title style={{ fontSize: 16, marginTop: 4 }}>
        Концепция контракта с несколькими подписями используется, когда несколько сторон авторизуют действия путем токенизации запросов, инициированных пользователями.
      </Title >
      <Title style={{ fontSize: 16, marginTop: 4 }}>
        Вместо прямой обработки сообщений пользователи получают уникальные токены, в которых заключены их запросы и голоса. Эти токены принадлежат пользователям, они упрощают отслеживание и не позволяют злоумышленникам перегрузить контракт. Честные пользователи собирают голоса с помощью временных токенов запроса. Как только достигается кворум, контракт с мультиподписью проверяет подлинность запроса и выполняет действие. Такая токенизация упрощает весь процесс, основываясь на временном состоянии системы, а не на индивидуальных долях в контракте или других значениях.
      </Title >

      <Title weight="4" style={{ marginTop: 16 }}>3. Подписочная модель оплаты</Title >
      <Title style={{ fontSize: 16, marginTop: 4 }}>
        Начиная с четвертой версии, кошельки поддерживают плагины, позволяющие создавать платежи по подписке. Это очень мощная функция, которая относительно хорошо масштабируется, потому что пользователь контролирует список плагинов.
      </Title >
      <Title style={{ fontSize: 16, marginTop: 4 }}>
        Однако можно представить себе лучший способ сделать это. Отличная идея — вместо адресов конкретных плагинов перечислять отдельные реализации кода этих плагинов. И если они используют один и тот же код, то для любого количества плагинов подписки будет достаточно одной записи.
      </Title >
    </div>
  );
};

export const Lesson3_1 = () => {
  return (
    <div>
      <Title weight="7" style={{ marginTop: 16 }}>3.1 Жизненный цикл разработки смарт-контрактов</Title>

      <Title style={{ fontSize: 16, marginTop: 4 }}>💎 Добро пожаловать в третью часть!</Title >

      <Title style={{ fontSize: 16, marginTop: 4 }}>
        В этой главе мы рассмотрим различные аспекты разработки смарт-контрактов. Начнем с разбора всего цикла разработки смарт-контракта, в том числе создания локального проекта, который можно компилировать в смарт-контракт. Кроме того, мы научимся писать код на FunC и проверять, что код нашего контракта работает так, как ожидается. Мы рассмотрим создание пользовательского скрипта развертывания, чтобы лучше понять логику внутренних процессов. Наконец, мы остановимся на повышении гибкости развертывания. В ходе следующих шести уроков мы получим обширные знания и навыки в области разработки смарт-контрактов.
      </Title >

      <Title weight="4">📝 Вы узнаете:</Title>
      <ul>
        <li>Какие этапы разработки смарт-контрактов предусмотрены в TON?</li>
        <li>Какой язык используется для разработки смарт-контрактов в блокчейне TON?</li>
        <li>Существуют ли стандартные локальные среды для написания, тестирования и развертывания смарт-контрактов?</li>
      </ul>



      <Title weight="4">📝 Что вы теперь знаете:</Title>
      <Title style={{ fontSize: 16, marginTop: 4 }}>
        Мы можем представить себе контракт TON как спутник, который запускают на орбиту Земли. Он летает вокруг Земли, взаимодействует с другими спутниками, может принимать информацию с Земли, обрабатывать ее и отправлять результаты. Но прежде чем запустить этот спутник, нужно пройти с ним через несколько этапов.
      </Title >

      <ul>
        <li>На первом этапе мы готовим локальную среду, которая позволит нам провести наш спутник через все последующие этапы.</li>
        <li>Фактически смарт-контракт на блокчейне TON хранится и исполняется в виде двоичного кода.</li>
        <li>На втором этапе мы описываем команды, которые может обрабатывать наш смарт-контракт.</li>
        <li>На третьем этапе мы пишем код на FunC.</li>
        <li>На четвертом этапе мы тестируем наш FunC-код локально.</li>
        <li>На пятом этапе мы развертываем наш контракт в тестнете.</li>
      </ul>

      <Title weight="4">📚 Конспект урока</Title>
      <Title style={{ fontSize: 16, marginTop: 4 }}>Вступление к третьей главе</Title >

      <Title style={{ fontSize: 16, marginTop: 4 }}>
        В этой главе мы перейдем к практике и разберем весь цикл разработки смарт-контракта в TON, пройдем вместе каждый шаг и в итоге получим готовую локальную среду для создания смарт-контракта, FunC-код, тесты для нашего контракта и, наконец, развернутый контракт.
      </Title >

      <Title weight="4" style={{ marginTop: 16 }}>Смарт-контракт в TON как спутник Земли</Title >
      <Title style={{ fontSize: 16, marginTop: 4 }}>
        Это лучшее сравнение, которое я смог придумать, чтобы объяснить жизненный цикл смарт-контракта TON: вы можете представить его как спутник, запущенный на орбиту Земли.
      </Title >

      <ul>
        <li>Есть лаборатория, в которой собирают спутник (локальная среда).</li>
        <li>Есть задокументированный протокол, определяющий все команды, которые может обрабатывать наш спутник (схема TL-B).</li>
        <li>Каждый сценарий поведения тестируется, пока спутник находится в лаборатории (локальные тесты).</li>
        <li>Затем спутник запускается в симулирующую среду, очень похожую на ту, что ожидает его в космосе (тестнет и ончейн-тесты после деплоя).</li>
        <li>Как мы отправим наш спутник на орбиту? С помощью ракет. Ракеты доставляют спутник на орбиту, оставляют его там и падают после успешного завершения своей миссии (деплой в мейннете).</li>
      </ul>

      <Title style={{ fontSize: 16, marginTop: 4 }}>С этого момента начинается самостоятельная жизнь спутника в космосе, где он работает со всем, что вы в нем запрограммировали.</Title >

      <Title weight="4" style={{ marginTop: 16 }}>Объясняю, зачем в теме смарт-контрактов на TON пример со спутником.</Title >
      <Title style={{ fontSize: 16, marginTop: 4 }}>
        Когда мы пишем смарт-контракт, то проходим схожий цикл.
      </Title >

      <ol>
        <li>Готовим локальную среду, которая позволит нам провести наш спутник/смарт-контракт через все этапы, упомянутые выше.</li>
        <li>Используем схему TL-B для описания команд, которые должен обрабатывать наш смарт-контракт.</li>
        <li>Самая приятная часть. Пишем код на FunC.</li>
        <li>Тестируем код FunC локально.</li>
        <li>Развертываем наш код в тестнете.</li>
        <li>Запуск. Деплой кода в мейннете.</li>
      </ol>

      <Title weight="4" style={{ marginTop: 16 }}>Существуют ли стандартные локальные среды для написания, тестирования и деплоя смарт-контрактов?</Title >
      <Title style={{ fontSize: 16, marginTop: 4 }}>
        Это хороший вопрос. Инструменты программирования в TON быстро развиваются, и иногда нет смысла создавать собственную локальную среду. Есть отличный инструмент, который разрабатывается командой TonTech и официально поддерживается TON Foundation — Blueprint.
      </Title >

      <Title style={{ fontSize: 16, marginTop: 4 }}>
        Чтобы использовать его, просто введите локально команду:
      </Title >

      <pre>npm create ton@latest</pre>

      <Title style={{ fontSize: 16, marginTop: 4 }}>Так вы получите новый проект с кодом для всех этапов, описанных выше. Подробнее об этом вы можете прочитать в документации Blueprint. Но в рамках этого курса мы все же создадим собственный проект, чтобы вы лучше понимали, как работают все процессы.</Title >

      <Title style={{ fontSize: 16, marginTop: 4 }}>Начнем!</Title >
    </div>
  );
};

export const Lesson3_4 = () => {
  return (
    <div></div>
  )
}

export const Lesson3_2 = () => {
  return (
    <div>
      <Title weight="7" style={{ marginTop: 16 }}>3.2 Настройка процесса компиляции</Title>


      <Title weight="4">📚 Конспект урока</Title>
      <Title style={{ fontSize: 16, marginTop: 4 }}>
        Цель этого урока — создать локальный проект, который будет компилироваться в наш смарт-контракт. Пока мы не будем писать на FunC, а просто подготовим нашу «лабораторию». Эта часть вы можете использовать во всех ваших будущих проектах.
      </Title >

      <Title style={{ fontSize: 16, marginTop: 4 }}>
        Для начала убедитесь, что у вас есть три вещи:
      </Title >
      <ul>
        <li>Node.js (версия 16.15.0 или новее). Инструкции по установке есть <a href="https://nodejs.org" target="_blank" rel="noopener noreferrer">здесь</a>. Для проверки установки введите <code>node -v</code> в терминале.</li>
        <li>Менеджер пакетов. Вероятно, он будет установлен вместе с Node.js, но на всякий случай проверьте. В этом уроке мы будем использовать Yarn, но вы можете использовать любой другой (например, npm).</li>
        <li>Хорошая IDE с поддержкой FunC и TypeScript. Мы рекомендуем Visual Studio Code с плагином FunC. Если вы используете IntelliJ, вот ссылка на <a href="https://plugins.jetbrains.com/plugin/xyz" target="_blank" rel="noopener noreferrer">плагин FunC</a> для нее.</li>
      </ul>

      <video controls>
        <source src="/videos/3.2.mp4" type="video/mp4" />

      </video>

      <Title weight="4">Настройка проекта</Title>
      <ol>
        <li>Создадим папку вашего проекта:
          <pre><code>mkdir my_first_contract && cd my_first_contract</code></pre>
        </li>
        <li>Создадим файл <code>package.json</code> с помощью менеджера пакетов:
          <pre><code>yarn init</code></pre>
          <Title style={{ fontSize: 16, marginTop: 4 }}>В результате файл <code>package.json</code> будет выглядеть так:</Title >
          <pre><code>{`{
  "name": "my_first_contract",
  "version": "1.0.0",
  "license": "MIT"
}`}</code></pre>
        </li>
        <li>Установим библиотеки для TypeScript:
          <pre><code>yarn add typescript ts-node @types/node @swc/core --dev</code></pre>
        </li>
        <li>Создадим файл <code>tsconfig.json</code> и добавим конфигурацию:
          <pre><code>{`{
  "compilerOptions": {
    "target": "es2020",
    "module": "commonjs",
    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true,
    "strict": true,
    "skipLibCheck": true,
    "resolveJsonModule": true
  },
  "ts-node": {
    "transpileOnly": true,
    "transpiler": "ts-node/transpilers/swc"
  }
}`}</code></pre>
        </li>
        <li>Установим библиотеки для TON:
          <pre><code>yarn add @ton/core @ton/crypto @ton-community/func-js --dev</code></pre>
        </li>
      </ol>

      <Title weight="4">Пример кода на FunC</Title>
      <Title style={{ fontSize: 16, marginTop: 4 }}>Создадим минимальный проект с кодом на FunC:</Title >
      <pre><code>{`mkdir contracts && cd contracts && touch main.fc`}</code></pre>
      <Title style={{ fontSize: 16, marginTop: 4 }}>В файле <code>main.fc</code> добавим:</Title >
      <pre><code>{`() recv_internal(int msg_value, cell in_msg, slice in_msg_body) impure {}`}</code></pre>

      <Title weight="4">Пишем скрипт компиляции</Title>
      <ol>
        <li>Создадим папку <code>scripts</code> и файл <code>compile.ts</code>:
          <pre><code>{`mkdir scripts && cd scripts && touch compile.ts`}</code></pre>
        </li>
        <li>Добавим скрипт для компиляции:
          <pre><code>{`import * as fs from "fs";
import process from "process";
import { Cell } from "@ton/core";
import { compileFunc } from "@ton-community/func-js";

async function compileScript() {
  export const compileResult = await compileFunc({
    targets: ["./contracts/main.fc"],
    sources: (x) => fs.readFileSync(x).toString("utf8"),
  });

  if (compileResult.status === "error") {
    process.exit(1);
  }

  fs.writeFileSync("build/main.compiled.json", JSON.stringify({
    hex: Cell.fromBoc(Buffer.from(compileResult.codeBoc, "base64"))[0]
      .toBoc()
      .toString("hex"),
  }));
}
compileScript();`}</code></pre>
        </li>
        <li>Для удобного запуска добавим скрипт в <code>package.json</code>:
          <pre><code>{`"scripts": {
  "compile": "ts-node ./scripts/compile.ts"
}`}</code></pre>
        </li>
      </ol>

      <Title weight="4">Запуск компиляции</Title>
      <Title style={{ fontSize: 16, marginTop: 4 }}>Теперь, запустив <code>yarn compile</code>, вы получите результаты компиляции, сохраненные в <code>build/main.compiled.json</code>.</Title >
    </div>
  );
};


export const Lesson3_3 = () => {
  return (
    <div>
      <Title weight="7" style={{ marginTop: 16 }}>3.3 Написание простого контракта на FunC</Title>

      <Title weight="4">📚 Конспект урока</Title>
      <Title style={{ fontSize: 16, marginTop: 4 }}>
        На прошлом уроке мы создали скрипт компиляции, и теперь пора
        приступать к кодингу на FunC. Как только мы напишем код, запустим{" "}
        <code>yarn compile</code> и проверим, что наш код заработает на TVM.
      </Title >

      <Title weight="4">📝 Параметры, получаемые через обработчик внутренних сообщений <code>recv_internal</code></Title>
      <Title style={{ fontSize: 16, marginTop: 4 }}>
        Откроем файл <code>contracts/main.fc</code> и посмотрим на нашу функцию:
      </Title >
      <pre>
        {`
() recv_internal(int msg_value, cell in_msg, slice in_msg_body) impure {

}
`}
      </pre>
      <Title style={{ fontSize: 16, marginTop: 4 }}>Функция <code>recv_internal</code> получает три параметра:</Title >
      <ul>
        <li><strong>msg_value</strong> — количество TON, прилагаемых к сообщению.</li>
        <li><strong>in_msg</strong> — сообщение с информацией об отправителе и данными.</li>
        <li><strong>in_msg_body</strong> — читаемая часть сообщения.</li>
      </ul>

      <Title weight="4">🔹 Спецификаторы функции</Title>
      <Title style={{ fontSize: 16, marginTop: 4 }}>
        <code>impure</code> означает, что функция может иметь сторонние эффекты.
      </Title >

      <video controls>
        <source src="/videos/3.3.mp4" type="video/mp4" />

      </video>

      <Title weight="4">🔹 Импортируем <code>stdlib.fc</code></Title>
      <Title style={{ fontSize: 16, marginTop: 4 }}>Добавьте в начале файла <code>#include "imports/stdlib.fc";</code></Title >

      <Title weight="4">🔹 Парсим <code>in_msg</code></Title>
      <Title style={{ fontSize: 16, marginTop: 4 }}>Давайте начнем с разбора сообщения, чтобы получить флаг и адрес отправителя:</Title >
      <pre>
        {`
() recv_internal(int msg_value, cell in_msg, slice in_msg_body) impure {
  slice cs = in_msg.begin_parse();
  int flags = cs~load_uint(4);
  slice sender_address = cs~load_msg_addr();
}
`}
      </pre>

      <Title weight="4">🔹 Используем постоянное хранилище</Title>
      <Title style={{ fontSize: 16, marginTop: 4 }}>Сохраним адрес отправителя в постоянное хранилище <code>c4</code>:</Title >
      <pre>
        {`
() recv_internal(int msg_value, cell in_msg, slice in_msg_body) impure {
  slice cs = in_msg.begin_parse();
  int flags = cs~load_uint(4);
  slice sender_address = cs~load_msg_addr();
  set_data(begin_cell().store_slice(sender_address).end_cell());
}
`}
      </pre>

      <Title weight="4">🔹 Использование getter-методов</Title>
      <Title style={{ fontSize: 16, marginTop: 4 }}>
        Создадим функцию <code>get_the_latest_sender</code> для получения последнего отправителя:
      </Title >
      <pre>
        {`
slice get_the_latest_sender() method_id {
  slice ds = get_data().begin_parse();
  return ds~load_msg_addr();
}
`}
      </pre>

      <Title weight="4">🛠 Компиляция контракта</Title>
      <Title style={{ fontSize: 16, marginTop: 4 }}>Запустите <code>yarn compile</code> для проверки кода. Если все верно, вы увидите:</Title >
      <pre>
        {`
=================================================================
Compile script is running, let's find some FunC code to compile...
 - Compilation successful!
 - Compiled code saved to build/main.compiled.json
✨  Done in 1.40s.
`}

        <video controls>
          <source src="https://cdn.stepik.net/video/326904/1080/0ca4e213567348d4338e11870be61b32.mp4" type="video/mp4" />

        </video>

      </pre>
      <Title style={{ fontSize: 16, marginTop: 4 }}>Поздравляем с написанием первого контракта на FunC!</Title >
    </div>
  );
};

export const Lesson3_5 = () => {
  return (
    <div>
      <Title weight="7" style={{ marginTop: 16 }}>3.5 Развертывание смарт-контракта</Title>



      <Title weight="4">📚 Конспект урока</Title>
      <Title style={{ fontSize: 16, marginTop: 4 }}>
        Как мы уже упоминали в первом уроке этой главы, инструментарий программирования в TON развивается, и иногда нет смысла создавать собственную локальную среду с нуля. Стоит упомянуть об отличном инструменте, который поддерживается командой TonTech и официально поддерживается TON Foundation — Blueprint. С ним деплой также очень упрощается. Однако здесь мы создадим свой собственный сценарий развертывания, чтобы лучше понять его логику.
      </Title >

      <Title style={{ fontSize: 16, marginTop: 4 }}>
        Мы уже на финишной прямой! Если вы дошли до этой части, я более чем уверен, что вы сможете стать разработчиком TON-контрактов. Для меня большая честь вести вас на этом пути!
      </Title >

      <Title style={{ fontSize: 16, marginTop: 4 }}>
        На данный момент мы создали инструментарий для компиляции и тестирования. Но спутники должны быть в космосе — и смарт-контракты должны быть в блокчейне. Вперед!
      </Title >

      <Title weight="4">Работаем с тестнетом</Title>
      <Title style={{ fontSize: 16, marginTop: 4 }}>
        Вы уже слышали о тестнете — отдельной сети, предназначенной для тестирования разработчиками всех возможных компонентов. Тестнет работает с тестовыми монетами TON. Они не имеют реальной стоимости, поэтому вам не нужно их покупать. Вы можете легко получить их у официального телеграм-бота testnet giver.
      </Title >

      <Title style={{ fontSize: 16, marginTop: 4 }}>
        Мы собираемся развернуть наш контракт в тестнете. Бот запросит у вас адрес, поэтому вам нужно иметь кошелек, который будет работать с тестовыми монетами TON. Вы, вероятно, знакомы с кошельками Tonhub и Tonkeeper — они имеют встроенную функцию testnet. Вы можете использовать любой из них; я же для примера буду использовать кошелек Tonhub, созданный командой TonWhales. Он доступен для iOS и Android. Перейдите по ссылке и загрузите приложение кошелька.
      </Title >

      <Title weight="4">Скрипт деплоя</Title>
      <Title style={{ fontSize: 16, marginTop: 4 }}>
        Создадим новый файл в папке <code>scripts</code> — <code>deploy.ts</code>. Как вы помните, для развертывания контракта нам необходимо:
      </Title >
      <ul>
        <li>Вычислить будущий адрес контракта.</li>
        <li>Создать исходное состояние контракта (код и исходные данные).</li>
        <li>Составить сообщение с исходным состоянием.</li>
        <li>Отправить это сообщение на будущий адрес контракта в блокчейне.</li>
      </ul>

      <pre>
        {`import { hex } from "../build/main.compiled.json";
import { beginCell, Cell, contractAddress, StateInit, storeStateInit } from "@ton/ton";

async function deployScript() {
  export const codeCell = Cell.fromBoc(Buffer.from(hex, "hex"))[0];
  export const dataCell = new Cell();
  export const stateInit: StateInit = { code: codeCell, data: dataCell };
  export const stateInitBuilder = beginCell();
  storeStateInit(stateInit)(stateInitBuilder);
  export const stateInitCell = stateInitBuilder.endCell();
  export const address = contractAddress(0, { code: codeCell, data: dataCell });
}

deployScript();`}
      </pre>

      <Title style={{ fontSize: 16, marginTop: 4 }}>
        Нам здесь знакомо все кроме типа <code>StateInit</code> и функции <code>storeStateInit</code>. <code>storeStateInit</code> предоставляется библиотекой <code>@ton/core</code> , он помогает нам создать ячейку, в которой будет <code>StateInit</code>.
      </Title >

      <Title style={{ fontSize: 16, marginTop: 4 }}>...</Title >

      <video controls>
        <source src="/videos/3.5.mp4" type="video/mp4" />

      </video>

      <Title weight="4">Ончейн-тесты после деплоя</Title>
      <Title style={{ fontSize: 16, marginTop: 4 }}>
        Да, у вас все получилось! Но есть еще одна важная вещь, о которой стоит позаботиться — ончейн-тестирование после деплоя. После того как наш контракт развернут, нужно проверить, что он ведет себя так, как мы хотим.
      </Title >
    </div>
  );
};

export const Lesson3_6 = () => {
  return (
    <div>
      <Title weight="7" style={{ marginTop: 16 }}>3.6 Развертывание из тестовой сети (тестнета) в основную (мейннет)</Title>

      <Title weight="4">📝 В этом уроке вы узнаете</Title>

      <Title style={{ fontSize: 16, marginTop: 4 }}>В ходе этого урока мы настроим наш скрипт так, чтобы он гибко переключался между тестнетом и мейннетом. Затем мы научимся разворачивать наш контракт в мейннете.</Title >

      <Title weight="4">Добавляем переменную окружения</Title>
      <Title style={{ fontSize: 16, marginTop: 4 }}>Для этого нам понадобится новая переменная окружения <code>testnet</code>. Мы добавим её в наш скрипт, чтобы он знал, какую сеть использовать. В соответствии с этой новой переменной мы также скорректируем наш код.</Title >

      <Title style={{ fontSize: 16, marginTop: 4 }}>Сначала обновим раздел скрипта. Начнем с добавления команды <code>mainnet</code>:</Title >
      <pre><code>{`"deploy:mainnet": "yarn compile && ts-node ./scripts/deploy.ts"`}</code></pre>
      <Title style={{ fontSize: 16, marginTop: 4 }}>Мы хотим, чтобы <code>testnet</code> была переменной окружения по умолчанию:</Title >
      <pre><code>{`"deploy": "TESTNET=true yarn compile && ts-node ./scripts/deploy.ts"`}</code></pre>

      <Title style={{ fontSize: 16, marginTop: 4 }}>Теперь чтобы использовать для деплоя мейннет, добавим mainnet для тестирования:</Title >
      <pre><code>{`"onchaintest:mainnet": "ts-node ./scripts/onchaintest.ts"`}</code></pre>

      <video controls>
        <source src="/videos/3.6.mp4" type="video/mp4" />

      </video>

      <Title weight="4">Настройка dotenv</Title>
      <Title style={{ fontSize: 16, marginTop: 4 }}>Для установки dotenv выполните команду:</Title >
      <pre><code>yarn add dotenv</code></pre>
      <Title style={{ fontSize: 16, marginTop: 4 }}>Добавим <code>dotenv</code> в код:</Title >
      <pre><code>{`import dotenv from "dotenv";\ndotenv.config();`}</code></pre>

      <Title weight="4">Изменение зависимости от сети</Title>
      <Title style={{ fontSize: 16, marginTop: 4 }}>В <code>deploy.ts</code> используем <code>process.env.TESTNET</code> для переключения между тестнетом и мейннетом:</Title >
      <pre><code>{`let link = \`https://tonhub.com/transfer/\` + address.toString({ testOnly: process.env.TESTNET ? true : false });`}</code></pre>

      <Title weight="4">Изменяем логи</Title>
      <Title style={{ fontSize: 16, marginTop: 4 }}>Для логов при деплое добавим информацию о сети:</Title >
      <pre><code>{`console.log(\`Please scan the QR code below to deploy the contract to \${process.env.TESTNET ? "testnet" : "mainnet"}:\`);`}</code></pre>

      <Title weight="4">Скрипты</Title>
      <pre><code>{`
"scripts": {
  "compile": "ts-node ./scripts/compile.ts",
  "test": "yarn compile && yarn jest",
  "deploy": "yarn compile && TESTNET=true ts-node ./scripts/deploy.ts",
  "deploy:mainnet": "yarn compile && ts-node ./scripts/deploy.ts",
  "onchaintest": "TESTNET=true ts-node ./scripts/onchaintest.ts",
  "onchaintest:mainnet": "ts-node ./scripts/onchaintest.ts"
}`}</code></pre>

      <Title weight="4">Деплой в мейннете</Title>
      <Title style={{ fontSize: 16, marginTop: 4 }}>Теперь можно выполнить команду <code>yarn deploy:mainnet</code> для развертывания в мейннете. Это позволит увидеть контракт в Tonhub и подтвердить транзакцию реальными средствами.</Title >

      <Title style={{ fontSize: 16, marginTop: 4 }}>Поздравляем! Вы завершили урок по развертыванию из тестнета в мейннет.</Title >
    </div>
  );
};


export const Lesson8_4 = () => {
  return (
    <div>
      <div style={{ padding: '20px', fontFamily: 'Arial, sans-serif' }}>
        <div style={{ textAlign: 'center', marginBottom: '20px' }}>

          <video controls>
            <source src="/videos/8.4.mp4" type="video/mp4" />

          </video>


          <h2>📚 Конспект урока</h2>
        </div>

        <section style={{ marginBottom: '20px' }}>
          <p>
            В контрактах на виртуальной машине Ethereum Virtual Machine можно получать доступ к каждому сохраненному элементу отдельно и менять его.
            В TON хранилище доступно через <code>get_data()</code>/<code>set_data()</code> (регистр c4 содержит ссылки на ячейки).
            Таким образом, разработчик должен «управлять» хранилищем вручную.
          </p>
        </section>

        <section style={{ marginBottom: '20px' }}>
          <h3>Стандартный обработчик сообщений</h3>
          <p>
            Стандартный обработчик сообщений в TON следует этому подходу:
          </p>
          <pre>
            {`() handle_something(...) impure {
    (int total_supply, <a lot of vars>) = load_data();
    
    ... ;; do something, change data
    
    save_data(total_supply, <a lot of vars>);
}`}
          </pre>
          <p>
            Увы, мы понимаем, что <code>&lt;a lot of vars&gt;</code> — это реальное перечисление всех полей данных контракта. Например:
          </p>
          <pre>
            {`(int total_supply, int swap_fee, int min_amount, int is_stopped, int user_count, int max_user_count, 
slice admin_address, slice router_address, slice jettonA_address, slice jettonA_wallet_address, 
int jettonA_balance, int jettonA_pending_balance, slice jettonB_address, slice jettonB_wallet_address, 
int jettonB_balance, int jettonB_pending_balance, int mining_amount, int datetime_amount, int minable_time, 
int half_life, int last_index, int last_mined, cell mining_rate_cell, cell user_info_dict, cell operation_gas, 
cell content, cell lp_wallet_code) = load_data();`}
          </pre>
        </section>

        <section style={{ marginBottom: '20px' }}>
          <h4>Проблема 1: сложно обновлять структуру хранилища</h4>
          <p>
            Добавление еще одного поля требует обновления всего контракта.
            Если вы решите добавить еще одно поле, например <code>is_paused</code>, вам нужно будет обновить операторы
            <code>load_data()</code> и <code>save_data()</code> во всем контракте. А это не только трудоемко, но и приводит к ошибкам, которые сложно заметить.
          </p>
        </section>

        <section style={{ marginBottom: '20px' }}>
          <h4>Проблема 2: загрязнение пространства имен</h4>
          <p>
            Чтение всех полей хранилища в текущем пространстве имен загрязняет его. В одном из примеров из аудита,
            <code>min_amount</code> был заменен локальной переменной, что позволило злоумышленнику перезаписать стейт контракта.
          </p>
        </section>

        <section style={{ marginBottom: '20px' }}>
          <h4>Проблема 3: повышение стоимости газа</h4>
          <p>
            Парсинг всего хранилища и упаковка его обратно при каждом вызове каждой функции увеличивает стоимость газа.
          </p>
        </section>

        <section style={{ marginBottom: '20px' }}>
          <h4>Решение 1: использовать глобальные переменные</h4>
          <p>
            На этапе прототипирования можно использовать глобальные переменные:
          </p>
          <pre>
            {`global int var1;
global cell var2;
global slice var3;

() load_data() impure {
    var cs = get_data().begin_parse();
    var1 = cs~load_coins();
    var2 = cs~load_ref();
    var3 = cs~load_bits(512);
}

() save_data() impure {
    set_data(
        begin_cell()
            .store_coins(var1)
            .store_ref(var2)
            .store_bits(var3)
            .end_cell()
        );
}`}
          </pre>
        </section>

        <section>
          <h4>Решение 2: использовать вложенное хранение</h4>
          <p>
            После прототипирования можно организовать хранение более эффективно:
          </p>
          <pre>
            {`() handle_something(...) impure {
    (slice swap_data, cell liquidity_data, cell mining_data, cell discovery_data) = load_data();
    (int total_supply, int swap_fee, int min_amount, int is_stopped) = swap_data.parse_swap_data();
    …
    swap_data = pack_swap_data(total_supply + lp_amount, swap_fee, min_amount, is_stopped);
    save_data(swap_data, liquidity_data, mining_data, discovery_data);
}`}
          </pre>
          <p>
            Вложенные переменные могут быть нескольких уровней вложенности, что позволяет уменьшить загрязнение пространства имен.
          </p>
        </section>
      </div>

      <div style={{ padding: '20px', fontFamily: 'Arial, sans-serif' }}>
        <h2>Используйте end_parse()</h2>
        <p>
          Используйте <code>end_parse()</code> везде, где это возможно, при чтении данных из хранилища и из полезной
          нагрузки сообщения.
        </p>
        <p>
          Поскольку TON использует битовые потоки с переменным форматом данных, полезно убедиться, что вы читаете
          столько же, сколько и пишете. Это может сэкономить вам часы отладки.
        </p>

        <h3>Используйте вспомогательные функции и избегайте магических чисел</h3>
        <p>
          Этот код взят из реального проекта. Он может напугать даже опытного разработчика из-за большого количества
          магических чисел:
        </p>
        <pre style={{ background: '#f4f4f4', padding: '10px', borderRadius: '5px' }}>
          {`var msg = begin_cell()
    .store_uint(0xc4ff, 17)         ;; 0 11000100 0xff
    .store_uint(config_addr, 256)
    .store_grams(1 << 30)           ;; ~1 gram of value
    .store_uint(0, 107)
    .store_uint(0x4e565354, 32)
    .store_uint(query_id, 64)
    .store_ref(vset);
    
send_raw_message(msg.end_cell(), 1);`}
        </pre>
        <p>Вводите столько констант и оберток, сколько необходимо, для получения более выразительного кода:</p>
        <pre style={{ background: '#f4f4f4', padding: '10px', borderRadius: '5px' }}>
          {`var msg = begin_cell()
    .store_msg_flags(BOUNCEABLE)
    .store_slice(to_wallet_address)
    .store_coins(amount)
    .store_msgbody_prefix_stateinit()
    .store_ref(state_init)
    .store_ref(master_msg);

send_raw_message(msg.end_cell(), SEND_MODE_PAY_FEES_SEPARETELY);`}
        </pre>

        <h3>Пример бага</h3>
        <p>
          Не забывайте о подводных камнях и потенциальных ошибках, не связанных конкретно с TON. Вот пример из реального
          проекта:
        </p>
        <pre style={{ background: '#f4f4f4', padding: '10px', borderRadius: '5px' }}>
          {`() handle_transfer(...) impure {
  ...
  (slice from_user_info, int from_flag) = user_info_dict.udict_get?(256, from_addr_hash);
  int from_balance = from_user_info~load_coins();
  ...
  (slice to_user_info, int to_flag) = user_info_dict.udict_get?(256, to_addr_hash);
  int to_balance = to_user_info~load_coins();
  ...
  ;; save decreased from_balance to user_info_dict
  ;; save increased to_balance to user_info_dict
}`}
        </pre>
        <p>
          Перевод денег на один и тот же адрес фактически удваивает баланс, поскольку <code>to_balance</code> перезаписывает
          обнуленный <code>from_balance</code>.
        </p>

        <h3>Подведем итоги</h3>
        <ul>
          <li>Используйте вложенную структуру хранения и глобальные переменные для надежной работы с хранилищем.</li>
          <li>Пишите обертки и объявляйте константы, чтобы сохранить выразительность кода.</li>
          <li>Тестируйте свой код.</li>
          <li>Проводите тщательный аудит, чтобы избежать убытков.</li>
        </ul>
      </div>
    </div>
  );
};


export const Lesson8_3 = () => {
  return (
    <div style={{ padding: '20px', fontFamily: 'Arial, sans-serif' }}>
      <h2>📚 Конспект урока</h2>

      <video controls>
        <source src="/videos/8.3.mp4" type="video/mp4" />

      </video>


      <section>
        <p>
          <strong>В EVM:</strong> если пользователь предоставит слишком мало газа, всё будет отменено.
          Если же он предоставит достаточно газа, фактические затраты будут автоматически рассчитаны и вычтены из его баланса.
        </p>
        <p>
          В <strong>Solidity</strong> газ не представляет особой проблемы для разработчиков контрактов. Если пользователь предоставит слишком мало газа, всё будет отменено, как будто ничего не произошло (но газ не будет возвращен).
          Если же он предоставит достаточно, фактические затраты будут автоматически рассчитаны и вычтены из его баланса.
        </p>
      </section>

      <section>
        <h3>В TON ситуация иная:</h3>
        <ul>
          <li>Если газа недостаточно, транзакция будет выполнена частично.</li>
          <li>Если газа слишком много, излишки необходимо вернуть, за что отвечает разработчик.</li>
          <li>TON не может делать всё сам из-за своей асинхронной природы.</li>
        </ul>
        <p>
          Если «группа контрактов» обменивается сообщениями, то управление и расчеты должны выполняться в каждом сообщении.
          Полное выполнение транзакции со всеми вытекающими последствиями может занять время, и к концу у пользователя может не оказаться достаточного количества TON в кошельке.
          Здесь используется принцип Carry-Value.
        </p>
      </section>

      <section>
        <h3>Подсчет газа и проверка msg_value</h3>
        <ol>
          <li>Найдите входящие точки потока сообщений.</li>
          <li>Определите стоимость каждого обработчика.</li>
          <li>Проверьте, достаточна ли msg_value на входящих точках.</li>
          <li>Не требуйте везде с запасом — газ автоматически делится между «последствиями».</li>
        </ol>
        <p>
          Например, если контракт отправляет три сообщения, можно отправить каждому по 0,33 TON, чтобы избежать лишних требований.
          Если код начинает отправлять больше сообщений, потребности в газе необходимо перепроверить.
        </p>
      </section>

      <section>
        <h3>Аккуратно возвращайте излишки газа</h3>
        <ul>
          <li>Если излишки газа не возвращать отправителю, со временем они будут накапливаться в контрактах, что неоптимально.</li>
          <li>Вы можете добавить функцию для извлечения лишнего газа, как в TON Jetton, где газ возвращается отправителю с сообщением <code>op::excesses</code>.</li>
        </ul>
        <pre style={{ background: '#f4f4f4', padding: '10px', borderRadius: '5px' }}>
          {`send_raw_message(msg, SEND_MODE_CARRY_ALL_REMAINING_MESSAGE_VALUE)`}
        </pre>
        <p>
          Этот режим передаёт остаток газа, полезен при линейном потоке сообщений, когда каждый обработчик отправляет одно сообщение.
        </p>
      </section>

      <section>
        <h3>Когда не рекомендуется хранить весь неиспользованный газ</h3>
        <ul>
          <li>Storage fee вычитается из баланса контракта, а не из поступающего газа.</li>
          <li>Генерация событий съедает баланс контракта, а не газ.</li>
          <li>Прикрепление значения к сообщению или использование SEND_MODE_PAY_FEES_SEPARETELY также съедает баланс.</li>
        </ul>
        <pre style={{ background: '#f4f4f4', padding: '10px', borderRadius: '5px' }}>
          {`var msg = begin_cell().store_uint(0x18, 6).store_slice(destination)
      .store_coins(10000000)                ;; This will be deducted from contract balance
      .store_uint(1, 1 + 4 + 4 + 64 + 32 + 1 + 1).store_ref(msg_body);
    send_raw_message(msg.end_cell(), 0);`}
        </pre>
        <p>
          Эти действия расходуют баланс контракта, что может стать убыточным, если не возвращать неиспользованный газ.
        </p>
      </section>

      <section>
        <h3>Стандартный способ расчета стоимости газа</h3>
        <p>Пример кода из TON Wallet:</p>
        <pre style={{ background: '#f4f4f4', padding: '10px', borderRadius: '5px' }}>
          {`int ton_balance_before_msg = my_ton_balance - msg_value;
int storage_fee = const::min_tons_for_storage - min(ton_balance_before_msg, const::min_tons_for_storage);
msg_value -= storage_fee + const::gas_consumption;

if(forward_ton_amount) {
    msg_value -= (forward_ton_amount + fwd_fee);
    ...
}

if (msg_value > 0) {    ;; there is still something to return
    var msg = begin_cell()
        .store_uint(0x10, 6)
        .store_slice(response_address)
        .store_coins(msg_value)
    ...`}
        </pre>
        <p>
          Помните: если средства на балансе контракта закончатся, транзакция будет выполнена частично.
        </p>
      </section>

      <section>
        <h3>Resume</h3>
        <ul>
          <li>TON требует от разработчиков управлять газом и проверять его достаточность.</li>
          <li>Газовые затраты могут расти при использовании неограниченной структуры данных.</li>
          <li>Рекомендуется возвращать излишки газа отправителю.</li>
          <li>Если газ закончится, транзакция будет выполнена частично, что может быть критичным.</li>
        </ul>
      </section>
    </div>
  );
};



export const Lesson8_2 = () => (
  <div style={{ padding: '20px', fontFamily: 'Arial, sans-serif' }}>
    <h2>📚 Конспект урока</h2>

    <video controls>
      <source src="/videos/8.2.mp4" type="video/mp4" />

    </video>


    <section>
      <p>
        <strong>При разработке для виртуальной машины Ethereum</strong>, для удобства проект часто разбивается на несколько контрактов. В некоторых случаях можно реализовать весь функционал в одном контракте, не разделяя его, даже когда это необходимо (например, пары ликвидности в AMM).
      </p>
      <p>
        <em>В EVM:</em> транзакции исполняются полностью: либо все получается, либо все возвращается.
      </p>
      <p>
        <strong>В TON</strong> рекомендуется избегать неограниченных структур данных и делить контракт на небольшие части, которые управляют маленькими объемами данных. Пример — реализация Jetton (TON-версия стандарта ERC-20 токенов).
      </p>
    </section>

    <section>
      <h3>Шардинг контрактов</h3>
      <p>Структура Jetton:</p>
      <ul>
        <li><strong>jetton-minter:</strong> хранит total_supply, minter_address, метаданные, jetton_wallet_code</li>
        <li><strong>jetton-wallet:</strong> для каждого владельца, хранит адрес владельца, баланс, адрес jetton-minter, jetton_wallet_code</li>
      </ul>
      <p>Jetton-minter хранит общие данные о токенах, а jetton-wallet хранит индивидуальные данные для каждого владельца. Это позволяет передавать Jetton между кошельками напрямую, что важно для параллельной обработки транзакций.</p>
    </section>

    <section>
      <h3>Возможно частичное выполнение транзакций</h3>
      <p>В TON контракты могут частично выполнять транзакции. Например, поток сообщений в стандартном Jetton может включать:</p>
      <ol>
        <li>Отправка сообщения <code>op::transfer</code> на <code>sender_wallet</code>.</li>
        <li>Уменьшение баланса токенов в <code>sender_wallet</code>.</li>
        <li>Отправка <code>op::internal_transfer</code> на <code>destination_wallet</code>.</li>
        <li>Увеличение баланса токенов в <code>destination_wallet</code>.</li>
        <li>Отправка <code>op::transfer_notification</code> получателю.</li>
        <li>Возврат лишнего газа.</li>
      </ol>
      <p>Если <code>destination_wallet</code> не сможет обработать сообщение <code>op::internal_transfer</code>, шаги после этого не будут выполнены. Это может привести к неконсистентности и потере денег.</p>
    </section>

    <section>
      <h3>Схемы движения сообщений</h3>
      <p>Рекомендуется составлять диаграммы для отслеживания сообщений в контрактах. Это особенно полезно для сложных контрактов, как децентрализованные биржи (DEX), где в одном процессе может быть более десятка сообщений.</p>
      <p>В TON внешние вызовы представляют собой сообщения, которые обрабатываются позже, что требует тщательного проектирования.</p>
    </section>

    <section>
      <h3>Избегайте неудачных отправок и обрабатывайте отклоненные сообщения</h3>
      <ul>
        <li>Проверьте полезную нагрузку и подачу газа на входящих точках.</li>
        <li>Проверяйте источник сообщений на последующих шагах.</li>
      </ul>
      <p>Не допускайте сбоев на последующих шагах. Если возникает сомнение, проверьте поток сообщений.</p>
      <p>Отклоненные сообщения также рекомендуется обрабатывать. Например, в Jetton, если кошелек получателя не принимает токены, они возвращаются отправителю.</p>
    </section>

    <section>
      <h3>Carry-Value Pattern</h3>
      <p>
        Этот паттерн предполагает передачу значений, а не только сообщений. Например, <code>sender_wallet</code> вычитает баланс и отправляет его на <code>destination_wallet</code>, который либо принимает, либо возвращает значение обратно.
      </p>
      <p>В Jetton можно проверить баланс, но невозможно получить точный баланс ончейн, так как к моменту получения запроса баланс может измениться.</p>
    </section>

    <section>
      <h3>Возврат значения вместо отказа</h3>
      <p>
        Вместо отклонения запроса рекомендуется возвращать Jetton. Это помогает избежать потерь при неудачном выполнении запроса.
      </p>
      <p>
        Например, при получении <code>op::transfer</code> от <code>sender_wallet</code> и недостатке газа или несоответствии полезной нагрузки, Jetton должны быть возвращены.
      </p>
    </section>

    <section>
      <h3>Подведение итогов</h3>
      <ul>
        <li>В TON важно избегать неограниченных структур данных и учитывать асинхронную природу сообщений.</li>
        <li>Jetton требует ручного анализа полезной нагрузки и условий при получении.</li>
        <li>Возвращайте значения с помощью <code>sender_address</code>, так как адрес отправителя может быть подменен.</li>
      </ul>
    </section>
  </div>
);




export const Lesson8_1 = () => {
  return (
    <div>
      <h1>💎 Добро пожаловать в восьмую часть!</h1>
      <p>
        В этой главе вы изучите различные темы, связанные с безопасностью и
        управлением блокчейном в TON. Начнем с углубленного изучения наивного
        кошелька, атак повторного воспроизведения, атак отказа в обслуживании и
        рекомендаций по контрактам. После этого переключимся на поток сообщений,
        получим необходимые знания и навыки для эффективной работы с ним. Управление
        газом в TON — еще один важный аспект, рассматриваемый в одном из уроков, где
        вы поймете и освоите эффективные стратегии. Кроме того, глава включает в
        себя краткий курс по управлению хранением данных в TON — там вы получите
        необходимые знания и приемы для эффективного хранения данных.
      </p>

      <h2>📚 Конспект урока</h2>
      <p>
        TON — это инновационный блокчейн, в котором реализованы новейшие
        концепции разработки смарт-контрактов. Но она всё еще требует
        аккуратности. К традиционным угрозам безопасности и атакам добавляются
        новые, уникальные для TON. Давайте обсудим их.
      </p>

      <h3>В этой части мы поговорим о внешних сообщениях.</h3>

      <h4>Входные точки смарт-контрактов</h4>
      <p>
        Как вы знаете, у смарт-контракта в TON есть три возможные входные точки:
      </p>
      <ul>
        <li>Каждый контракт должен включать функцию recv_internal.</li>
        <li>Опционально, recv_external применяется для входящих внешних сообщений.</li>
        <li>run_ticktock вызывается в ticktock-тразнакциях в специальных смарт-контрактах. Обычные контракты это не используют.</li>
      </ul>

      <p>
        Здесь мы поговорим о recv_external. Концепция вам знакома: пользователь
        создает транзакцию офф-чейн, подписывает приватным ключом и отправляет ее
        контракту.
      </p>

      <h4>Реализация наивного кошелька</h4>
      <p>Кошелек в TON — это просто смарт-контракт. Давайте напишем простой наивный кошелек:</p>
      <pre>
        {`
() recv_external(slice in_msg) impure {     
    var signature = in_msg~load_bits(512);  ;; считываем подпись из входящего сообщения
    var cs = in_msg;                        ;; создаем копию входящего сообщения
    var ds = get_data().begin_parse();      ;; чтение хранилища контрактов
    var public_key = ds~load_uint(256);     ;; чтение авторизованного ключа (владельца контракта)
    throw_unless(34, check_signature(slice_hash(in_msg), signature, public_key));
    accept_message();                       ;; мы готовы заплатить за газ, чтобы обработать это
    var mode = cs~load_uint(8);             ;; считываем mode из входящего сообщения
    send_raw_message(cs~load_ref(), mode);  ;; отправляем полученное сообщение
}
        `}
      </pre>

      <p>
        Этот контракт будет обрабатывать сообщения, подписанные владельцем
        контракта. Но ничто не мешает злоумышленникам прочитать полезную нагрузку и
        повторить передачу. Это известно как атака повторного воспроизведения.
        Злоумышленник получает сообщение, подписанное владельцем контракта, и снова
        отправляет его на этот кошелек. Оно будет выполнено еще раз.
      </p>

      <h4>Атака повторного воспроизведения</h4>
      <p>
        Владельцу кошелька теперь нужно указать временную метку, до которой сообщение
        действительно. Однако в течение этого времени оно все еще может быть
        воспроизведено.
      </p>
      <pre>
        {`
() recv_external(slice in_msg) impure {
    var signature = in_msg~load_bits(512);
    var cs = in_msg; 
    var valid_until = cs~load_uint(32);         ;; добавлено
    throw_if(35, valid_until <= now());         ;; добавлено
    var ds = get_data().begin_parse();
    var public_key = ds~load_uint(256);
    throw_unless(34, check_signature(slice_hash(in_msg), signature, public_key));
    accept_message();
    var mode = cs~load_uint(8);
    send_raw_message(cs~load_ref(), mode);
}
        `}
      </pre>

      <h4>Атака повторного воспроизведения (с порядковым номером)</h4>
      <p>
        Теперь владельцу кошелька необходимо указать порядковый номер, который
        увеличивается по сравнению с сохраненным. Это гарантирует, что любое
        сообщение может быть обработано только один раз:
      </p>
      <pre>
        {`
() recv_external(slice in_msg) impure {
    var signature = in_msg~load_bits(512);
    var cs = in_msg; 
    var (msg_seqno, valid_until) = (cs~load_uint(32), cs~load_uint(32));            ;; изменено
    throw_if(35, valid_until <= now());
    var ds = get_data().begin_parse(); 
    var (stored_seqno, public_key) = (ds~load_uint(32), ds~load_uint(256));         ;; изменено
    throw_unless(33, msg_seqno == stored_seqno);                                    ;; добавлено
    throw_unless(34, check_signature(slice_hash(in_msg), signature, public_key));
    accept_message();
    var mode = cs~load_uint(8);
    send_raw_message(cs~load_ref(), mode);

    set_data(begin_cell()                                   ;; добавлено
        .store_uint(stored_seqno + 1, 32)
        .store_uint(public_key, 256)
        .end_cell());                                   
}
        `}
      </pre>

      <h4>Атака отказа в обслуживании</h4>
      <p>
        Важное замечание: <code>recv_external</code> следует использовать осторожно, а{" "}
        <code>accept_message()</code> — только после проверки. В противном случае за
        счет вредоносных «безгазовых» исключений из контракта можно выкачать весь газ.
      </p>

      <pre>
        {`
() recv_external(slice in_msg) impure {
    var signature = in_msg~load_bits(512);
    var cs = in_msg; 
    var (msg_seqno, valid_until) = (cs~load_uint(32), cs~load_uint(32));    
    throw_if(35, valid_until <= now());
    var ds = get_data().begin_parse(); 
    var (stored_seqno, public_key) = (ds~load_uint(32), ds~load_uint(256)); 
    throw_unless(33, msg_seqno == stored_seqno);                                    
    throw_unless(34, check_signature(slice_hash(in_msg), signature, public_key));

    accept_message();                           ;; accept after checking signature only

    var mode = cs~load_uint(8);
    send_raw_message(cs~load_ref(), mode);
    set_data(begin_cell()
        .store_uint(stored_seqno + 1, 32)
        .store_uint(public_key, 256)
        .end_cell());                                   
}
        `}
      </pre>

      <h3>Неудачная атака повторного воспроизведения</h3>
      <p>
        Обратите внимание, что если после <code>accept_message()</code> произойдет
        ошибка, то транзакция будет записана в блокчейн и с баланса контракта будет
        списана комиссия. Но хранилище не будет обновлено и действия не будут
        применены.
      </p>

      <h3>EVM vs TON</h3>
      <p>
        В виртуальной машине Ethereum (EVM) защита от повторного воспроизведения
        осуществляется «из коробки». В TON защита более гибкая и требует внимательного
        подхода.
      </p>

      <h3>Рекомендации</h3>
      <p>
        К счастью, есть простая рекомендация для всех разработчиков смарт-контрактов
        TON: избегайте обработки внешних сообщений.
      </p>
    </div>
  );
};

export const Lesson7_5 = () => {
  return (
    <div>
      <h1>📚 Конспект урока</h1>
      <p>В этом уроке мы рассмотрим код классического объекта NFT. А также узнаем о предмете SoulBound (SBT).</p>

      <h2>Контракт NFT-объекта</h2>
      <p>Откроем и рассмотрим контракт.</p>
      <pre>
        <code>
          {`int min_tons_for_storage() asm "50000000 PUSHINT"; ;; 0.05 TON`}
        </code>
      </pre>
      <p>
        Как и в предыдущих уроках, мы видим, что есть определенные константы, которые показывают, какое минимальное количество монет мы должны хранить в контракте, чтобы иметь возможность оплатить аренду.
      </p>

      <pre>
        <code>
          {`(int, int, slice, slice, cell) load_data() {{
	slice ds = get_data().begin_parse();
	var(index, collection_address) = (ds~load_uint(64), ds~load_msg_addr());
	if (ds.slice_bits() > 0) {{
		return(-1, index, collection_address, ds~load_msg_addr(), ds~load_ref());
	}} else {{
		return(0, index, collection_address, null(), null()); // nft not initialized yet
	}}
}}`}
        </code>
      </pre>
      <p>
        Затем мы видим load_data. Этот load_data немного отличается от того, что мы видели раньше. Обычно мы просто читали данные из локального хранилища, но здесь у нас более сложная логика. Итак, мы открываем ячейку локального хранилища для чтения, а затем считываем index и collection_address. Мы знаем, что такое индекс и адрес коллекции.
      </p>

      <h2>store_data</h2>
      <pre>
        <code>
          {`() store_data(int index, slice collection_address, slice owner_address, cell content) impure {{
	set_data(
		begin_cell()
			.store_uint(index, 64)
			.store_slice(collection_address)
			.store_slice(owner_address)
			.store_ref(content)
			.end_cell()
	);
}}`}
        </code>
      </pre>
      <p>
        Мы также видим, что store_data — это всего лишь регулярное выражение, как и в других контрактах, что мы видели.
      </p>

      <video controls>
        <source src="/videos/7.5.mp4" type="video/mp4" />

      </video>


      <h2>Заключение</h2>
      <p>
        От очень простой логики третьей части мы прошли путь к пониманию множества вещей в сложных контрактах, таких как NFT или Jetton. Шаг за шагом вы можете изучать определенный синтаксис и концепции, и тогда перед вами откроется более сложная логика.
      </p>
      <p>Я очень ценю ваше внимание. Возможно, это самая сложная глава для вас, хотя мы ничего не кодили, а просто читали материал, который вы никогда раньше не видели. Он очень сложный. Поэтому я очень горжусь тем, что вы дошли до этого последнего урока, и с нетерпением жду встречи с вами в следующих главах. Надеюсь, вам нравится этот курс.</p>
    </div>
  )
};


export const Lesson7_4 = () => {
  return (
    <div className="lesson-container">
      <h1>📚 Конспект урока</h1>

      {/* Video Section */}
      <video controls>
        <source src="/videos/7.4.mp4" type="video/mp4" />

      </video>


      {/* Lesson Content */}
      <div className="lesson-content">
        <p>
          В том же репозитории, где хранится код взаимозаменяемых токенов, размещен код невзаимозаменяемых токенов. Это стандартная реализация стандарта NFT, описанная так же, как и Jetton. Здесь вы можете найти примеры метаданных в соответствии со стандартами, которые мы рассматривали в предыдущих уроках.
        </p>
        <p>
          Для NFT также есть два типа контрактов — NFT-коллекции и NFT-объекта. Первый контракт деплоит вторые. Если вы хотите выпустить коллекцию, содержащую 10 000 объектов, вам придется развернуть 10 001 смарт-контракт. Вы будете делать это самостоятельно.
        </p>

        <h3>Смарт-контракт NFT-коллекции</h3>
        <pre>
          {`(slice, int, cell, cell, cell) load_data() inline {
  var ds = get_data().begin_parse();
  return 
    (ds~load_msg_addr(), ;; owner_address
     ds~load_uint(64), ;; next_item_index
     ds~load_ref(), ;; content
     ds~load_ref(), ;; nft_item_code
     ds~load_ref()  ;; royalty_params
     );
}`}
        </pre>

        <p>
          Здесь, в хранилище, у нас есть owner_address, next_item_index, у нас есть content. Опять же, этот content — ячейка, созданная в соответствии со стандартом метаданных, который также доступен в том же стандартном репозитории, который мы рассмотрели. Затем у нас есть nft_item_code и ячейка с royalty_params. Скоро мы узнаем о них больше.
        </p>

        <h3>Функции смарт-контракта</h3>
        <pre>
          {`() save_data(slice owner_address, int next_item_index, cell content, cell nft_item_code, cell royalty_params) impure inline {
  set_data(begin_cell()
    .store_slice(owner_address)
    .store_uint(next_item_index, 64)
    .store_ref(content)
    .store_ref(nft_item_code)
    .store_ref(royalty_params)
    .end_cell());
}`}
        </pre>

        <p>
          В смарт-контракте объекта будут меняться владельцы. Также может быть изменен владелец коллекции. Вам нужно понять эту логику. Первым контрактом, который мы рассмотрим, будет NFT-коллекция, а затем мы рассмотрим смарт-контракт NFT-объекта. Опять же, всё не просто отдельные контракты, это система смарт-контрактов.
        </p>

        <h3>Deploy NFT item</h3>
        <pre>
          {`() deploy_nft_item(int item_index, cell nft_item_code, int amount, cell nft_content) impure {
  cell state_init = calculate_nft_item_state_init(item_index, nft_item_code);
  slice nft_address = calculate_nft_item_address(workchain(), state_init);
  var msg = begin_cell()
            .store_uint(0x18, 6)
            .store_slice(nft_address)
            .store_coins(amount)
            .store_uint(4 + 2 + 1, 1 + 4 + 4 + 64 + 32 + 1 + 1 + 1)
            .store_ref(state_init)
            .store_ref(nft_content);
  send_raw_message(msg.end_cell(), 1); ;; pay transfer fees separately, revert on errors
}`}
        </pre>

        <p>
          Здесь показан пример кода для деплоя NFT. Мы вычисляем state_init и адрес NFT, создаем сообщение с монетами и передаем его для отправки.
        </p>

        <h3>Функция для отправки роялти</h3>
        <pre>
          {`() send_royalty_params(slice to_address, int query_id, slice data) impure inline {
  var msg = begin_cell()
    .store_uint(0x10, 6)
    .store_slice(to_address)
    .store_coins(0)
    .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
    .store_uint(op::report_royalty_params(), 32)
    .store_uint(query_id, 64)
    .store_slice(data);
  send_raw_message(msg.end_cell(), 64); ;; carry all the remaining value of the inbound message
}`}
        </pre>

        <h3>Продолжение примеров</h3>
        <p>
          Далее идут другие примеры операций, таких как изменение владельца коллекции и пакетная деплоя новых NFT. Логика работы этих функций основана на предыдущих описаниях и обеспечена стандартом.
        </p>

        <h3>Заключение</h3>
        <p>
          Этот контракт намного проще, чем тот, который мы создавали с Jetton. Важно, чтобы вы снова увидели, что в TON все является контрактом. Очень простая логика может создать сложную систему контрактов.
        </p>
      </div>
    </div>
  );
};

export const Lesson7_3 = () => {
  return (
    <div>
      <h1>📚 Конспект урока</h1>

      <div>
        <h2>Что такое Jetton-кошелек?</h2>
        <p>
          В этом уроке мы рассмотрим смарт-контракт Jetton-кошелька. Как вы помните, смарт-контракт кошелька разворачивается смарт-контрактом майнера, когда мы майним Jetton. Или же смарт-контракт кошелька разворачивается другим смарт-контрактом кошелька, когда мы осуществляем перевод.
        </p>

        <h3>Подготовка:</h3>
        <pre>
          <code>
            {`int min_tons_for_storage() asm "10000000 PUSHINT"; ;; 0.01 TON
int gas_consumption() asm "15000000 PUSHINT"; ;; 0.015 TON`}
          </code>
        </pre>
        <p>
          В этом коде мы видим, что нам необходимо иметь определенное количество TON для оплаты хранения и газа для выполнения операций.
        </p>

        <video controls>
          <source src="https://cdn.stepik.net/video/326930/1080/abe56f44e1ef5ea0718a4daa410d3f6e.mp4" type="video/mp4" />

        </video>

        <h3>Код хранилища:</h3>
        <pre>
          <code>
            {`{- Storage
storage#_ balance:Coins owner_address:MsgAddressInt jetton_master_address:MsgAddressInt jetton_wallet_code:^Cell = Storage;
}`}
          </code>
        </pre>

        <h3>Функции:</h3>
        <pre>
          <code>
            {`(int, slice, slice, cell) load_data() inline {... }
() save_data (int balance, slice owner_address, slice jetton_master_address, cell jetton_wallet_code) impure inline {... }`}
          </code>
        </pre>
        <p>
          Мы добавляем функции load_data и save_data для инкапсуляции логики работы с хранилищем.
        </p>

        <h3>Логика работы с транзакциями:</h3>
        <pre>
          <code>
            {`int op = in_msg_body~load_uint(32);

if (op == op::transfer()) { ;; outgoing transfer
send_tokens(in_msg_body, sender_address, msg_value, fwd_fee);
return ();
}

if (op == op::internal_transfer()) { ;; incoming transfer
receive_tokens(in_msg_body, sender_address, my_balance, fwd_fee, msg_value);
return ();
}

if (op == op::burn()) { ;; burn
burn_tokens(in_msg_body, sender_address, msg_value, fwd_fee);
return ();
}`}
          </code>
        </pre>

        <h3>Функция получения токенов:</h3>
        <pre>
          <code>
            {`() receive_tokens (slice in_msg_body, slice sender_address, int my_ton_balance, int fwd_fee, int msg_value) impure {... }`}
          </code>
        </pre>

        <h3>Функция сжигания токенов:</h3>
        <pre>
          <code>
            {`() burn_tokens (slice in_msg_body, slice sender_address, int msg_value, int fwd_fee) impure {... }`}
          </code>
        </pre>
      </div>


    </div>
  );
};

export const Lesson7_2 = () => {
  return (
    <div>
      <div className="lesson-notes">
        <h1>📚 Конспект урока</h1>

        <section>
          <h2>Введение</h2>
          <p>
            Мы начнем со стандарта Jetton. Jetton, также известный как стандарт
            взаимозаменяемых токенов, был добавлен в TON в расширении TEP-74. В
            этом репозитории находится полное описание того, как должны
            функционировать контракты Jetton, и два контракта — мастер-контракт
            (майнер-контракт) и контракт кошелька.
          </p>
        </section>

        <video controls>
          <source src="/videos/7.2.mp4" type="video/mp4" />

        </video>

        <section>
          <h2>Спецификация смарт-контракта кошелька</h2>
          <p>
            Смарт-контракт кошелька должен обрабатывать определенные сообщения.
            Например, передавать и объяснять всё, что происходит и что вызвано
            передачей. Он должен обрабатывать сообщения о сгорании. У него есть
            get-метод get_wallet_data, в котором он должен возвращать данные о
            себе. Это основной способ реализации контракта кошелька.
          </p>
        </section>

        <section>
          <h2>Мастер-контракт</h2>
          <p>
            Другой контракт — мастер-контракт. В нем мы размещаем не код, а
            функциональность, которая должна быть реализована определенным образом,
            чтобы другие контракты могли взаимодействовать с вашими контрактами
            Jetton. Они будут точно знать, как работать с вашими контрактами,
            потому что у вас есть мастер-контракты. Все эти Jetton минтит майнер.
          </p>
        </section>

        <section>
          <h2>Работа с кодом контрактов Jetton</h2>
          <p>
            Мы будем использовать еще несколько файлов, например утилиты Jetton, так как
            они содержат определенную логику, которая помогает основному файлу майнера
            Jetton. Кроме того, у нас есть опкоды и параметры — это полезные служебные
            файлы, и мы инкапсулируем в них некоторую логику, чтобы использовать ее повторно.
          </p>
        </section>

        <section>
          <h2>Пример кода майнера Jetton</h2>
          <pre>
            {`;; storage scheme
;; storage#_ total_supply:Coins admin_address:MsgAddress content:^Cell jetton_wallet_code:^Cell = Storage;

(int, slice, cell, cell) load_data() inline {
  slice ds = get_data().begin_parse();
  return (
      ds~load_coins(), ;; total_supply
      ds~load_msg_addr(), ;; admin_address
      ds~load_ref(), ;; content
      ds~load_ref()  ;; jetton_wallet_code
  );
}

() save_data(int total_supply, slice admin_address, cell content, cell jetton_wallet_code) impure inline {
  set_data(begin_cell()
            .store_coins(total_supply)
            .store_slice(admin_address)
            .store_ref(content)
            .store_ref(jetton_wallet_code)
           .end_cell()
          );
}`}
          </pre>
          <p>
            Мы уже знаем, что эта логика включает в себя чтение и сохранение данных в
            хранилище. Мы можем использовать эту функцию в любом месте кода, чтобы
            облегчить процесс чтения данных.
          </p>
        </section>

        <section>
          <h2>Логика минтинга</h2>
          <p>
            Например, мы сохранили функциональность минтинга внутри этой функции.
            Мы готовим какие-то данные, считываем кое-что важное — об этом мы
            еще поговорим. А затем запускаем функцию mint_tokens.
          </p>
        </section>

        <section>
          <h2>Функция обработки сообщений</h2>
          <pre>
            {`() recv_internal(int msg_value, cell in_msg_full, slice in_msg_body) impure {
    if (in_msg_body.slice_empty?()) { ;; ignore empty messages
        return ();
    }
    slice cs = in_msg_full.begin_parse();
    int flags = cs~load_uint(4);

    if (flags & 1) { ;; ignore all bounced messages
        return ();
    }
    slice sender_address = cs~load_msg_addr();

    int op = in_msg_body~load_uint(32);
    int query_id = in_msg_body~load_uint(64);

    (int total_supply, slice admin_address, cell content, cell jetton_wallet_code) = load_data();

    if (op == op::mint()) {
        throw_unless(73, equal_slices(sender_address, admin_address));
        slice to_address = in_msg_body~load_msg_addr();
        int amount = in_msg_body~load_coins();
        cell master_msg = in_msg_body~load_ref();
        slice master_msg_cs = master_msg.begin_parse();
        master_msg_cs~skip_bits(32 + 64); ;; op + query_id
        int jetton_amount = master_msg_cs~load_coins();
        mint_tokens(to_address, jetton_wallet_code, amount, master_msg);
        save_data(total_supply + jetton_amount, admin_address, content, jetton_wallet_code);
        return ();
    }

    if (op == op::burn_notification()) {
        int jetton_amount = in_msg_body~load_coins();
        slice from_address = in_msg_body~load_msg_addr();
        throw_unless(74,
            equal_slices(calculate_user_jetton_wallet_address(from_address, my_address(), jetton_wallet_code), sender_address)
        );
        save_data(total_supply - jetton_amount, admin_address, content, jetton_wallet_code);
        slice response_address = in_msg_body~load_msg_addr();
        if (response_address.preload_uint(2) != 0) {
          var msg = begin_cell()
            .store_uint(0x10, 6) ;; nobounce - int_msg_info$0 ihr_disabled:Bool bounce:Bool bounced:Bool src:MsgAddress -> 011000
            .store_slice(response_address)
            .store_coins(0)
            .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
            .store_uint(op::excesses(), 32)
            .store_uint(query_id, 64);
          send_raw_message(msg.end_cell(), 2 + 64);
        }
        return ();
    }

    if (op == 3) { ;; change admin
        throw_unless(73, equal_slices(sender_address, admin_address));
        slice new_admin_address = in_msg_body~load_msg_addr();
        save_data(total_supply, new_admin_address, content, jetton_wallet_code);
        return ();
    }

    if (op == 4) { ;; change content, delete this for immutable tokens
        throw_unless(73, equal_slices(sender_address, admin_address));
        save_data(total_supply, admin_address, in_msg_body~load_ref(), jetton_wallet_code);
        return ();
    }

    throw(0xffff);
}`}
          </pre>
          <p>
            Здесь вы видите основную структуру обработки сообщений с различными
            операциями, такими как minting, burn и изменение администратора.
          </p>
        </section>
      </div>
      <div>
        <h1>Работа с Контрактом Jetton: Пошаговое Руководство</h1>

        <section>
          <h2>Основы Логики</h2>
          <p>
            Это очень просто, мы уже можем легко понять это. Вы меняете содержимое или отключаете возможность удаления этой логики. Если вы не используете редактируемый контент или хотите сделать его редактируемым, то можете оставить ее. Здесь администратор сможет сохранять данные с различными метаданными. Это базовые принципы работы, и вам нужно лишь немного попрактиковаться, чтобы понять, насколько можно это всё усложнить и что вы можете здесь использовать из того, что вы узнали, когда писали контракт.
          </p>
          <p>
            Конечно, у нас есть и методы get, довольно простые в нашем случае. Вы просто возвращаете то, что прочитали здесь, и делаете это доступным для внешнего мира. Так, вы можете получить total_supply, admin_address, content, jetton_wallet_code, а получение адреса кошелька поможет выяснить, какой адрес кошелька кому соответствует.
          </p>
        </section>

        <section>
          <h2>Обработка Сообщений</h2>
          <p>
            Хорошо, давайте потратим еще несколько минут, чтобы понять, что находится внутри. Эта логика довольно проста. Мы не хотим обрабатывать сообщения без содержимого, поэтому мы возвращаем и выполняем:
          </p>
          <pre>
            {`recv_internal(int msg_value, cell in_msg_full, slice in_msg_body) impure {
    if (in_msg_body.slice_empty?()) { ;; ignore empty messages
        return ();
`}
          </pre>
          <p>
            Затем мы хотим определить флаги, чтобы игнорировать все отклоненные сообщения. Здесь мы остановим выполнение:
          </p>
          <pre>
            {`if (flags & 1) { ;; ignore all bounced messages
    return ();
}`}
          </pre>
          <p>
            Затем мы выясняем, кто является отправителем. В этот момент мы считываем все нужное из in_msg_full — это очень важно, чтобы понять, как работать дальше.
          </p>
          <pre>
            {`slice sender_address = cs~load_msg_addr();`}
          </pre>
        </section>

        <section>
          <h2>Метод mint и Логика Минтинга</h2>
          <p>
            Теперь давайте перейдем к методу mint. Здесь происходит проверка, является ли отправитель администратором, и если это так, запускается логика минтинга:
          </p>
          <pre>
            {`if (op == op::mint()) {
    throw_unless(73, equal_slices(sender_address, admin_address));
    slice to_address = in_msg_body~load_msg_addr();
    int amount = in_msg_body~load_coins();
    cell master_msg = in_msg_body~load_ref();
    slice master_msg_cs = master_msg.begin_parse();
    master_msg_cs~skip_bits(32 + 64); ;; op + query_id
    int jetton_amount = master_msg_cs~load_coins();
    mint_tokens(to_address, jetton_wallet_code, amount, master_msg);
    save_data(total_supply + jetton_amount, admin_address, content, jetton_wallet_code);
    return ();
}`}
          </pre>
          <p>
            Если mint был вызван не администратором, произойдет ошибка. Далее мы читаем содержимое сообщения, чтобы извлечь нужные данные.
          </p>
        </section>

        <section>
          <h2>Функция mint_tokens</h2>
          <p>
            Давайте рассмотрим функцию mint_tokens, которая отправляет токены на указанный адрес:
          </p>
          <pre>
            {`() mint_tokens(slice to_address, cell jetton_wallet_code, int amount, cell master_msg) impure {
    cell state_init = calculate_jetton_wallet_state_init(to_address, my_address(), jetton_wallet_code);
    slice to_wallet_address = calculate_jetton_wallet_address(state_init);
    var msg = begin_cell()
      .store_uint(0x18, 6)
      .store_slice(to_wallet_address)
      .store_coins(amount)
      .store_uint(4 + 2 + 1, 1 + 4 + 4 + 64 + 32 + 1 + 1 + 1)
      .store_ref(state_init)
      .store_ref(master_msg);
    send_raw_message(msg.end_cell(), 1);
}`}
          </pre>
          <p>
            Эта функция вычисляет адрес кошелька получателя и отправляет необходимые данные через raw-сообщение. Мы также обрабатываем transfer fees отдельно.
          </p>
        </section>

        <section>
          <h2>Уведомления о Сжигании (Burn Notifications)</h2>
          <p>
            Последний момент, который мы рассмотрим, — это обработка уведомлений о сжигании (burn notification). Здесь мы проверяем, откуда пришло уведомление и сжигаем нужное количество токенов:
          </p>
          <pre>
            {`if (op == op::burn_notification()) {
    int jetton_amount = in_msg_body~load_coins();
    slice from_address = in_msg_body~load_msg_addr();
    throw_unless(74,
        equal_slices(calculate_user_jetton_wallet_address(from_address, my_address(), jetton_wallet_code), sender_address)
    );
    save_data(total_supply - jetton_amount, admin_address, content, jetton_wallet_code);
    slice response_address = in_msg_body~load_msg_addr();
    if (response_address.preload_uint(2) != 0) {
      var msg = begin_cell()
        .store_uint(0x10, 6)
        .store_slice(response_address)
        .store_coins(0)
        .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
        .store_uint(op::excesses(), 32)
        .store_uint(query_id, 64);
      send_raw_message(msg.end_cell(), 2 + 64);
    }
    return ();
}`}
          </pre>
          <p>
            В этой части мы сжигаем Jetton, проверяя отправителя и обновляя данные в хранилище.
          </p>
        </section>

        <footer>
          <p>Это основные шаги для работы с контрактом Jetton. Продолжайте изучать и экспериментировать с кодом!</p>
        </footer>
      </div>
    </div>
  );
};


export const Lesson7_1 = () => {
  return (
    <div>
      <h1>💎 Добро пожаловать в седьмую часть!</h1>
      <p>
        В этой главе мы рассмотрим темы, связанные с продвинутыми реализациями контрактов. Погрузимся в тонкости Jetton и NFT, изучим их значение и возможные варианты использования. Кроме того, мы посвятим один урок разбору стандарта Jetton и его реализаций. Другой урок будет посвящен анализу возможностей и тонкостей смарт-контракта кошелька. Мы изучим смарт-контракт NFT, рассмотрим его структуру и особенности. Наконец, мы внимательно рассмотрим код, управляющий NFT. Цель этой главы — дать полное понимание этих продвинутых реализаций контрактов, что позволит вам эффективно использовать их возможности.
      </p>
      <div>
        {/* Replace with actual video */}
      </div>
      <h3>📚 Конспект урока</h3>
      <p>
        Добро пожаловать в новую главу. В этой главе мы поговорим о более сложных реализациях наших контрактов — Jetton и NFT. Как вы уже знаете, все в TON является контрактом. Кошелек — это контракт. Логика смарт-контракта — это контракт. Jetton и NFT также являются смарт-контрактами.
      </p>
      <p>
        У этих смарт-контрактов есть стандартные реализации. Они нужны, чтобы все понимали, как взаимодействовать с Jetton или NFT. Мы рассмотрим репозиторий, в котором хранятся все стандартные реализации, по которым сообщество уже договорилось, и все работают с Jetton и NFT определенным образом.
      </p>
      <p>
        Мы копнем еще глубже, и проанализируем этот код стандартной реализации. Стандарт — это просто определение требований к контракту и его поведению со стороны. Но есть и готовые стандартные реализации контрактов. Некоторые опубликованные контракты уже реализуют всю логику с учетом лучших практик стандарта. Таковые имеются как для Jetton, так и для NFT, и мы рассмотрим их код.
      </p>
      <p>
        Вы поймете общую логику. Для Jetton существует не один контракт, а несколько. У нас есть контракты майнера и кошелька. Их логику вы поймете в следующем уроке. То же самое касается и NFT. Есть контракты коллекций, контракты отдельных токенов. Вы узнаете больше о том, как они функционируют, взаимодействуют друг с другом, лучше освоите сам язык FunC, изучив эти стандартные реализации и увидев, как все может быть реализовано. В результате вы сможете использовать эти знания позже, когда будете писать свои собственные контракты. Я в восторге от того, что нас ждет, давайте перейдем к следующему уроку.
      </p>
    </div>
  );
};

export const Lesson6_4 = () => {
  return (
    <div>
      <h1>📚 Конспект урока</h1>

      <div>

        <video controls>
          <source src="/videos/6.4.mp4" type="video/mp4" />

        </video>
      </div>

      <p>
        Сегодня мы создадим еще один проект — чат-бот. Это будет бэкенд-проект, потому что чат-боты работают так. Вы пишете некий скрипт, который запускается на вашем сервере, и используете API-ключ для подключения к серверу Telegram. Ваш скрипт может получать обновления с сервера Telegram, когда приходят новые сообщения, и запускать ответы от имени вашего чат-бота.
      </p>

      <h3>Подготовка</h3>
      <p>Начнем проект с создания новой директории и перемещения в нее:</p>
      <pre>
        mkdir first_contract_tg_bot<br />
        cd first_contract_tg_bot
      </pre>

      <p>Теперь установим новый пакет:</p>
      <pre>yarn init</pre>

      <p>
        Для работы с TypeScript установим необходимые пакеты:
      </p>
      <pre>yarn add typescript ts-node @types/node @swc/core --dev</pre>

      <p>Создаем файл `tsconfig.json`:</p>
      <pre>
        {`{
					"compilerOptions": {
						"target": "es2020",
						"module": "commonjs",
						"esModuleInterop": true,
						"forceConsistentCasingInFileNames": true,
						"strict": true,
						"skipLibCheck": true,
						"resolveJsonModule": true
					},
					"ts-node": {
						"transpileOnly": true,
						"transpiler": "ts-node/transpilers/swc"
					}
				}`}
      </pre>

      <p>Устанавливаем Telegraf для создания чат-бота:</p>
      <pre>yarn add telegraf</pre>

      <h3>Программируем чат-бота</h3>
      <p>
        Создаем папку `src` и файл `index.ts`. Добавляем скрипт в `package.json` для запуска:
      </p>
      <pre>
        {`{
					"name": "first_contract_tg_bot",
					"version": "1.0.0",
					"main": "index.js",
					"license": "MIT",
					"dependencies": {
						"telegraf": "^4.12.2"
					},
					"scripts": {
						"start": "ts-node ./src/index.ts"
					}
				}`}
      </pre>

      <p>Импортируем `Telegraf` и создаем экземпляр бота:</p>
      <pre>
        {`import { Telegraf } from "telegraf";
				const bot = new Telegraf(process.env.TG_BOT_TOKEN!);`}
      </pre>

      <p>Используем dotenv для загрузки переменной среды:</p>
      <pre>
        import dotenv from "dotenv";
        dotenv.config();
      </pre>

      <p>
        Чтобы запустить бота, добавим пример кода с приветственным сообщением и клавиатурой:
      </p>
      <pre>
        {`bot.start((ctx) =>
			ctx.reply("Welcome to our counter app!", {
				reply_markup: {
				keyboard: [
					["Increment by 5"],
					["Deposit 1 TON"],
					["Withdraw 0.7 TON"],
				],
				},
			})
			);

			bot.launch();
			process.once("SIGINT", () => bot.stop("SIGINT"));
			process.once("SIGTERM", () => bot.stop("SIGTERM"));`}
      </pre>


      <p>Запускаем бота:</p>
      <pre>yarn start</pre>

      <p>Наш бот работает, вы можете проверить его, нажав Start в Telegram.</p>
    </div>
  );
};
export const Lesson6_3 = () => {
  return (
    <div>
      {/* Video Section */}
      <div>
        <video controls>
          <source src="/videos/6.3.mp4" type="video/mp4" />

        </video>
      </div>

      {/* Lesson Content */}
      <div>
        <h2>📚 Конспект урока</h2>

        <p>
          Посмотрим, как установить SDK, который позволит нам использовать
          API-интерфейсы TWA (Telegram Web App). Я открываю файл App.tsx в
          проекте, который мы сделали в предыдущей главе, и ввожу команду:
        </p>
        <code>yarn add @twa-dev/sdk</code>

        <p>Отлично, мы установили его. Теперь мы собираемся импортировать веб-приложение и начать взаимодействовать с ним.</p>

        <p>
          Во-первых, мы покажем платформу пользователю в нашем интерфейсе — это удобно, если вы хотите сделать разные функции в зависимости от платформы.
          Во-вторых, мы добавим кнопку для запуска какого-либо действия.
        </p>

        <p>
          Давайте начнем с платформы. Мы сделали текст полужирным и добавили
          платформу WebApp platform. Это будет отображаться на экране:
        </p>

        <div className="Card">
          {`{WebApp.platform}
					Our contract Address
					className="Hint">{contract_address?.slice(0, 30) + "..."}
					<b>Our contract Balance</b>
					{contract_balance && (
						<div className="Hint">{fromNano(contract_balance)}
					)}`}
        </div>

        <div className="Card">
          <b>Counter Value</b>
          <div>{counter_value ?? "Loading..."}</div>
        </div>

        <a onClick={showAlert}>Show Alert</a>

        <br />

        {connected && (
          <a onClick={sendIncrement}>Increment by 5</a>
        )}

        <br />

        {connected && (
          <a onClick={sendDeposit}>Request deposit of 1 TON</a>
        )}

        <br />

        {connected && (
          <a onClick={sendWithdrawalRequest}>Request 0.7 TON withdrawal</a>
        )}

        <p>
          Все очень просто. Теперь мы сохраним файлы и закоммитим их в GitHub:
        </p>
        <code>
          git status<br />
          git add .<br />
          git commit -m "TWA SDK integrated"<br />
          git push<br />
          git push origin master
        </code>

        <p>Развиваем GitHub Actions</p>

        <p>
          Как вы помните, у нас настроены GitHub Actions, чтобы наше приложение было создано и развернуто в GitHub Pages. Мы видим, что этот процесс уже запущен:
        </p>
        <p>TWA SDK integrated - action</p>

        <p>
          GitHub Actions — это мощная штука. Вы можете сэкономить много времени, делегируя туда разные задачи.
        </p>

        <p>
          Мы запустим ещё одно действие, которое опубликует нашу страницу на GitHub из отдельной ветки. Подождите, пока оно тоже завершится.
        </p>

        <p>
          Как видите, мы не можем определить платформу пользователя, ведь объект веб-приложения недоступен, поскольку мы находимся не внутри Telegram. Так что это всего лишь драфт публикации, который нужно расширить с помощью многочисленных проверок, чтобы понять, что ваш пользователь находится в Telegram.
        </p>

        <p>Теперь мы можем перейти к мобильному Telegram и посмотреть, как это работает внутри мессенджера.</p>

        <p>Моя платформа — iOS. Как я уже говорил, это всего лишь драфт публикации, и позднее мы сможем сделать ее намного лучше.</p>

        <p>Если я нажму Show alert, то появится всплывающее окно:</p>
        <p>Web app in Telegram</p>

        <p>
          TWA SDK дает вам много возможностей. Все, что вам нужно сделать, — это прочитать документацию, которая учит многим вещам. Например, как указать платформу, цветовые схемы, некоторые параметры темы, сделать ее расширенной, настроить высоту области просмотра.
        </p>

        <p>
          Еще одна интересная вещь — управление кнопками, главной кнопкой и кнопкой возврата. Вы можете использовать эти компоненты и прикреплять к ним действия.
        </p>

        <p>
          Вы получаете много возможностей, и ваше веб-приложение превращается в очень доступное приложение для Telegram. Вы можете вложить в него огромную функциональность и отличный пользовательский опыт.
        </p>

        <p>
          Если я закрою веб-приложение в Telegram, я увижу, что это всё еще чат. Поэтому, если вы хотите создать лучший пользовательский опыт с вашим приложением в целом, вам следует использовать гибридный подход: оставить часть функций в веб-приложении, а часть — в командах чата.
        </p>

        <p>
          В следующем уроке мы реализуем ту же самую функциональность с инкрементом «5», счетчиком, а также запросом на вывод и ввод средств.
        </p>

        <p>
          Дополним это комментариями в чате. Вы сами решаете, что использовать для своего продукта.
        </p>

        <p>
          В любом случае, мы поймем, как работает всё. Также мы узнаем, как передать информацию из веб-приложения в чат-бот Telegram, и создадим чат-бота, который будет управлять этим разговором.
        </p>

        <p>
          Веб-приложение отвечает за функциональность. Оно авторизует кошелек, ведет сессию. Но чат-бот, как отдельное приложение, будет поддерживать разговор с пользователем.
        </p>

        <p>
          Между ними нужно будет создать соединение, чтобы веб-приложение могло передавать информацию чат-боту. Так ваша беседа будет синхронизирована с тем, что происходит в веб-приложении.
        </p>

        <p>Я очень горжусь вашим прогрессом. Увидимся в следующем уроке!</p>
      </div>
    </div>
  );
};

export const Lesson6_2 = () => {
  return (
    <div>
      <video controls>
        <source src="/videos/6.2.mp4" type="video/mp4" />

      </video>
      <div>

      </div>

      {/* Lesson Content */}
      <div>
        <h2>📚 Конспект урока</h2>

        <p>Как создать бота в Telegram? Это очень просто. Вам просто нужно написать в телеграме в специальный аккаунт под названием BotFather. Сегодня мы создадим бота, выберем для него имя и интегрируем наше приложение в кнопку меню этого бота.</p>

        <p>В диалоге с BotFather мы нажимаем Start и видим несколько вариантов. Переходим в меню и нажимаем Create a new bot. Далее нас спросят, как мы хотим назвать нашего бота, пусть это будет TON&CO Tutorial. Теперь нам нужно выбрать имя. Обычные имена всегда заканчиваются на bot. Если мы попробуем tonandcotutorial в качестве имени, от нас потребуют имя с постфиксом bot, поэтому я назову его tonandcotutorialbot. Вы можете придумать и лучшее имя для своего проекта, но для тестирования подойдет это.</p>

        <p>Я получил ключ HTTP API. Он понадобится нам позже, не в этом и не в следующем уроке. Мы будем использовать его для создания интерфейса команд чат-бота.</p>

        <p>Теперь заходим в меню, нажимаем Edit your bots, выбираем нашего бота и переходим в Bot settings. В настройках бота выбираем Menu button. Затем нажимаем Configure menu button. Нас попросят прислать URL, который будет открываться при нажатии на кнопку меню нашего бота. Это именно та ссылка, которую мы получили в предыдущем уроке после публикации приложения на GitHub Pages:</p>

        <p>
          <a href="https://markokhman.github.io/first_contract_front_end/" target="_blank" rel="noopener noreferrer">
            https://markokhman.github.io/first_contract_front_end/
          </a>
        </p>

        <p>Далее он запрашивает название. Пусть будет просто Counter (счетчик). Таким образом, пользователь может нажать на Counter, и мое приложение откроется. Затем мы можем вернуться к боту.</p>

        <p>Еще один момент: в настройках бота вы можете редактировать название, текст about, описание и даже картинку. Редактирование команд позволит вам иметь более одной кнопки в меню. У вас будет достаточно времени, чтобы поиграться с ботом, а сейчас мы сосредоточимся на том, как будет работать наша кнопка меню.</p>

        <p>Я открываю нашего бота и нажимаю кнопку Start. Здесь я вижу кнопку Counter, и если я нажму на нее, она спросит меня, согласен ли я перейти на сайт. Я нажимаю OK, и бот открывает наше приложение. Там я могу настроить высоту — это можно сделать с помощью SDK, о котором я вам рассказывал.</p>

        <p>В нашем приложении мы нажимаем Connect Wallet. Я выбираю Tonkeeper и получаю запрос на подключение кошелька. Теперь, когда кошелек подключен, мое приложение также авторизовано. Я закрываю счетчик, а когда открываю его снова, он все равно сохраняет сессию, потому что это веб-приложение.</p>

        <p>Но это не простое веб-приложение: оно дает нам множество возможностей для взаимодействия с нашим ботом. Интерфейс бота может быть очень разнообразным и открывать нам совершенно новый уровень работы. Поэтому в следующем уроке мы узнаем больше о SDK, о том, как заставить его работать еще эффективнее и как взаимодействовать с самим Telegram.</p>

        <p>А позже мы также узнаем, как создавать классные интерфейсы чата, работающие с веб-приложением, которое мы только что создали. Следующие два урока будут очень интересными.</p>
      </div>
    </div>
  );
};


export const Lesson6_1 = () => {
  return (
    <div>
      {/* Video Section */}



      {/* Lesson Content */}
      <div>
        <h2>💎 Добро пожаловать в шестую часть!</h2>
        <p>
          В этой главе мы рассмотрим различные аспекты дистрибуции приложений,
          упаковки TWA (Telegram Web App), использования SDK и создания чат-ботов.
          Эти темы будут подробно изложены на протяжении всей главы, чтобы дать
          вам полное представление о новом подходе к дистрибуции приложений.
        </p>

        <h3>📚 Конспект урока</h3>
        <p>
          В этой главе мы подробно рассмотрим новый способ дистрибуции нашего
          приложения. Мы опубликовали его на GitHub Pages, и теперь оно доступно
          для всех желающих в Интернете через браузер. Однако, чтобы ваше
          приложение стало удобнее, нужно, чтобы оно было доступно в привычной
          оболочке.
        </p>

        <p>
          Мессенджер — один из лучших способов сделать это. Допустим, один из
          ваших пользователей делится вашим приложением со своим другом или
          группой людей. Самый простой способ привлечь их — это не переводить
          их в браузер, а дать им возможность пользоваться вашим приложением
          нативно. Telegram — это удивительный мессенджер с беспрецедентными
          возможностями и амбициями. Он действительно приносит пользу. Telegram
          насчитывает до 700 миллионов активных пользователей ежемесячно и более
          50 миллионов — ежедневно. Огромная потенциальная база для вашего
          приложения!
        </p>

        <p>
          Боты Telegram уже давно являются отличным способом для программирования
          работы с пользователями Telegram. Telegram-боты могут полностью
          заменить любой веб-сайт. Они поддерживают бесшовную авторизацию,
          интегрированные платежи через 15 платежных провайдеров: Google Pay и
          Apple Pay прямо из коробки. Они доставляют пользователям персонализированные
          пуш-уведомления и многое другое.
        </p>

        <p>
          С веб-приложениями — функцией, появившейся недавно, — боты выходят в
          совершенно новое измерение. Разработчики ботов могут создавать
          бесконечно гибкие интерфейсы с помощью JavaScript, одного из самых
          популярных языков программирования в мире. Интерфейс чата получил
          широкое распространение, хотя все мы привыкли к графическому
          интерфейсу для большинства задач.
        </p>

        <p>
          Мы хотим, чтобы наше приложение было бесшовно доступно для многих
          пользователей, чтобы они могли легко поделиться этим опытом со своими
          близкими. Мы собираемся сделать следующий шаг и настроить наше
          приложение как веб-приложение в Telegram.
        </p>

        <h3>Погружаемся в веб-приложения Telegram</h3>
        <p>
          Посмотрим как выглядят веб-приложения Telegram. На главной странице
          Telegram мы переходим к API и видим Bot API. Здесь открывается новая
          функция — веб-приложения Telegram. Они работают внутри бота, но дают
          возможность создать встроенный веб-интерфейс.
        </p>

        <p>
          Его можно использовать для многих целей: приема платежей, пользовательских
          сценариев, обработки файлов и выполнения определенных задач. Вы также
          можете интегрировать его с другими сервисами, например с GMail, как показано
          в примере. А еще вы можете создавать игры.
        </p>

        <p>
          Веб-приложения Telegram выходят за рамки базового взаимодействия
          «запрос-ответ». Именно поэтому их очень важно освоить — это даст вам
          множество преимуществ в любых проектах, которые вы захотите запустить.
        </p>

        <p>
          Давайте посмотрим, как они интегрированы. Переходим в раздел
          веб-приложений. Мы уже создали свое веб-приложение, теперь нам нужно
          интегрировать его в этого бота. Тогда мы сможем менять цветовую схему
          и язык в зависимости от настроек устройства пользователя.
        </p>

        <h3>Telegram web app launch options</h3>
        <p>
          Интеграция здесь довольно простая. Веб-приложение Telegram можно запустить
          в боте с помощью кнопки на клавиатуре, кнопки в строке или кнопки меню
          бота. В следующих уроках мы будем использовать кнопку меню бота. Раздел
          веб-приложений — это очень богатый ресурс, позволяющий на практике
          изучить возможности веб-приложений Telegram.
        </p>

        <p>
          Мы уже создали веб-приложение, доступное в браузере. Нам нужно узнать,
          как интегрировать его с API Telegram. Telegram предоставляет нам скрипт,
          который мы можем просто загрузить и получить доступ к многочисленным
          функциям, таким как цветовая схема, платформа, определение размера экрана
          и так далее.
        </p>

        <p>
          Теперь нам нужно интегрировать наше приложение с помощью скрипта.
          Давайте поищем SDK, который поможет нам сделать это на React. К счастью,
          он у нас есть — ссылка будет доступна и в дополнительных материалах
          к уроку. Просто установите эту библиотеку:
        </p>

        <pre>
          <code>npm i @twa-dev/sdk</code>
        </pre>

        <p>
          Она позволит нам легко получить доступ к методам скрипта. В следующем
          уроке мы создадим нашего бота, научимся интегрировать то, что у нас
          уже есть, а затем улучшим наше приложение, чтобы оно более эффективно
          взаимодействовало с API бота.
        </p>
      </div>
    </div>
  );
};




export const Lesson4_1 = () => {
  return (
    <div>
      <h1>💎 Добро пожаловать в четвертую часть!</h1>
      <video controls>
        <source src="/videos/4.1.mp4" type="video/mp4" />

      </video>

      <p>
        В этой главе мы рассмотрим различные аспекты разработки и внедрения смарт-контрактов. Начнем с добавления более сложной логики для расширения функциональности нашего смарт-контракта. Далее сосредоточимся на том, чтобы наши тесты обновлялись и улучшались в соответствии с последними изменениями. Мы займемся реализацией продвинутой логики для вывода и ввода средств. Кроме того, мы рассмотрим процесс инициализации нашего существующего контракта, включив в него обширное тестовое покрытие. Мы пройдем через развертывание контракта в мейннете с помощью Blueprint. Наконец, мы вооружим вас необходимыми знаниями для эффективной проверки кода контракта. Эти уроки дадут вам полное представление о разработке смарт-контрактов и навыки для успешного развертывания и проверки.
      </p>

      <h2>here must be video</h2>

      <h3>📚 Конспект урока</h3>
      <p>
        Добро пожаловать в четвертую главу! Здесь мы будем создавать все более сложную логику в нашем смарт-контракте, разбираться в ней и писать локальные тесты с помощью TypeScript.
      </p>

      <p>Итоговый код главы 4 доступен в этом репозитории.</p>

      <p>
        Вернемся к коду контракта, который мы написали в предыдущей главе. Продолжим писать поверх этого контракта.
      </p>

      <pre>
        <code>
          {`#include "imports/stdlib.fc";

() recv_internal(int msg_value, cell in_msg, slice in_msg_body) impure {
    slice cs = in_msg.begin_parse();
    int flags = cs~load_uint(4);
    slice sender_address = cs~load_msg_addr();

    set_data(begin_cell().store_slice(sender_address).end_cell());
}

slice get_the_latest_sender() method_id {
   slice ds = get_data().begin_parse();
   return ds~load_msg_addr();
}`}
        </code>
      </pre>

      <p>
        Расскажу подробнее о нашем плане на этот урок. Мы собираемся переписать наш контракт так, чтобы он имел следующую логику:
      </p>
      <ul>
        <li>Контракт будет получать сообщение с определенной командой (команды используются для определения того, какие функции нашего контракта должны быть выполнены с данными, полученным в сообщении).</li>
        <li>Основываясь на этой конкретной команде, контракт увеличивает число, которое хранится в нашем хранилище c4.</li>
        <li>Контракт предоставляет getter-метод, который возвращает текущее значение счетчика и адрес, по которому было отправлено сообщение с кодом операции инкремента.</li>
      </ul>

      <p>
        Мы уже знакомы с концепциями записи в хранилище данных и getter-методами, так что это не будет проблемой, а скорее хорошей практикой для наших навыков.
      </p>

      <p>
        Однако понятие команд для нас является новым. Команды также называют кодами операций, или сокращенно — опкодами. Опкоды обычно хранятся в самом начале, в slice in_msg_body, который передается в функцию recv_internal. Опкод — это просто целое число, которое мы используем для обозначения определенного логического блока. Это стандартная практика, но сами по себе опкоды там не появляются — их нужно передавать при составлении сообщения перед его отправкой.
      </p>

      <p>
        Важно заметить, что когда мы считываем данные, мы должны делать это в том же порядке, в каком мы их туда записали. Чтобы убедиться, что get_data() работает правильно при первом запуске, мы вернемся к концепции исходных данных, то есть будем хранить определенное значение в хранилище c4 с самого момента развертывания контракта.
      </p>

      <h4>Опкод</h4>
      <p>
        Как я уже говорил, мы собираемся поместить опкод в самое начало in_msg_body, поэтому давайте напишем код, который будет считывать его оттуда:
      </p>

      <pre>
        <code>
          {`#include "imports/stdlib.fc";

() recv_internal(int msg_value, cell in_msg, slice in_msg_body) impure {
    slice cs = in_msg.begin_parse();
    int flags = cs~load_uint(4);
    slice sender_address = cs~load_msg_addr();
    
    int op = in_msg_body~load_uint(32);

    set_data(begin_cell().store_slice(sender_address).end_cell());
}

slice get_the_latest_sender() method_id {
   slice ds = get_data().begin_parse();
   return ds~load_msg_addr();
}`}
        </code>
      </pre>

      <p>
        В FunC у некоторых переменных необходимо указать количество бит, выделяемых для их хранения в памяти. Поэтому в методе ~load_uint(32) нам нужно установить значение 32 — это означает, что данное целое число может иметь максимальный размер в 32 бита.
      </p>

      <p>
        Операционный код, который мы будем использовать для увеличения значения счетчика, будет равен 1. Да, вот так просто. Давайте обернем нашу логику в оператор if, который проверит, что операционный код равен 1:
      </p>

      <pre>
        <code>
          {`() recv_internal(int msg_value, cell in_msg, slice in_msg_body) impure {
    slice cs = in_msg.begin_parse();
    int flags = cs~load_uint(4);
    slice sender_address = cs~load_msg_addr();
    
    int op = in_msg_body~load_uint(32);
    
    if (op == 1) {
        ;; counter logic is coming here                                
        set_data(begin_cell().store_slice(sender_address).end_cell());
    }
}

slice get_the_latest_sener() method_id {
   slice ds = get_data().begin_parse();
   return ds~load_msg_addr();
}`}
        </code>
      </pre>

      <p>В FunC мы используем двойную точку с запятой (;;), чтобы начать строку комментария</p>

      <h4>Логика счетчика</h4>
      <p>
        Наша структура локального хранилища будет выглядеть следующим образом:
      </p>
      <ul>
        <li>counter_value — целое число;</li>
        <li>sender_address — это slice, адрес кошелька, который последний раз отправлял команду инкремента.</li>
      </ul>

      <p>
        В функции recv_internal мы предполагаем, что в локальном хранилище уже хранятся эти данные, поэтому мы просто считываем их. Нам не нужно считывать предыдущего последнего отправителя, потому что когда мы будем сохранять значения обратно в хранилище, мы уже будем использовать новое значение для этого — sender_address текущего сообщения.
      </p>

      <pre>
        <code>
          {`slice ds = get_data().begin_parse();
int counter_value = ds~load_uint(32);                    
set_data(
  begin_cell().store_uint(counter_value + 1, 32).store_slice(sender_address).end_cell()
);`}
        </code>
      </pre>

      <p>
        Но теперь нам также нужно обновить getter-метод, чтобы он считывал данные из хранилища в правильном порядке. Мы переименуем его в get_contract_storage_data, поскольку теперь он также передает значение счетчика и адрес последнего отправителя:
      </p>

      <pre>
        <code>
          {`(int, slice) get_contract_storage_data() method_id {
  slice ds = get_data().begin_parse();
  return (
    ds~load_uint(32), ;; counter_value
    ds~load_msg_addr() ;; the most recent sender
  );
}`}
        </code>
      </pre>

      <h4>Финальный код</h4>
      <pre>
        <code>
          {`#include "imports/stdlib.fc";

() recv_internal(int msg_value, cell in_msg, slice in_msg_body) impure {
  slice cs = in_msg.begin_parse();
  int flags = cs~load_uint(4);
  slice sender_address = cs~load_msg_addr();

  int op = in_msg_body~load_uint(32);
    
  if (op == 1) {
    slice ds = get_data().begin_parse();
    int counter_value = ds~load_uint(32);                    
    set_data(
      begin_cell().store_uint(counter_value + 1, 32).store_slice(sender_address).end_cell()
    );
    return ();
  }

  return ();
}

(int, slice) get_contract_storage_data() method_id {
  slice ds = get_data().begin_parse();
  return (
    ds~load_uint(32), ;; counter_value
    ds~load_msg_addr() ;; the most recent sender
  );
}`}
        </code>
      </pre>

      <p>Вы можете проверить, правильно ли работает наш контракт, через yarn compile.</p>

      <p>В следующем уроке мы обновим наши тесты для смарт-контракта, чтобы они помогали нам проверять его поведение.</p>
    </div>
  );
};


export const Lesson4_2 = () => {
  return (
    <div>

      <video controls>
        <source src="/videos/4.2.mp4" type="video/mp4" />

      </video>

      <h1>📚 Конспект урока</h1>
      <p>Как я упоминал ранее, написание тестов будет важной частью в программировании контрактов на FunC. Посмотрим, какие тесты мы создали в предыдущей главе:</p>

      <pre>
        <code>
          {`import { Cell, toNano } from "ton-core";
import { hex } from "../build/main.compiled.json";
import { Blockchain } from "@ton-community/sandbox";
import { MainContract } from "../wrappers/MainContract";
import "@ton-community/test-utils";

describe("main.fc contract tests", () => {
  it("should get the proper most recent sender address", async () => {
    const blockchain = await Blockchain.create();
    const codeCell = Cell.fromBoc(Buffer.from(hex, "hex"))[0];

    const myContract = blockchain.openContract(
      await MainContract.createFromConfig({}, codeCell)
    );

    const senderWallet = await blockchain.treasury("sender");

    const sentMessageResult = await myContract.sendInternalMessage(
      senderWallet.getSender(),
      toNano("0.05")
    );

    expect(sentMessageResult.transactions).toHaveTransaction({
      from: senderWallet.address,
      to: myContract.address,
      success: true,
    });

    const data = await myContract.getData();

    expect(data.recent_sender.toString()).toBe(senderWallet.address.toString());
  });
});`}
        </code>
      </pre>

      <p>Стоит отметить, что если мы запустим yarn test сейчас, то он не сработает, так как наш код сильно изменился. Чтобы исправить это, нам нужно сделать следующее:</p>
      <ul>
        <li>Предоставить верные данные об исходном состоянии, чтобы в нашем хранилище c4 были предварительно заполнены некоторые данные. Мы предоставим 0 в качестве значения счетчика и zeroAddress в качестве адреса отправителя (в соответствии с новой логикой нашего контракта).</li>
        <li>В теле сообщения, которое мы отправляем контракту, нужно передать определенный опкод.</li>
      </ul>

      <p>Разберемся с исходными данными. Как вы помните, мы используем метод createFromConfig для инициализации нашего контракта и задаем исходные данные состояния нашего контракта в виде простой пустой ячейки следующим образом:</p>

      <pre>
        <code>
          {`static createFromConfig(config: any, code: Cell, workchain = 0) {
    const data = beginCell().endCell();
    const init = { code, data };
    const address = contractAddress(workchain, init);

    return new MainContract(address, init);
}`}
        </code>
      </pre>

      <p>Затем createFromConfig вызывается в main.spec.ts:</p>

      <pre>
        <code>
          {`const myContract = blockchain.openContract(
  await MainContract.createFromConfig({}, codeCell)
);`}
        </code>
      </pre>

      <p>До сих пор мы передавали пустой объект для конфигурации. Теперь разрешим передавать значения, которые понадобятся для инициализации данных нашего контракта.</p>

      <p>Теперь определим, как должна выглядеть наша конфигурация. Сразу после imports в нашем wrappers/MainContract.ts мы определим новый тип MainContractConfig.</p>

      <pre>
        <code>
          {`export type MainContractConfig = {
  number: number;
  address: Address;
};`}
        </code>
      </pre>

      <p>Еще одной классной вспомогательной функцией, которую мы реализуем в том же файле-обертке, будет mainContractConfigToCell. Она будет получать объект типа MainContractConfig, правильно упаковывать его параметры в ячейку и возвращать ее. Нам это нужно, поскольку хранилище c4 — это ячейка памяти:</p>

      <pre>
        <code>
          {`export function mainContractConfigToCell(config: MainContractConfig): Cell {
  return beginCell().storeUint(config.number, 32).storeAddress(config.address).endCell();
}`}
        </code>
      </pre>

      <p>Обновим createFromConfig в том же файле, но внутри класса MainContract. Теперь он должен использовать функцию mainContractConfigToCell для формирования ячейки данных с начальным состоянием:</p>

      <pre>
        <code>
          {`export class MainContract implements Contract {
  constructor(
    readonly address: Address,
    readonly init?: { code: Cell; data: Cell }
  ) {}

  static createFromConfig(
    config: MainContractConfig,
    code: Cell,
    workchain = 0
  ) {
    const data = mainContractConfigToCell(config);
    const init = { code, data };
    const address = contractAddress(workchain, init);

    return new MainContract(address, init);
  }
}`}
        </code>
      </pre>

      <p>Теперь, когда наш метод createFromConfig готов, обновим сценарий его использования в файле tests/main.spec.ts:</p>

      <pre>
        <code>
          {`describe("main.fc contract tests", () => {
  it("should get the proper most recent sender address", async () => {
    const blockchain = await Blockchain.create();
    const codeCell = Cell.fromBoc(Buffer.from(hex, "hex"))[0];

    const initAddress = await blockchain.treasury("initAddress");

    const myContract = blockchain.openContract(
      await MainContract.createFromConfig(
        {
          number: 0,
          address: initAddress.address,
        },
        codeCell
      )
    );
  });
});`}
        </code>
      </pre>

      <p>На данный момент мы успешно инициализировали наш контракт! Поздравляем :)</p>

      <p>Теперь мы собираемся отправлять определенные данные в теле сообщения, обновим метод sendInternalMessage в нашем wrappers/MainContract.ts. Мы переименуем его в sendIncrement, и теперь он будет принимать еще одно значение — increment_by. Это будет число, на которое будет увеличено значение хранилища текущего контракта.</p>

      <pre>
        <code>
          {`async sendIncrement(
    provider: ContractProvider,
    sender: Sender,
    value: bigint,
    increment_by: number
  ) {
    const msg_body = beginCell()
      .storeUint(1, 32) // OP code
      .storeUint(increment_by, 32) // increment_by value
      .endCell();

    await provider.internal(sender, {
      value,
      sendMode: SendMode.PAY_GAS_SEPARATELY,
      body: msg_body,
    });
  }`}
        </code>
      </pre>

      <p>Еще один метод, который нужно обновить в нашей обертке, — getData. Как вы помните, мы переименовали getter-метод, и он теперь возвращает два значения. Давайте подкорректируем наш метод-обертку:</p>

      <pre>
        <code>
          {`async getData(provider: ContractProvider) {
    const { stack } = await provider.get("get_contract_storage_data", []);
    return {
      number: stack.readNumber(),
      recent_sender: stack.readAddress(),
    };
  }`}
        </code>
      </pre>

      <p>Теперь мы готовы вернуться к нашему tests/main.spec.ts и приступить к завершению теста. Вот его обновленный код:</p>

      <pre>
        <code>
          {`describe("main.fc contract tests", () => {
  it("should successfully increase counter in contract and get the proper most recent sender address", async () => {
    const blockchain = await Blockchain.create();
    const codeCell = Cell.fromBoc(Buffer.from(hex, "hex"))[0];

    const initAddress = await blockchain.treasury("initAddress");

    const myContract = blockchain.openContract(
      await MainContract.createFromConfig(
        {
          number: 0,
          address: initAddress.address,
        },
        codeCell
      )
    );

    const senderWallet = await blockchain.treasury("sender");

    const sentMessageResult = await myContract.sendIncrement(
      senderWallet.getSender(),
      toNano("0.05"),
      1
    );

    expect(sentMessageResult.transactions).toHaveTransaction({
      from: senderWallet.address,
      to: myContract.address,
      success: true,
    });

    const data = await myContract.getData();

    expect(data.recent_sender.toString()).toBe(senderWallet.address.toString());
    expect(data.number).toEqual(1);
  });
});`}
        </code>
      </pre>

      <p>Что мы здесь изменили?</p>
      <ul>
        <li>Теперь мы передаем в метод sendIncrement число 1, так что наше результирующее значение должно быть увеличено на 1.</li>
        <li>Мы задали ожидания для значений recent_sender и number.</li>
      </ul>

      <p>Теперь мы можем запустить наш тест с помощью команды yarn test. Если вы все сделали правильно, то в терминале увидите следующее:</p>

      <pre>
        <code>
          {`PASS  tests/main.spec.ts
main.fc contract tests
  ✓ should successfully increase counter in contract and get the proper most recent sender address (452 ms)

Test Suites: 1 passed, 1 total
Tests:       1 passed, 1 total
Snapshots:   0 total
Time:        4.339 s, estimated 5 s`}
        </code>
      </pre>

      <p>Ура, наш контракт со счетчиком протестирован как надо. С нетерпением жду новой логики в следующих уроках!</p>
    </div>
  );
};


export const Lesson4_3 = () => {
  return (
    <div>
      <video controls>
        <source src="/videos/4.3.mp4" type="video/mp4" />

      </video>
      <h1>📚 Конспект урока</h1>
      <p>На этом уроке мы будем практиковаться в реализации различных команд, создании функций и даже отправке сообщений из контракта. Все это мы будем делать поверх нашего текущего контракта</p>

      <h2>Рассмотрим план занятия.</h2>
      <ul>
        <li>Наш контракт станет более строгим к командам, так как мы введем новый опкод для логики внесения средств в контракт;</li>
        <li>Еще один опкод, который мы добавим, — это вывод средств. В процессе вывода средства фактически отправляются на адрес в виде сообщения, поэтому мы научимся отправлять сообщения из контракта.</li>
        <li>Все средства, которые придут с неизвестными кодами операций, будут возвращены отправителю.</li>
        <li>Мы введем в наше хранилище новое значение — владелец контракта. Только владелец контракта сможет выводить средства. Мы также разделим загрузку хранилища и ввод логики функций, чтобы сохранить наш основной код чистым.</li>
      </ul>

      <h3>На старт, внимание, марш!</h3>
      <p>Разделение логики управления хранилищем</p>

      <p>Прежде всего разберемся с данными владельца контракта в хранилище (не забывайте, что нам придется добавлять этот адрес в хранилище при инициировании контракта). Для этого мы создадим две новые функции — load_data и save_data:</p>

      <pre>
        <code>
          {`(int, slice, slice) load_data() inline {
  var ds = get_data().begin_parse();
  return (
    ds~load_uint(32), ;; counter_value
    ds~load_msg_addr(), ;; the most recent sender
    ds~load_msg_addr() ;; owner_address
  );
}

() save_data(int counter_value, slice recent_sender, slice owner_address) impure inline {
  set_data(begin_cell()
    .store_uint(counter_value, 32) ;; counter_value
    .store_slice(recent_sender) ;; the most recent sender
    .store_slice(owner_address) ;; owner_address
    .end_cell());
}`}
        </code>
      </pre>

      <p>Здесь нужно обратить внимание на две вещи:</p>
      <ul>
        <li>Строчный cпецификатор. Вы уже немного знаете о спецификаторах. Если функция имеет строчный спецификатор, то ее код фактически подставляется во все места, где вызывается функция. Использовать рекурсивные вызовы в строчных функциях запрещено.</li>
        <li>Почему в load_data() нет спецификатора impure функции? Мы уже говорили об этом в третьей главе, но давайте напомним. Причина в том, что эта функция не влияет на состояние контракта. Она предназначена только для чтения.</li>
      </ul>

      <h3>Новые опкоды</h3>
      <p>Давайте посмотрим, как наши функции управления хранением размещены в коде, а также введем дополнительные коды операций и настроим выдачу ошибку, если код операции неизвестен нашему контракту:</p>

      <pre>
        <code>
          {`#include "imports/stdlib.fc";

(int, slice, slice) load_data() inline {
  var ds = get_data().begin_parse();
  return (
    ds~load_uint(32), ;; counter_value
    ds~load_msg_addr(), ;; the most recent sender
    ds~load_msg_addr() ;; owner_address
  );
}

() save_data(int counter_value, slice recent_sender, slice owner_address) impure inline {
  set_data(begin_cell()
    .store_uint(counter_value, 32) ;; counter_value
    .store_slice(recent_sender) ;; the most recent sender
    .store_slice(owner_address) ;; owner_address
    .end_cell());
}

() recv_internal(int msg_value, cell in_msg, slice in_msg_body) impure {
  slice cs = in_msg.begin_parse();
  int flags = cs~load_uint(4);
  slice sender_address = cs~load_msg_addr();

  int op = in_msg_body~load_uint(32);

  var (counter_value, recent_sender, owner_address) = load_data();
    
  if (op == 1) {
    save_data(counter_value + 1, sender_address, owner_address);
    return();
  }

  if (op == 2) {
    ;; deposit
  }

  if (op == 3) {
    ;; withdrawal
  }

  throw(777);
}

(int, slice, slice) get_contract_storage_data() method_id {
  var (counter_value, recent_sender, owner_address) = load_data();
  return (
    counter_value,
    recent_sender,
    owner_address
  );
}`}
        </code>
      </pre>

      <p>Несколько моментов, на которые стоит обратить внимание:</p>
      <ul>
        <li>Мы также обновили функцию get_the_latest_sender, чтобы она возвращала адрес владельца.</li>
        <li>Если считанный из тела сообщения опкод не сработал ни в одном из операторов if, мы выбрасываем ошибку с кодом 777. Это число вы можете выбрать сами, но убедитесь, что оно не пересекается с официальными кодами ошибки. exit_code больше 1 считается кодом ошибки, поэтому завершение с таким кодом может привести к откату/отказу транзакции.</li>
        <li>Мы используем функцию return() для успешного завершения выполнения контракта.</li>
      </ul>

      <h3>Ввод и вывод средств</h3>
      <p>При пополнении счета (op == 2) все очень просто, так как в этом случае мы просто успешно завершаем исполнение, поэтому средства принимаются. В противном случае средства будут возвращены отправителю.</p>

      <pre>
        <code>
          {`if (op == 2) {
    return();
}`}
        </code>
      </pre>

      <p>Однако с выводом средств все становится немного сложнее. Нам нужно сравнить адрес отправителя с адресом владельца смарт-контракта. Давайте посмотрим, что нам нужно знать, чтобы реализовать эту логику.</p>
      <ul>
        <li>Для сравнения адресов владельца и отправителя мы используем стандартную функцию FunC equal_slice_bits().</li>
        <li>Мы используем функцию throw_unless(), чтобы выдать ошибку, если в результате сравнения получили false. Существует также другой способ выдать ошибку - throw_if(). Он выдает ошибку, если условие, переданное в эту функцию, возвращает true.</li>
        <li>В теле сообщения с этой операцией должно быть целое число, указывающее на сумму, запрашиваемую для снятия. Мы сравниваем эту сумму с фактическим балансом контракта (стандартная функция FunC get_balance()).</li>
        <li>Нам нужно установить некоторый минимум, который должен оставаться на контракте, и выдать ошибку, если запрашиваемая сумма не позволяет этого сделать.</li>
      </ul>

      <p>Посмотрим, как это работает:</p>

      <pre>
        <code>
          {`const const::min_tons_for_storage = 10000000; ;; 0.01 TON

if (op == 3) {
    throw_unless(103, equal_slice_bits(sender_address, owner_address));

    int withdraw_amount = in_msg_body~load_coins();
    var [balance, _] = get_balance();
    throw_unless(104, balance >= withdraw_amount);

    int return_value = min(withdraw_amount, balance - const::min_tons_for_storage);
    
    ;; TODO: Sending internal message with funds
    
    return();
}`}
        </code>
      </pre>

      <h3>Отправка внутренних сообщений</h3>
      <p>send_raw_message — это стандартная функция, которая принимает ячейку с сообщением и целое число, содержащее сумму mode и flag. В настоящее время существует 3 режима и 3 флага для сообщений.</p>

      <pre>
        <code>
          {`var msg = begin_cell()
    .store_uint(0x18, 6)
    .store_slice(addr)
    .store_coins(grams)
    .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
    .store_uint(op_code, 32)
    .store_uint(query_id, 64);
    
send_raw_message(msg.end_cell(), mode);`}
        </code>
      </pre>

      <p>Посмотрим, что здесь происходит:</p>
      <ul>
        <li>Мы начинаем составлять ячейку, преобразуя значение 0x18 в 6 бит. Это означает определенные флаги и режимы для сообщения.</li>
        <li>Мы сериализуем адрес назначения, количество монет и другие параметры сообщения.</li>
      </ul>

      <h3>Финализируем код контракта</h3>
      <p>Напоследок добавим обработку операций с выводом средств и сообщения для успешных операций. Также важно отметить, что контракты могут возвращать средства только владельцам, поэтому обязательно проверяйте, кто их запрашивает!</p>
    </div>
  );
};

export const Lesson4_4 = () => {
  return (
    <div>
      <h2>📚 Конспект урока</h2>
      <p>На этом уроке мы напишем довольно много интересных тестов.</p>

      <p>
        Сначала мы обновим процесс инициализации для ранее написанного контракта, потому что мы ввели новую переменную, которая должна храниться в хранилище c4.
      </p>

      <p>
        Если вы попробуете запустить наш тест сейчас через <code>yarn test</code>, то увидите, что тест провалится. Вы также увидите следы транзакций, произошедших с нашим контрактом. Одна из них будет иметь код ошибки 9. Посмотрите в документации, что это значит.
      </p>

      <pre>9 - Незаполнение ячейки. Примитив чтения из slice пытался прочитать больше бит или ссылок, чем есть на самом деле.</pre>

      <p>Обновляем обертку контракта:</p>
      <pre>
        // ...library imports
        export type MainContractConfig = {'{'}<br />
        number: number;<br />
        address: Address;<br />
        owner_address: Address;<br />
        {'}'}<br />

        export function mainContractConfigToCell(config: MainContractConfig): Cell {'{'}<br />
        return beginCell()<br />
        .storeUint(config.number, 32)<br />
        .storeAddress(config.address)<br />
        .storeAddress(config.owner_address)<br />
        .endCell();<br />
        {'}'}<br />
        // ...contact wrapper class
      </pre>

      <p>
        Еще одна вещь, которую нужно обновить в файле обертки — метод getData, чтобы он возвращал адрес владельца. Мы также создадим новый метод getBalance, чтобы проверить, что баланс был увеличен после пополнения.
      </p>

      <pre>
        async getData(provider: ContractProvider) {'{'}<br />
        const {'{'} stack {'}'} = await provider.get("get_contract_storage_data", []);<br />
        return {'{'}<br />
        number: stack.readNumber(),<br />
        recent_sender: stack.readAddress(),<br />
        owner_address: stack.readAddress(),<br />
        {'}'};<br />
        {'}'}<br />

        async getBalance(provider: ContractProvider) {'{'}<br />
        const {'{'} stack {'}'} = await provider.get("balance", []);<br />
        return {'{'}<br />
        number: stack.readNumber(),<br />
        {'}'};<br />
        {'}'}<br />
      </pre>

      <p>
        Введем новое хранилище средств, которое будет фактически являться владельцем контракта. Только этот владелец сможет в дальнейшем выводить средства. Обновляем <code>tests/main.spec.ts</code>:
      </p>

      <pre>
        {`// ...library imports
        describe("main.fc contract tests", () => {'{'}<br />
        it("should get the proper most recent sender address", async () => {'{'}<br />
        const blockchain = await Blockchain.create();<br />
        const codeCell = Cell.fromBoc(Buffer.from(hex, "hex"))[0];<br />

        const initAddress = await blockchain.treasury("initAddress");<br />
        const ownerAddress = await blockchain.treasury("ownerAddress");<br />

        const myContract = blockchain.openContract(<br />
        await MainContract.createFromConfig(<br />
        {'{'}<br />
        number: 0,<br />
        address: initAddress.address,<br />
        owner_address: ownerAddress.address, // now we create myContract from 3 parameters<br />
        {'}'}<br />
        , codeCell<br />
        )<br />
        );<br />
        // ...rest of testing code`}
      </pre>

      <p>
        Теперь все в порядке, <code>yarn test</code> заканчивается успешным прохождением нашего теста. Тестируем ввод средств.
      </p>

      <p>
        Давайте добавим еще несколько тестов в наш файл <code>main.spec.ts</code>.
      </p>

      <pre>
        {`// We need to additionally import SandboxContract and TreasuryContract<br />
        import {'{'} Blockchain, SandboxContract, TreasuryContract {'}'} from "@ton/sandbox";<br />
        // ... other library imports
        describe("main.fc contract tests", () => {'{'}<br />
        let blockchain: Blockchain;<br />
        let myContract: SandboxContract&lt;MainContract&gt;;<br />
        let initWallet: SandboxContract&lt;TreasuryContract&gt;;<br />
        let ownerWallet: SandboxContract&lt;TreasuryContract&gt;;<br />
        
        beforeEach(async () => {'{'}<br />
        blockchain = await Blockchain.create();<br />
        initWallet = await blockchain.treasury("initWallet");<br />
        ownerWallet = await blockchain.treasury("ownerWallet");<br />

        const codeCell = Cell.fromBoc(Buffer.from(hex, "hex"))[0];<br />

        myContract = blockchain.openContract(<br />
        await MainContract.createFromConfig(<br />
        {'{'}<br />
        number: 0,<br />
        address: initWallet.address,<br />
        owner_address: ownerWallet.address,<br />
        {'}'}<br />
        , codeCell<br />
        )<br />
        );<br />
        {'}'}<br />
        
        it("should get the proper most recent sender address", async () => {'{'}<br />
        const senderWallet = await blockchain.treasury("sender");<br />

        const sentMessageResult = await myContract.sendIncrement(<br />
        senderWallet.getSender(),<br />
        toNano("0.05"),<br />
        1<br />
        );<br />

        expect(sentMessageResult.transactions).toHaveTransaction({'{'}<br />
        from: senderWallet.address,<br />
        to: myContract.address,<br />
        success: true,<br />
        {'}'});<br />

        const data = await myContract.getData();<br />

        expect(data.recent_sender.toString()).toBe(senderWallet.address.toString());<br />
        expect(data.number).toEqual(1);<br />
        {'}'});`}<br />
      </pre>

      <p>
        Мы просто перенесли часть строк первого теста в <code>beforeEach</code>, остальные строки должны остаться внутри первого теста <code>it</code>.
      </p>

      <p>
        Давайте напишем тест для ввода средств. Мы отправим сообщение с <code>op == 2</code>, а затем проверим баланс. Как обычно, сначала создадим метод-обертку, назовем его <code>sendDeposit</code>:
      </p>

      <pre>
        async sendDeposit(provider: ContractProvider, sender: Sender, value: bigint) {'{'}<br />
        const msg_body = beginCell()<br />
        .storeUint(2, 32) // OP code<br />
        .endCell();<br />

        await provider.internal(sender, {'{'}<br />
        value,<br />
        sendMode: SendMode.PAY_GAS_SEPARATELY,<br />
        body: msg_body,<br />
        {'}'});<br />
        {'}'}<br />
      </pre>

      <p>
        Мы не будем вдаваться в подробности этого метода, поскольку он очень прост: мы просто передаем код операции внутри тела сообщения.
      </p>

      <pre>
        {`it("successfully deposits funds", async () => {'{'}<br />
        const senderWallet = await blockchain.treasury("sender");<br />

        const depositMessageResult = await myContract.sendDeposit(<br />
        senderWallet.getSender(),<br />
        toNano("5")<br />
        );<br />

        expect(depositMessageResult.transactions).toHaveTransaction({'{'}<br />
        from: senderWallet.address,<br />
        to: myContract.address,<br />
        success: true,<br />
        {'}'});<br />

        const balanceRequest = await myContract.getBalance();<br />

        expect(balanceRequest.number).toBeGreaterThan(toNano("4.99"));<br />
        {'}'});`}<br />
      </pre>

      <p>Обратите внимание: мы проверяем, что баланс контракта больше, чем 4,99 TON, поскольку мы знаем, что часть средств будет потеряна на комиссиях.</p>

      <video controls>
        <source src="/videos/4.4.mp4" type="video/mp4" />

      </video>
    </div>
  );
};

export const Lesson5_3 = () => {
  return (
    <div>
      <p>Here must be video</p>
      <h2>📚 Конспект урока</h2>
      <p>
        На данный момент мы уже реализовали авторизацию пользователя для
        взаимодействия с нашим контрактом. Теперь давайте подключим DApp к нашему
        контракту, который мы разработали ранее.
      </p>
      <p>
        Есть несколько требований, которые нужно учесть, чтобы правильно реализовать шаги в этом и последующих уроках:
      </p>
      <ul>
        <li>Мы будем работать с тестнетом. Убедитесь, что ваш контракт из урока 5 главы 4 развернут на тестнете.</li>
        <li>Убедитесь, что вы развернули этот контракт с нужными исходными данными, а именно:</li>
        <ul>
          <li>значение счетчика,</li>
          <li>адрес последнего отправителя,</li>
          <li>
            адрес владельца — это должен быть адрес вашего активного кошелька в
            тестнете, поскольку вы собираетесь выполнять операции по выводу
            средств, а как вы помните, логика нашего контракта будет отклонять
            любые попытки вывода средств не от владельца.
          </li>
        </ul>
        <li>
          Убедитесь, что у вас есть монеты для тестнета. Вы можете легко получить
          их здесь.
        </li>
      </ul>
      <p>
        К этому моменту я предполагаю, что вы развернули свой контракт из урока 5
        главы 4 на тестнете и знаете его адрес. Вот адрес моего контракта:
      </p>
      <p>
        <code>EQCS7PUYXVFI-4uvP1_vZsMVqLDmzwuimhEPtsyQKIcdeNPu</code>
      </p>
      <p>
        Как вы помните, мы уже поработали с нашим контрактом во время написания
        тестов. Мы создали файл-обертку, который помогал нам отправлять внутренние
        сообщения контракту, а также запускали getter-методы. Приятный сюрприз:
        сейчас мы можем использовать тот же самый файл-обертку :)
      </p>
      <p>
        Давайте создадим новую папку <code>contracts</code> в папке <code>src</code> и
        скопируем файл <code>wrapper/MainContract.ts</code> из предыдущей части. Вы также
        можете найти его в моем репозитории.
      </p>
      <p>
        Теперь у нас есть подходящая обертка для контракта, но как нам связать его
        с ончейн-контрактом из браузера? В третьей главе мы определяли, развернут
        контракт или нет, используя экземпляр <code>TonClient</code> из библиотеки{' '}
        <code>ton</code>. Он использовал RPC-провайдер ton-access для получения информации
        из блокчейна (спасибо команде Orbs) и отправки запросов нашему контракту.
      </p>
      <p>
        Прежде чем мы начнем, давайте сделаем «очень React-овую» вещь — реализуем
        react-хук общего назначения, который поможет нам инициализировать
        async-объекты. Мы будем использовать его для соединения с TonClient.
      </p>
      <p>
        Создайте файл <code>src/hooks/useAsyncInitialize.ts</code> с содержимым:
      </p>
      <pre>
        <code>{`
import { useEffect, useState } from "react";

export function useAsyncInitialize<T>(func: () => Promise<T>, deps: any[] = []) {
  const [state, setState] = useState<T | undefined>();

  useEffect(() => {
    (async () => {
      setState(await func());
    })();
  }, deps);

  return state;
}
`}</code>
      </pre>
      <p>
        Далее мы создадим еще один react-хук, который будет опираться на
        useAsyncInitialize и инициализировать RPC-клиент в нашем приложении.
      </p>
      <p>
        Создайте <code>src/hooks/useTonClient.ts</code> с содержимым:
      </p>
      <pre>
        <code>{`
import { getHttpEndpoint } from "@orbs-network/ton-access";
import { TonClient } from "ton";
import { useAsyncInitialize } from "./useAsyncInitialize";

export function useTonClient() {
  return useAsyncInitialize(
    async () =>
      new TonClient({
        endpoint: await getHttpEndpoint({ network: "testnet" }),
      })
  );
}
`}</code>
      </pre>
      <p>
        Создадим еще один хук под названием <code>useMainContract()</code>.
      </p>
      <p>
        Вот как выглядит код этого хука:
      </p>
      <pre>
        <code>{`
import { useEffect, useState } from "react";
import { MainContract } from "../contracts/MainContract";
import { useTonClient } from "./useTonClient";
import { useAsyncInitialize } from "./useAsyncInitialize";
import { Address, OpenedContract } from "ton-core";

export function useMainContract() {
  const client = useTonClient();
  const [contractData, setContractData] = useState(null);

  const mainContract = useAsyncInitialize(async () => {
    if (!client) return;
    const contract = new MainContract(
      Address.parse("EQCS7PUYXVFI-4uvP1_vZsMVqLDmzwuimhEPtsyQKIcdeNPu")
    );
    return client.open(contract) as OpenedContract<MainContract>;
  }, [client]);

  useEffect(() => {
    async function getValue() {
      if (!mainContract) return;
      setContractData(null);
      const val = await mainContract.getData();
      setContractData({
        counter_value: val.number,
        recent_sender: val.recent_sender,
        owner_address: val.owner_address,
      });
    }
    getValue();
  }, [mainContract]);

  return {
    contract_address: mainContract?.address.toString(),
    ...contractData,
  };
}
`}</code>
      </pre>
      <p>Теперь мы готовы показать данные в интерфейсе.</p>
      <p>Заменим содержимое <code>src/App.tsx</code>:</p>
      <pre>
        <code>{`
import "./App.css";
import { TonConnectButton } from "@tonconnect/ui-react";
import { useMainContract } from "./hooks/useMainContract";

function App() {
  const {
    contract_address,
    counter_value,
    recent_sender,
    owner_address,
    contract_balance,
  } = useMainContract();

  return (
    <div>
      <div>
        <TonConnectButton />
      </div>
      <div>
        <div className="Card">
          <b>Our contract Address</b>
          <div className="Hint">{contract_address?.slice(0, 30) + "..."}</div>
          <b>Our contract Balance</b>
          <div className="Hint">{contract_balance}</div>
        </div>
        <div className="Card">
          <b>Counter Value</b>
          <div>{counter_value ?? "Loading..."}</div>
        </div>
      </div>
    </div>
  );
}

export default App;
`}</code>
      </pre>
      <p>
        Теперь все готово, и данные контракта можно читать из блокчейна.
      </p>
    </div>
  );
}


export const Lesson4_5 = () => {
  return (
    <div>
      <h1>📚 Конспект урока</h1>
      <p>
        В начале нашего пути в программирование на FunC мы говорили, что существует отличная библиотека/инструмент под названием Blueprint. Blueprint разработан командой TonTech и официально поддерживается TON Foundation.
      </p>
      <p>
        На протяжении уроков мы создавали свои собственные скрипты с нуля, чтобы вы могли понять, что именно происходит под капотом в проектах, созданных с помощью Blueprint. Теперь же я рекомендую начинать каждый новый проект со следующей команды:
      </p>
      <code>npm create ton@latest</code>
      <p>
        Эта команда с помощью Blueprint сгенерирует для вас новый проект, включающий код для всех этапов разработки контракта, которые мы проходили в третьей главе.
      </p>
      <p>
        В этой главе мы не будем оптимизировать наш проект, чтобы он работал так же, как проекты, сгенерированные с помощью Blueprint. Вместо этого мы слегка обновим наш проект в части компиляции и деплоя, чтобы он использовал компоненты, импортированные из Blueprint.
      </p>
      <h2>Делегируем Blueprint компиляцию</h2>
      <p>
        Прежде всего нам нужно будет обновить файл package.json. С этого момента мы оставим в нем только один скрипт — test. Мы делаем это, потому что, как уже говорилось, компиляция и деплой будут выполняться компонентами, импортированными из Blueprint.
      </p>
      <code>
        {`"scripts": {"test": "jest" }`}
      </code>
      <p>
        Однако если мы запустим yarn test сейчас, команда просто начнет тесты на контракте без его предварительной компиляции. Давайте это исправим.
      </p>
      <p>
        Как я уже говорил, мы будем использовать набор компиляторов из Blueprint, так что давайте установим эту замечательную библиотеку:
      </p>
      <code>yarn add @ton/blueprint --dev</code>
      <p>
        В файле tests/main.spec.ts мы должны сделать несколько важных обновлений. Вот как он выглядел раньше (мы приводим здесь только начальную часть, перед реальными тестами):
      </p>
      <pre>
        {`
import { Cell, toNano } from "@ton/core";
import { hex } from "../build/main.compiled.json";
import { Blockchain, SandboxContract, TreasuryContract } from "@ton/sandbox";
import { MainContract } from "../wrappers/MainContract";
import "@ton/test-utils";

describe("main.fc contract tests", () => {
  let blockchain: Blockchain;
  let myContract: SandboxContract<MainContract>;
  let initWallet: SandboxContract<TreasuryContract>;
  let ownerWallet: SandboxContract<TreasuryContract>;

  beforeEach(async () => {
    blockchain = await Blockchain.create();
    initWallet = await blockchain.treasury("initWallet");
    ownerWallet = await blockchain.treasury("ownerWallet");

    const codeCell = Cell.fromBoc(Buffer.from(hex, "hex"))[0];

    myContract = blockchain.openContract(
      await MainContract.createFromConfig(
        {
          number: 0,
          address: initWallet.address,
          owner_address: ownerWallet.address,
        },
        codeCell
      )
    );
  });
  // ... the rest of testing code
});
        `}
      </pre>
      <p>
        Внесем в него 3 изменения:
        <ul>
          <li>Импортируем функцию compile из библиотеки @ton-blueprint.</li>
          <li>Реализуем хук beforeAll из jest для компиляции кода и предоставления его для остальных хуков и тестов.</li>
          <li>Обновим хук beforeEach, чтобы использовать ячейку кода из хука beforeAll.</li>
        </ul>
      </p>
      <p>Вот как будет выглядеть наш код после изменений:</p>
      <pre>
        {`
import { compile } from "@ton/blueprint";

describe("main.fc contract tests", () => {
  let blockchain: Blockchain;
  let myContract: SandboxContract<MainContract>;
  let initWallet: SandboxContract<TreasuryContract>;
  let ownerWallet: SandboxContract<TreasuryContract>;
  let codeCell: Cell;

  beforeAll(async () => {
    codeCell = await compile("MainContract");
  });

  beforeEach(async () => {
    blockchain = await Blockchain.create();
    initWallet = await blockchain.treasury("initWallet");
    ownerWallet = await blockchain.treasury("ownerWallet");

    myContract = blockchain.openContract(
      await MainContract.createFromConfig(
        {
          number: 0,
          address: initWallet.address,
          owner_address: ownerWallet.address,
        },
        codeCell
      )
    );
  });
  
  // ... the rest of testing code
});
        `}
      </pre>
      <h2>Делегируем Blueprint деплой</h2>
      <p>
        Следующее, что нам нужно сделать, — обновить наш скрипт развертывания, используя суперспособности Blueprint. Сразу оговорюсь: мы будем использовать нашу обертку MainContract и ее метод .createFromConfig. Так мы сможем легко настроить начальное состояние нашего контракта перед его развертыванием.
      </p>
      <p>
        В этом уроке мы снова будем использовать кошелек Tonhub для тестнета и мейннета. Вам будет предложено отсканировать QR-код и авторизовать ваше приложение для взаимодействия с кошельком (запрос транзакций).
      </p>
      <p>
        Поздравляем! Вы узнали еще один способ развертывания контрактов — причем довольно эффективный! :)
      </p>
    </div>
  );
};


export const Lesson5_5 = () => {
  return (
    <div>

      <video controls>
        <source src="/videos/5.5.mp4" type="video/mp4" />

      </video>
      <h1>📚 Конспект урока</h1>
      <p>
        В начале нашего пути в программирование на FunC мы говорили, что существует отличная библиотека/инструмент под названием Blueprint. Blueprint разработан командой TonTech и официально поддерживается TON Foundation.
      </p>
      <p>
        На протяжении уроков мы создавали свои собственные скрипты с нуля, чтобы вы могли понять, что именно происходит под капотом в проектах, созданных с помощью Blueprint. Теперь же я рекомендую начинать каждый новый проект со следующей команды:
      </p>
      <code>npm create ton@latest</code>
      <p>
        Эта команда с помощью Blueprint сгенерирует для вас новый проект, включающий код для всех этапов разработки контракта, которые мы проходили в третьей главе.
      </p>
      <p>
        В этой главе мы не будем оптимизировать наш проект, чтобы он работал так же, как проекты, сгенерированные с помощью Blueprint. Вместо этого мы слегка обновим наш проект в части компиляции и деплоя, чтобы он использовал компоненты, импортированные из Blueprint.
      </p>
      <h2>Делегируем Blueprint компиляцию</h2>
      <p>
        Прежде всего нам нужно будет обновить файл package.json. С этого момента мы оставим в нем только один скрипт — test. Мы делаем это, потому что, как уже говорилось, компиляция и деплой будут выполняться компонентами, импортированными из Blueprint.
      </p>
      <code>
        {`"scripts": {"test": "jest" }`}
      </code>
      <p>
        Однако если мы запустим yarn test сейчас, команда просто начнет тесты на контракте без его предварительной компиляции. Давайте это исправим.
      </p>
      <p>
        Как я уже говорил, мы будем использовать набор компиляторов из Blueprint, так что давайте установим эту замечательную библиотеку:
      </p>
      <code>yarn add @ton/blueprint --dev</code>
      <p>
        В файле tests/main.spec.ts мы должны сделать несколько важных обновлений. Вот как он выглядел раньше (мы приводим здесь только начальную часть, перед реальными тестами):
      </p>
      <pre>
        {`
import { Cell, toNano } from "@ton/core";
import { hex } from "../build/main.compiled.json";
import { Blockchain, SandboxContract, TreasuryContract } from "@ton/sandbox";
import { MainContract } from "../wrappers/MainContract";
import "@ton/test-utils";

describe("main.fc contract tests", () => {
  let blockchain: Blockchain;
  let myContract: SandboxContract<MainContract>;
  let initWallet: SandboxContract<TreasuryContract>;
  let ownerWallet: SandboxContract<TreasuryContract>;

  beforeEach(async () => {
    blockchain = await Blockchain.create();
    initWallet = await blockchain.treasury("initWallet");
    ownerWallet = await blockchain.treasury("ownerWallet");

    const codeCell = Cell.fromBoc(Buffer.from(hex, "hex"))[0];

    myContract = blockchain.openContract(
      await MainContract.createFromConfig(
        {
          number: 0,
          address: initWallet.address,
          owner_address: ownerWallet.address,
        },
        codeCell
      )
    );
  });
  // ... the rest of testing code
});
        `}
      </pre>
      <p>
        Внесем в него 3 изменения:
        <ul>
          <li>Импортируем функцию compile из библиотеки @ton-blueprint.</li>
          <li>Реализуем хук beforeAll из jest для компиляции кода и предоставления его для остальных хуков и тестов.</li>
          <li>Обновим хук beforeEach, чтобы использовать ячейку кода из хука beforeAll.</li>
        </ul>
      </p>
      <p>Вот как будет выглядеть наш код после изменений:</p>
      <pre>
        {`
import { compile } from "@ton/blueprint";

describe("main.fc contract tests", () => {
  let blockchain: Blockchain;
  let myContract: SandboxContract<MainContract>;
  let initWallet: SandboxContract<TreasuryContract>;
  let ownerWallet: SandboxContract<TreasuryContract>;
  let codeCell: Cell;

  beforeAll(async () => {
    codeCell = await compile("MainContract");
  });

  beforeEach(async () => {
    blockchain = await Blockchain.create();
    initWallet = await blockchain.treasury("initWallet");
    ownerWallet = await blockchain.treasury("ownerWallet");

    myContract = blockchain.openContract(
      await MainContract.createFromConfig(
        {
          number: 0,
          address: initWallet.address,
          owner_address: ownerWallet.address,
        },
        codeCell
      )
    );
  });
  
  // ... the rest of testing code
});
        `}
      </pre>
      <h2>Делегируем Blueprint деплой</h2>
      <p>
        Следующее, что нам нужно сделать, — обновить наш скрипт развертывания, используя суперспособности Blueprint. Сразу оговорюсь: мы будем использовать нашу обертку MainContract и ее метод .createFromConfig. Так мы сможем легко настроить начальное состояние нашего контракта перед его развертыванием.
      </p>
      <p>
        В этом уроке мы снова будем использовать кошелек Tonhub для тестнета и мейннета. Вам будет предложено отсканировать QR-код и авторизовать ваше приложение для взаимодействия с кошельком (запрос транзакций).
      </p>
      <p>
        Поздравляем! Вы узнали еще один способ развертывания контрактов — причем довольно эффективный! :)
      </p>
    </div>
  );
};


export const Lesson4_6 = () => (
  <div>
    <video controls>
      <source src="/videos/4.6.mp4" type="video/mp4" />

    </video>
    <h3>📚Конспект урока</h3>
    <p>
      Теперь, когда наш контракт развернут в мейннете, мы можем приступить к проверке кода контракта. Как мы уже говорили в предыдущем уроке, очень важно убедиться, что пользователи вашего контракта точно знают, что произойдет с их средствами, когда они отправят их на адрес контракта.
    </p>
    <p>
      Итак, как вы можете видеть в эксплорере после отправки вашего контракта в мейннет, код контракта не проверен. В проверенном же контракте любой, кто захочет узнать логику контракта, сможет легко ее прочитать. Как это сделать?
    </p>
    <h4>Загружаем контракт в TON Verifier</h4>
    <p>
      Недавно TON получил очень важный инструмент — TON Verifier. Сюда вы загружаете свой исходный код, он компилируется, ячейка кода форматируется, и ее хеш становится доказательством того, что код не менялся.
    </p>
    <p>
      Этот код и эта хеш-ячейка впоследствии хранятся децентрализованно, так что любой пользователь может посмотреть исходный код и убедиться, что он не менялся ранее. Таким образом, нет необходимости как-то гарантировать согласованность кода. Здесь нет какого-то централизованного органа — это просто реестр, контролируемый несколькими людьми, которые могут подтвердить неизменность вашего кода.
    </p>
    <p>
      Теперь мы подключим наш кошелек, загрузим сюда наш код и создадим транзакцию для его децентрализованного хранения в IPFS или другом сервисе «под капотом». В этом и следующих уроках мы будем использовать кошелек Tonkeeper, чтобы вы могли познакомиться с разными кошельками.
    </p>
    <p>
      Сейчас TON Verifier работает в мейннете, поэтому мы протестируем контракт в боевой среде. Обычно вы делаете это только один раз, когда разворачиваете финальный контракт. Вам не нужно беспокоиться о трате на это денег — это похоже на покупку, скажем, SSL-сертификата. Фактически вы платите за предоставление данных о вашем контракте в блокчейн, чтобы исследователи могли получить ваш код.
    </p>
    <p>
      Давайте начнем. Я сканирую QR-код с помощью кошелька Tonkeeper в мейннете и разрешаю TON Verifier запрашивать у меня транзакции. Теперь скопируем адрес контракта, который мы развернули в мейннете, и вставим его в поисковую строку TON Verifier.
    </p>
    <p>QR code to scan</p>
    <p>
      Как мы видим, контракт, очевидно, не верифицирован. Теперь нам нужно предоставить TON Verifier исходный (.func в нашем случае) код нашего контракта. Для этого открываем папку проекта, находим подпапку contracts и выбираем файл main.fc. Затем загружаем stdlib.fc из подпапки imports — в противном случае вы получите ошибку при компиляции. Наконец, укажите директорию в строке stdlib.fc на веб-странице (imports в нашем случае) и нажмите Compile.
    </p>
    <p>
      В случае успеха вы получите сообщение: «Отлично! Хеш после компиляции соответствует этому контракту в блокчейне» (Great! Compile output hash matches this on-chain contract). Нажмите Publish. Затем Tonkeeper попросит подтвердить транзакцию. Как я уже сказал, это будет стоить немного, 0,5 TON. После подтверждения транзакции потребуется некоторое время, чтобы изменения попали в сеть.
    </p>
    <p>
      Когда результаты нашей проверки будут опубликованы, мы можем нажать View Verified Contract и увидеть наш проверенный исходный код. Теперь любой, кто захочет взаимодействовать с нашим контрактом, сможет увидеть его исходный код. Здесь TON Verifier также позволяет нам запустить методы getter и получать данные о балансе.
    </p>
    <p>
      Теперь в эксплорерах наш исходный код отмечен как проверенный. Потребуется несколько минут, чтобы распространить код в децентрализованном реестре, о чем я уже говорил. После этого наш код будет доступен для чтения любому пользователю. Мы можем перепроверить его с помощью другого эксплорера, например TON API.
    </p>
    <p>
      Отлично! Теперь наш контракт проверен, и мы готовы создать фронтенд для взаимодействия с нашим контрактом. Мы развернули его в мейннете, чтобы показать, как она будет работать в реальной жизни для реального проекта. В следующей главе мы также развернем этот контракт в тестовой сети, потому что мы собираемся много тестировать и создавать фронтенд для этого контракта.
    </p>
    <h4>Деплой в тестнет</h4>
    <p>
      Как вы помните из предыдущего урока, деплой в тестнет довольно прост. Эта команда поможет вам:
    </p>
    <pre><code>yarn blueprint run</code></pre>
    <p>
      В ответ меня спросят, в какую сеть я хочу развернуть контракт. Я выбираю тестнет. Затем я выбираю тип кошелька — совместимый с TON Connect мобильный кошелек, потому что через транзакции с кошелька TonConnect мы будем авторизовывать действия на фронтенде.
    </p>
    <p>
      Для этого деплоя я также собираюсь использовать Tonkeeper. С помощью QR-кода я авторизую Tonkeeper для работы с моим локальным проектом. Я подтверждаю транзакцию. Затем отправляется транзакция деплоя контракта, я подтверждаю ее, и... потрясающе! Контракт развернут.
    </p>
    <p>
      Нам нужно сделать еще кое-что. В следующих главах мы создадим фронтенд для нашего контракта. И мы не хотим тратить реальные деньги мейннета на тестирование всех взаимодействий, с которыми мы собираемся работать. Поэтому нам нужно, чтобы наш контракт работал с адресами тестовой сети.
    </p>
    <p>
      Давайте изменим самый последний адрес и адрес владельца тестового кошелька, подключенного к моему Tonkeeper прямо сейчас:
    </p>
    <pre><code>
      {`const myContract = MainContract.createFromConfig(
  {
    number: 0, //initial value of the counter
    address: address("KQBMjM3cGuQ315MLsdqw/wEat26aG1a2b0DFHMIschNfp5R-K"),
    owner address: address("KQBM;M3cGuQ31SMLsdqwwEat26aG1a2b0DFHMIScnNfp5R-K"),
  },
);`}
    </code></pre>
    <p>
      По сути, я собираюсь изменить исходное состояние, поэтому адрес развертывания должен быть другим. И, конечно же, он будет находиться в тестнете. Я разверну контракт в тестнете с этими новыми адресами. Считайте это подготовкой к следующей главе, в которой мы создадим фронтенд.
    </p>
    <p>Повторим команду, которую мы использовали выше:</p>
    <pre><code>yarn blueprint run</code></pre>
    <p>
      Я выбираю testnet, затем Tonhub wallet, как мы делали это при других деплоях. Теперь я сканирую QR-код, чтобы открыть тестовую сеть TonHub. Там я подтверждаю транзакцию и жду, когда мой контракт будет развернут в тестнете. Ура, он развернут, и мы можем найти его адрес в проводнике.
    </p>
    <p>
      Нам не нужно проверять его здесь, мы будем использовать его в основном для тестирования. Сохраните адрес этого контракта, который вы видите в эксплорере, — он понадобится вам в следующих уроках для создания фронтенда.
    </p>
    <p>Я с нетерпением жду того, что мы будем делать дальше!</p>
  </div>
);


export const Lesson5_1 = () => (
  <div>
    <h2>💎 Добро пожаловать в пятую часть!</h2>
    <p>
      На первых уроках мы настроим наш dApp-проект (децентрализованное приложение) и реализуем нужную функциональность TonConnect. Далее перейдем к изучению взаимодействия с нашим контрактом через реальные транзакции. В завершение этой главы мы обсудим процесс публикации нашего dApp на GitHub Pages.
    </p>

    <h3>📚 Конспект урока</h3>
    <p>
      В предыдущей главе мы создали смарт-контракт с функцией счетчика, а также функциями пополнения/вывода средств. Этот смарт-контракт является бэкендом для нашего приложения. В данном уроке мы разработаем фронтенд, или пользовательский интерфейс, позволяющий конечным пользователям получать доступ к смарт-контракту через веб-браузер.
    </p>

    <p>
      Важно помнить, что приложение, которое мы создаем, является децентрализованным. Децентрализованные приложения (DApps) следуют определенным принципам.
    </p>
    <ul>
      <li>Бэкенд должен состоять исключительно из смарт-контрактов и не опираться ни на какие централизованные серверы.</li>
      <li>Фронтенд должен работать исключительно на стороне клиента и иметь открытый исходный код.</li>
    </ul>
    <p>
      Есть и другие правила, но на данный момент важнее всего эти два. Придерживаясь их, мы гарантируем, что не зависим от внутреннего сервера в работе нашего фронтенда. Если бы такой сервер использовался, он сам по себе был бы централизованным (поскольку не все конечные пользователи имели бы равный доступ), и в результате все наше приложение тоже стало бы централизованным.
    </p>

    <p>
      В этой главе мы будем работать с другим кошельком TON под названием Tonkeeper. Это позволит вам лучше понять, как работают различные кошельки. Кошелек будет взаимодействовать с нашим приложением через протокол TON Connect 2. Если вы выбрали кошелек, отличный от Tonkeeper, пожалуйста, убедитесь, что он поддерживает эту версию TON Connect. Не забудьте проверить, что ваш кошелек подключен к правильной сети — тестнету или мейннету.
    </p>

    <ul>
      <li>
        По умолчанию Tonkeeper работает в основной сети TON. Если вы решили работать в тестовой сети, вам нужно будет вручную переключить приложение в режим разработчика. Откройте вкладку настроек и быстро нажмите пять раз на версию приложения внизу. Должно появиться меню разработчика. Переключитесь в нем на тестнет. Таким же образом вы можете вернуться обратно в мейннет.
      </li>
    </ul>

    <h3>TON Connect</h3>
    <p>
      Разберемся, зачем нам вообще нужен TON Connect при создании пользовательского интерфейса нашего контракта.
    </p>
    <p>Здесь у нас в проекте участвуют 3 стороны:</p>
    <ul>
      <li>Блокчейн TON позволяет создавать масштабные приложения и сервисы с минимальным достаточным уровнем доверия.</li>
      <li>Приложения реализуют пользовательский интерфейс для бесконечного набора функций TON, основанных на смарт-контрактах, но при этом не имеют мгновенного доступа к средствам пользователей.</li>
      <li>Кошельки предоставляют пользовательский интерфейс для подтверждения транзакций и надежно хранят криптографические ключи пользователей на их персональных устройствах.</li>
    </ul>
    <p>
      Такое разделение позволяет быстро внедрять инновации и обеспечивать высокий уровень безопасности для пользователей: кошелькам не нужно самим строить защищенные системы, а приложениям не нужно брать на себя риск хранения счетов конечных пользователей.
    </p>
    <p>
      TON Connect — мост, который преодолевает этот концептуальный разрыв. Если кратко, TON Connect упрощает взаимодействие приложений с блокчейном TON через кошельки конечных пользователей.
    </p>
    <p>
      Иногда вы можете встретить TON Connect 2, это самая современная версия протокола. Используйте кошельки, которые поддерживают TON Connect 2.
    </p>

    <h3>Настраиваем проект нашего dApp</h3>
    <p>Мы создадим наш фронтенд с помощью React. Для проекта будем использовать Vite и его шаблон React. Выберите имя для проекта, например counter-front-end, затем откройте терминал и выполните следующие действия:</p>
    <pre><code>yarn create vite counter-front-end --template react-ts
      cd counter-front-end && yarn</code></pre>

    <p>Нужно будет установить еще несколько пакетов для взаимодействия с блокчейном TON. Мы уже видели их в действии в предыдущих главах при написании тестов и скриптов компиляции/развертывания. Выполните следующие команды в командной строке:</p>
    <pre><code>yarn add ton ton-core ton-crypto
      yarn add @orbs-network/ton-access</code></pre>

    <p>И последнее, но не менее важное: нам нужно преодолеть зависимость библиотеки TON от Nodejs Buffer, который недоступен в браузере. Мы можем сделать это, установив полифил:</p>
    <pre><code>yarn add vite-plugin-node-polyfills</code></pre>

    <p>Чтобы завершить исправление Buffer, обновите файл <code>vite.config.ts</code>:</p>
    <pre><code>
      {`import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import { nodePolyfills } from 'vite-plugin-node-polyfills';

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [react(), nodePolyfills()],
  base: '/',
});
`}
    </code></pre>
    <video controls>
      <source src="/videos/5.1.mp4" type="video/mp4" />

    </video>
    <p>Чтобы увидеть пустой шаблон приложения, запустите его в командной строке:</p>
    <pre><code>yarn dev</code></pre>
    <p>Затем откройте веб-браузер и откройте ссылку из результатов CLI (что-нибудь вроде <code>http://localhost:5173/</code>). Вы увидите новую страницу с содержимым TypeScript-шаблона Vite.</p>
    <p>Ура! Мы готовы начать работу с TON Connect, чтобы аутентифицировать конечного пользователя для работы с нашим DApp.</p>

    <p><i>P. S. За вдохновение для некоторых частей этого руководства спасибо потрясающему Шахару Якиру из команды Orbs.</i></p>
  </div>
);


export const Lesson5_2 = () => (
  <div>
    <video controls>
      <source src="/videos/5.2.mp4" type="video/mp4" />

    </video>
    <h2>📚 Конспект урока</h2>
    <p>Библиотека TON Connect React предоставит нам несколько полезных сервисов:</p>
    <ul>
      <li>Отображение конечному пользователю списка кошельков, поддерживающих TON Connect 2</li>
      <li>Запрос публичного адреса кошелька пользователя</li>
      <li>Выполнение запроса на транзакцию через кошелек</li>
    </ul>

    <h3>Установите библиотеку через терминал:</h3>
    <pre><code>yarn add @tonconnect/ui-react</code></pre>

    <p>
      Когда наше приложение подключается к кошельку пользователя, мы должны предоставить ему информацию о себе.
      «Кто запрашивает подтверждение?». Нам нужно идентифицировать себя с помощью файла манифеста. Кошелек будет
      запрашивать у пользователя разрешение на подключение к приложению и отображение информации из манифеста.
    </p>

    <h3>Структура файла манифеста выглядит так:</h3>
    <pre><code>
      {`{
  "url": "",
  "name": "Example",
  "iconUrl": ""
}`}
    </code></pre>

    <p>Я буду использовать это содержимое для файла manifest.json:</p>
    <pre><code>
      {`{
  "url": "https://join.toncompany.org",
  "name": "TON&Co. Tutorial",
  "iconUrl": "https://raw.githubusercontent.com/markokhman/func-course-chapter-5-code/master/public/tonco.png"
}`}
    </code></pre>

    <p>
      Как только я создам файл <code>public/manifest.json</code>, добавлю туда это содержимое и отправлю в удаленный репозиторий,
      мой манифест будет доступен здесь:
    </p>
    <a href="https://raw.githubusercontent.com/markokhman/func-course-chapter-5-code/master/public/manifest.json" target="_blank" rel="noopener noreferrer">
      https://raw.githubusercontent.com/markokhman/func-course-chapter-5-code/master/public/manifest.json
    </a>

    <p>
      Манифест должен быть общедоступным в интернете. Вы можете либо последовать моему примеру и создать свой собственный манифест,
      либо пока что использовать пример, который я развернул заранее во время разработки. Позже, когда мы развернем наш сайт,
      вы должны будете заменить этот пример на свой реальный манифест.
    </p>

    <h3>Измените файл <code>src/main.tsx</code> , чтобы использовать провайдер TON Connect:</h3>
    <pre><code>
      {`import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';
import './index.css';
import { TonConnectUIProvider } from '@tonconnect/ui-react';

// this manifest is used temporarily for development purposes
const manifestUrl = 'https://raw.githubusercontent.com/ton-community/tutorials/main/03-client/test/public/tonconnect-manifest.json';

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <TonConnectUIProvider manifestUrl={manifestUrl}>
    <App />
  </TonConnectUIProvider>,
)`}
    </code></pre>

    <h3>Добавляем кнопку Connect в приложение</h3>
    <p>
      Первое, что мы предложим пользователю, — это подключить его кошелек к приложению. Подключаясь, пользователь соглашается предоставить
      приложению публичный адрес своего кошелька. Это не очень опасно, так как вся история транзакций кошелька и его баланс и так
      находятся в открытом доступе в блокчейне.
    </p>

    <h3>Измените содержимое файла <code>src/App.tsx</code> на:</h3>
    <pre><code>
      {`import './App.css';
import { TonConnectButton } from '@tonconnect/ui-react';

function App() {
  return (
    <div>
      <TonConnectButton />
    </div>
  );
}

export default App;`}
    </code></pre>

    <h3>Запуск приложения</h3>
    <p>Для запуска приложения введите в командной строке:</p>
    <pre><code>npm run dev</code></pre>
    <p>
      Затем обновите браузер, демонстрирующий вашу предыдущую ссылку. Я предполагаю, что вы используете браузер на компьютере,
      где вы ведете разработку, а кошелек Tonkeeper установлен на вашем мобильном устройстве. Нажмите Connect Wallet на рабочем
      столе и выберите Tonkeeper (или любой другой поддерживаемый кошелек, который вы используете).
    </p>

    <p>
      TON Connect поддерживает работу как в режиме смартфон-смартфон, так и в режиме компьютер-смартфон. Поскольку наша разработка
      проходит в режиме компьютер-смартфон, для связи с кошельком на вашем смартфоне TON Connect будет использовать сканирование QR-кодов.
      Откройте мобильное приложение Tonkeeper, нажмите на кнопку QR в правом верхнем углу и отсканируйте код с экрана настольного компьютера.
    </p>

    <p>
      Если все подключено правильно, вы должны увидеть подтверждающий диалог в мобильном приложении кошелька. Когда вы одобрите
      соединение, то увидите свой адрес в пользовательском интерфейсе веб-приложения!
    </p>

    <p><i>P. S. За вдохновение для некоторых частей этого руководства спасибо потрясающему Шахару Якиру из команды Orbs.</i></p>
  </div>
);


export const Lossen5_3 = () => {
  return (
    <div>
      <p>Here must be video</p>
      <h2>📚 Конспект урока</h2>
      <p>
        На данный момент мы уже реализовали авторизацию пользователя для
        взаимодействия с нашим контрактом. Теперь давайте подключим DApp к нашему
        контракту, который мы разработали ранее.
      </p>
      <p>Есть несколько требований, которые нужно учесть, чтобы правильно реализовать шаги в этом и последующих уроках:</p>
      <ul>
        <li>Мы будем работать с тестнетом. Убедитесь, что ваш контракт из урока 5 главы 4 развернут на тестнете.</li>
        <li>Убедитесь, что вы развернули этот контракт с нужными исходными данными, а именно:</li>
        <ul>
          <li>значение счетчика,</li>
          <li>адрес последнего отправителя,</li>
          <li>
            адрес владельца — это должен быть адрес вашего активного кошелька в
            тестнете, поскольку вы собираетесь выполнять операции по выводу
            средств, а как вы помните, логика нашего контракта будет отклонять
            любые попытки вывода средств не от владельца.
          </li>
        </ul>
        <li>
          Убедитесь, что у вас есть монеты для тестнета. Вы можете легко получить
          их здесь.
        </li>
      </ul>
      <p>
        К этому моменту я предполагаю, что вы развернули свой контракт из урока 5
        главы 4 на тестнете и знаете его адрес. Вот адрес моего контракта:
      </p>
      <p>
        <code>EQCS7PUYXVFI-4uvP1_vZsMVqLDmzwuimhEPtsyQKIcdeNPu</code>
      </p>
      <p>
        Как вы помните, мы уже поработали с нашим контрактом во время написания
        тестов. Мы создали файл-обертку, который помогал нам отправлять внутренние
        сообщения контракту, а также запускали getter-методы. Приятный сюрприз:
        сейчас мы можем использовать тот же самый файл-обертку :)
      </p>
      <p>
        Давайте создадим новую папку <code>contracts</code> в папке <code>src</code> и
        скопируем файл <code>wrapper/MainContract.ts</code> из предыдущей части. Вы также
        можете найти его в моем репозитории. Файл-обертка контракта для нашего DApp
        находится по адресу <code>src/contracts/MainContract.ts</code>.
      </p>
      <p>
        Теперь у нас есть подходящая обертка для контракта, но как нам связать его
        с ончейн-контрактом из браузера? В третьей главе мы определяли, развернут
        контракт или нет, используя экземпляр <code>TonClient</code> из библиотеки{' '}
        <code>ton</code>. Он использовал RPC-провайдер ton-access для получения информации
        из блокчейна (спасибо команде Orbs) и отправки запросов нашему контракту. В этом
        уроке мы будем использовать такую же логику.
      </p>
      <p>
        Прежде чем мы начнем, давайте сделаем «очень React-овую» вещь — реализуем
        react-хук общего назначения, который поможет нам инициализировать
        async-объекты. Мы будем использовать его для соединения с TonClient.
      </p>
      <p>
        Создайте файл <code>src/hooks/useAsyncInitialize.ts</code> с содержимым:
      </p>
      <pre>
        <code>
          {`import {"{ useEffect, useState }"} from "react";
          <br />
          export function useAsyncInitialize {"<T>"}(func: () => Promise{"<T>"},
          deps: any[] = []) {"{"} <br />
          {"  "}const [state, setState] = useState{"<T | undefined>"}(); <br />
          {"  "}useEffect(() => {"{"} <br />
          {"    "}(async () => {"{"} <br />
          {"      "}setState(await func()); <br />
          {"    "}})(); <br />
          {"  "}}, deps); <br />
          {"  "}return state; <br />
          {"}"}`}
        </code>
      </pre>
      <p>
        Далее мы создадим еще один react-хук, который будет опираться на
        useAsyncInitialize и инициализировать RPC-клиент в нашем приложении. Как
        мы уже упоминали выше, будем использовать TON Access, который предоставит
        нам бесплатный доступ к API без троттлинга. Он также децентрализован, что
        является для нас предпочтительным способом доступа к сети.
      </p>
      <p>
        Создайте <code>src/hooks/useTonClient.ts</code> с содержимым:
      </p>
      <pre>
        <code>
          {`import {"{ getHttpEndpoint }"} from "@orbs-network/ton-access"; <br />
          import {"{ TonClient }"} from "ton"; <br />
          import {"{ useAsyncInitialize }"} from "./useAsyncInitialize"; <br />
          export function useTonClient() {"{"} <br />
          {"  "}return useAsyncInitialize( <br />
          {"    "}async () => <br />
          {"      "}new TonClient({"{"} <br />
          {"        "}endpoint: await getHttpEndpoint({"{"} network: "testnet" {"}"}), <br />
          {"      "}}) <br />
          ); <br />
          {"}"}`}
        </code>
      </pre>
      <p>
        Создадим еще один хук под названием <code>useMainContract()</code>, который
        будет принимать ончейн-адрес развернутого контракта и запускать
        getter-метод нашего контракта (с помощью метода .getData() нашей обертки).
      </p>
      <p>
        Вот как выглядит код этого хука:
      </p>
      <pre>
        <code>
          {`import {"{ useEffect, useState }"} from "react"; <br />
          import {"{ MainContract }"} from "../contracts/MainContract"; <br />
          import {"{ useTonClient }"} from "./useTonClient"; <br />
          import {"{ useAsyncInitialize }"} from "./useAsyncInitialize"; <br />
          import {"{ Address, OpenedContract }"} from "ton-core"; <br /><br />
          export function useMainContract() {"{"} <br />
          {"  "}const client = useTonClient(); <br />
          {"  "}const [contractData, setContractData] = useState{"<null | { counter_value: number; recent_sender: Address; owner_address: Address; }>()"}(); <br /><br />
          {"  "}const mainContract = useAsyncInitialize(async () => {"{"} <br />
          {"    "}if (!client) return; <br />
          {"    "}const contract = new MainContract( <br />
          {"      "}Address.parse("EQCS7PUYXVFI-4uvP1_vZsMVqLDmzwuimhEPtsyQKIcdeNPu") <br />
          {"    "}); <br />
          {"    "}return client.open(contract) as OpenedContract{"<MainContract>"}; <br />
          {"  "}}, [client]); <br /><br />
          {"  "}useEffect(() => {"{"} <br />
          {"    "}async function getValue() {"{"} <br />
          {"      "}if (!mainContract) return; <br />
          {"      "}setContractData(null); <br />
          {"      "}const val = await mainContract.getData(); <br />
          {"      "}setContractData({"{"} <br />
          {"        "}counter_value: val.number, <br />
          {"        "}recent_sender: val.recent_sender, <br />
          {"        "}owner_address: val.owner_address, <br />
          {"      "}}); <br />
          {"    "}} <br />
          {"    "}getValue(); <br />
          {"  "}}, [mainContract]); <br /><br />
          {"  "}return {"{"} <br />
          {"    "}contract_address: mainContract?.address.toString(), <br />
          {"    "...contractData, <br />
          {"  "}}; <br />
          {"}"}`}
        </code>
      </pre>
      <p>
        Теперь мы готовы показать в нашем интерфейсе данные, которые мы прочитали
        из блокчейна.
      </p>
      <p>
        Заменим содержимое нашего файла <code>src/App.tsx</code>:
      </p>
      <pre>
        <code>
          import "./App.css"; <br />
          import {"{ TonConnectButton }"} from "@tonconnect/ui-react"; <br />
          import {"{ useMainContract }"} from "./hooks/useMainContract"; <br /><br />
          function App() {"{"} <br />
          {"  "}const {"{"} <br />
          {"    "}contract_address, <br />
          {"    "}counter_value, <br />
          {"    "}recent_sender, <br />
          {"    "}owner_address, <br />
          {"    "}contract_balance, <br />
          {"  "} = useMainContract(); <br /><br />
          {"  "}return ( <br />
          {"    "}&lt;div&gt; <br />
          {"      "}&lt;div&gt; <br />
          {"        "}&lt;TonConnectButton /&gt; <br />
          {"      "}&lt;/div&gt; <br />
          {"      "}&lt;div&gt; <br />
          {"        "}&lt;div className='Card'&gt; <br />
          {"          "}&lt;b&gt;Our contract Address&lt;/b&gt; <br />
          {"          "}&lt;div className='Hint'&gt;{"{"}contract_address?.slice(0, 30) + "..."{"}"}&lt;/div&gt; <br />
          {"          "}&lt;b&gt;Our contract Balance&lt;/b&gt; <br />
          {"          "}&lt;div className='Hint'&gt;{"{"}contract_balance{"}"}&lt;/div&gt; <br />
          {"        "}&lt;/div&gt; <br />
          {"        "}&lt;div className='Card'&gt; <br />
          {"          "}&lt;b&gt;Counter Value&lt;/b&gt; <br />
          {"          "}&lt;div&gt;{"{"}counter_value ?? "Loading..."{"}"}&lt;/div&gt; <br />
          {"        "}&lt;/div&gt; <br />
          {"      "}&lt;/div&gt; <br />
          {"    "}&lt;/div&gt; <br />
          ); <br />
          {"}"}
        </code>
      </pre>
      <p>
        Отлично, на этом этапе все готово, и данные нашего контракта легко читаются
        из блокчейна.
      </p>
      <p>
        В следующем уроке мы будем выполнять эти действия для отправки инкремента,
        пополнения и вывода средств.
      </p>

      <video controls>
        <source src="/videos/5.3.mp4" type="video/mp4" />

      </video>

    </div>
  );
}

export const Lesson5_4 = () => {
  return (
    <div>
      <div>
        <video controls>
          <source src="/videos/5.4.mp4" type="video/mp4" />

        </video>
        <p>Here must be video</p>
        <h2>📚 Конспект урока</h2>
        <p>
          Мы настроили взаимодействие только для чтения, теперь давайте
          поработаем с контрактом через отправку транзакций.
        </p>
        <p>
          Первая функция, которую мы разработали для нашего контракта, — это
          инкремент. Добавим на главный экран кнопку, которая отправляет
          соответствующую транзакцию. Как вы помните, отправка транзакции в
          блокчейне расходует газ, поэтому мы ожидаем, что пользователь одобрит
          это действие в кошельке, который покажет, сколько монет TON будет
          потрачено.
        </p>
        <p>
          Прежде чем начать, мы добавим еще один хук, который сгенерирует объект
          отправителя из интерфейса TON Connect. Этот отправитель представляет
          подключенный кошелек и позволит нам отправлять транзакции от его
          имени. Пока мы работаем над запросом транзакции, мы также будем
          показывать в UI состояние подключения кошелька, чтобы наш UI скрывал
          элементы, требующие авторизации.
        </p>
        <p>
          Как и в предыдущих уроках, давайте используем react-хук для добавления
          некоторой функциональности, которую мы будем использовать в нашем
          коде позже.
        </p>
        <p>Создадим <code>src/hooks/useTonConnect.ts</code> с содержимым:</p>
        <pre>
          <code>
            {`import { useTonConnectUI } from '@tonconnect/ui-react';
  import { Sender, SenderArguments } from 'ton-core';
  
  export function useTonConnect() {
	const [tonConnectUI] = useTonConnectUI();
  
	return {
	  sender: {
		send: async (args: SenderArguments) => {
		  tonConnectUI.sendTransaction({
			messages: [{
			  address: args.to.toString(),
			  amount: args.value.toString(),
			  payload: args.body?.toBoc().toString('base64'),
			}],
			validUntil: Date.now() + 5 * 60 * 1000,
		  });
		}
	  },
	  connected: tonConnectUI.connected,
	};
  }`}
          </code>
        </pre>
        <p>Теперь улучшим наш хук useMainContract двумя небольшими функциями.</p>
        <ul>
          <li>
            Автоматический опрос счетчика каждые 5 секунд. Это нужно, чтобы
            показать пользователю, что значение действительно изменилось.
          </li>
          <li>
            Демонстрация функции sendIncrement в нашем классе интерфейса и
            подключение ее к отправителю.
          </li>
        </ul>
        <p>Обновим код, добавив несколько вещей:</p>
        <pre>
          <code>
            {`import { toNano } from "ton-core";
  import { useTonConnect } from "./useTonConnect";
  
  export function useMainContract() {
	const { sender } = useTonConnect();
  
	const sleep = (time: number) => new Promise((resolve) =>
	  setTimeout(resolve, time)
	);
  
	useEffect(() => {
	  async function getValue() {
		await sleep(5000);
		getValue();
	  }
	  getValue();
	}, [counterContract]);
  
	return {
	  value: val,
	  address: counterContract?.address.toString(),
	  sendIncrement: () => {
		return mainContract?.sendIncrement(sender, toNano(0.05), 3);
	  }
	};
  }`}
          </code>
        </pre>
        <p>Теперь обновим интерфейс:</p>
        <pre>
          <code>
            {`import { useTonConnect } from "./hooks/useTonConnect";
  
  function App() {
	const { sendIncrement } = useMainContract();
	const { connected } = useTonConnect();
  
	return (
	  <div>
		{connected && (
		  <a onClick={() => sendIncrement()}>Increment</a>
		)}
	  </div>
	);
  }`}
          </code>
        </pre>
      </div >
    </div >
  );
}


export const Lesson9 = () => {
  return (
    <div>
      <video controls>
        <source src="/videos/9.2.mp4" type="video/mp4" />
        Ваш браузер не поддерживает видео.
      </video>
      
      <video controls>
        <source src="/videos/9.3.mp4" type="video/mp4" />
        Ваш браузер не поддерживает видео.
      </video>
      
      <video controls>
        <source src="/videos/9.4.mp4" type="video/mp4" />
        Ваш браузер не поддерживает видео.
      </video>
      
      <video controls >
        <source src="/videos/9.5.mp4" type="video/mp4" />
        Ваш браузер не поддерживает видео.
      </video>
    </div>
  );
};
